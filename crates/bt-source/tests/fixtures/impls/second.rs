// A second inherent block for the same type, in a second file and naming it
// the way a move writes it. One type, four blocks, two files.

impl super::Gate {
    pub fn shut(&self) -> u8 {
        let _ = "renewable_deadline";
        2
    }
}
