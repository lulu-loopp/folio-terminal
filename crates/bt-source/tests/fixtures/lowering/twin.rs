// A second file, so the union has a boundary in it and a needle spanning the
// join cannot be manufactured: this file ends with `tail` and `lib.rs` is the
// file before it.
pub fn tail() -> u8 {
    9
}
