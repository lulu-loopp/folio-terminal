// **A decoy.** A walk that treated `reached.rs` as owning a directory named
// after itself would take this file and say nothing. rustc never compiles it,
// so the enumeration must report it UNREACHED.
