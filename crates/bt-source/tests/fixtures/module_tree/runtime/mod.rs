// The module a relocation creates: its own bytes are this file, and what it
// declares is a file of its own that an exact path does not reach.

mod quake;

pub const IN_THE_MODULE_FILE: &str = "the_needle_the_union_counts";
