## Packages in the resolved dependency graph

Generated by `cargo about` from `about.toml` and this template, over every target
and including development dependencies. One section per distinct licence **text**,
not per licence name: two crates under MIT ship two different copyright lines, and
a notice file that printed the terms once and the copyrights never would be the
wrong way round.

Folio's own `bt-*` crates are in the list too, under this product's own terms;
so are the vendored `alacritty_terminal` and `portable-pty`, which the section
above covers again and at greater length, because a licence name is not the whole
of what has to be said about a modified copy.

| Licence | Packages |
|---|---:|
{{#each overview}}| {{{name}}} | {{count}} |
{{/each}}

{{#each licenses}}
### {{{name}}}

Used by {{len used_by}} package(s):

{{#each used_by}}
- **{{{crate.name}}} {{{crate.version}}}**{{#if crate.repository}} — <{{{crate.repository}}}>{{/if}}
{{/each}}

````````text
{{{text}}}
````````

{{/each}}
