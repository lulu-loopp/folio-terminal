/target
/target-probe/
/target-m1-render-probe/
**/*.rs.bk
.playwright-mcp/
*.png
!docs/design/wordmark-r2/*.png
!docs/design/file-icons-r2/*.png
!docs/design/file-icons-r3/*.png
!docs/design/app-icon/*.png
# The three logos `packaging/msix/AppxManifest.xml` names. Checked in, and
# not generated at packaging time, because a release must not need a Python
# to build; `assets/app-icon/make-msix-logos.py` is how they can be
# checked rather than how they are obtained.
!packaging/msix/images/*.png
!docs/spikes/artifacts/03-visual/comparison.png
!docs/spikes/artifacts/multiwindow/*.png
!docs/spikes/artifacts/webview2/*.png
!docs/spikes/artifacts/webview2-w0/*.png
!docs/spikes/artifacts/webview-rehost/*.png
!docs/spikes/artifacts/tab-transfer/*.png
!docs/spikes/artifacts/pane-to-new-window/*.png
!docs/spikes/artifacts/page-keys/*.png
!docs/spikes/artifacts/cross-window-drag/*.png
!docs/plans/web-preview/w0p-evidence/attachments/*.png
!docs/plans/web-preview/w2-slice1-evidence/*.png
!docs/plans/web-preview/w2-slice5-evidence/*.png
!docs/plans/web-preview/w2-slice4-integration-evidence/*.png
!docs/plans/web-preview/favicon-evidence/*.png
!docs/plans/web-preview/preview-leaf-evidence/*.png
dist/
.tmp-repaint-capture/
local-images/
# Cover images and other pictures composed for a release, written here by
# hand or by a tool. They are drafts of something that ships elsewhere, not
# part of the tree.
output/
!docs/spikes/artifacts/win-landing/*.png
# The README's own pictures, taken by hand. docs/screenshots/README.md is the list.
!docs/screenshots/*.png
# The README's composed boards, framed from those same pictures by the layout
# sources beside them in assets/readme/src/.
!assets/readme/*.png

# Agent state. `.claude/worktrees/` holds real git worktrees: ignoring the
# directory does not detach them, and they were never tracked to begin with.
.claude/
.agents/

# Editor and toolchain droppings.
.vs/
.vscode/
*.pdb
*.ilk
# The design scripts are Python; importing one leaves this beside it.
__pycache__/
*.pyc

# Credentials. None of these has ever been in this tree, and the rule is here so
# that stays true by accident as well as on purpose.
.env
.env.*
*.pfx
*.p12
*.pem
*.key

# The Artifact Signing client, when it is fetched into the checkout rather than
# into `%LOCALAPPDATA%`, which is where `scripts/release/sign.ps1` puts it by
# default. It is 14 MB of Microsoft's bytes carrying Microsoft's own version
# number, and the script fetches the version it names; a copy committed here
# would be a second answer to the question of which one signed a release.
/tools/
