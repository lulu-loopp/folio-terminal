# Handoff:2026-08-21 — 下个 session 从这里接手

_最后更新:2026-08-21 凌晨,HEAD `eecf29e` 之后,main 干净;**两条线还在跑**(单 B 裸文件路径、dir_watch 竞态),见 §0_

> **v0.1.0-preview 已发布(2026-08-31 深夜)**:tag `v0.1.0-preview` = `4c98bf4`(CI 五 job 全绿的提交),Release https://github.com/lulu-loopp/folio-terminal/releases/tag/v0.1.0-preview ,资产=zip(sha256 `7fabeba6ef0172de3b5ea9a8570e85d7d0b723cffb0ffef5175073bee1b7b354`)+SHA256SUMS+SBOM+option-ext 源码;exe `--version` = `0.1.0 (4c98bf4d5e)`。冻结(`ebe29de`)后到 tag 之间只有四桩 CI 收敛修:①bt-math CJK 候选字族表(跑器 gulim.ttc cmap 谎报字形,产品修);②撕卡视频测试冷解码等待 5s→60s(测试修);③OSC 7 测试期望路径 8.3 短名规范化(测试修);④vendor 授权门自取原版归档钉校验和(脚本修)。发布件在 `4c98bf4` 重编重打包,六项本地烟测过;SmartScreen 真机走查过(folio-win11 clean 快照、Edge 真下载带 MotW、guest 内 sha256 吻合、首启蓝框 More info→Run anyway、二次启动零拦截、README Download/SmartScreen/First run 三节逐条相符),证据 `D:\VMs\tmp\smartscreen-v010\`。README 动图已挂(`cbef7d7`:双主题 `<picture>`、两手势 6.5s 提速版)。**发布后第一单(用户实机自报)已合**:`a58baaf` merge a-picture-pane-hears-the-disk——图片面看的文件从不在监视集(§7.1.3k ⑧ 反例)+ 两把按路径记的解码备忘录不核验磁盘;修=pane.image 进 `files_a_tab_stands_on`、解码钥匙带 mtime+len 指纹、开面即弃旧忆;四条变异红证,三门绿,CI 五 job 绿;顺带治好浮窗里的图片;浮窗**文档**无提示条查明是 seat 行缺口(另根,留账)。验收版:~~next26~~ **`dist\folio-next27.exe`(= `4910c15`,release,`0.1.0 (4910c153ee)`)——v0.1.1 十单齐**:预览跟盘(用户已实机验过 ✓)、通知摘句(`4f98c51`:hook 走 transcript 尾句、codex 读 notify 的 last-assistant-message、屏幕刮取有据否决)、卡列动词裁剪(`807d70d`,同单证伪弹簧滚速与 zoom 两桩)、浮窗提示条+跨窗替身(`b97f1c9`,「另一进程」假前提推翻)、更新提示(`3b0ce67`:/releases 列表取最高 semver,latest 对纯预发布仓 404;PRIVACY 同步)、可见性探针(`4ebb32c`:Reach 加 Marks 档,露着只留窗内记号;开关拆分 0826 已有之翻案)、设备丢失重建(`cf2e42d`:真 Device::destroy 集成测,帧上屏关预算,DComp 同死退化为旧行为留账)、恢复窄列/摆窗(`4910c15`:**最小化图标矩形被解算才是窄列日常来源**,-32000 拒于姿态;拖分隔条手不离不放行;恢复尺寸按落点屏收口;二窗占位覆盖档案修)。CI 全绿。用户 next27 五项实机验收通过(0902)。**v0.1.1-preview 已发布(2026-09-02)**:tag `v0.1.1-preview` = `f1b923a`,Release https://github.com/lulu-loopp/folio-terminal/releases/tag/v0.1.1-preview ,zip sha256 `625913090a67799f1229fd27fd3130928b83c96ddce8cca56ec14633a2354628`,exe `0.1.1 (f1b923a8b4)`,资产四件,六项烟测过,CI 五 job 绿。**0.2 定案**:多窗恢复经审计早在 0.1.0 落地(§2.7/§2.9,非候选);0.2 = 聚焦搜索 + 快捷终端;0.3 游戏接入 + winget;0.4 md 编辑。孵化仓 folio-games:打字流星 Rust 核已落(37 测,headless 达标 43–50s,`Ink` 语义色枚举)。**0.2 首批已合(0902 晚)**:快捷终端 `3d2e701`(§7.54:bt-platform/hotkey.rs RegisterHotKey+winit msg_hook 只 wake、`quake.rs`、置顶横贯鼠标所在屏顶、失焦收起、归还前台走 AttachThreadInput 配方、schema v12 `quake` 字段恢复为隐藏态、跨 DPI 呼出矩形被 §7.50 挟持期改写→`stand_window_at` 答完再说一遍;两决定:隐藏 quake 不撑进程活、General 页九行开始滚动;热键出厂不绑)+ 聚焦搜索 `64b4678`(§7.55:`palette.rs`/`palette_index.rs`,`Ctrl+Shift+P` 回表,五段不混排 动作/标签窗格/命令/文件/设置,有界索引 深度≤6 ≤30k 跳重目录 建于 files 侧,fuzzyScore 纯函数,跨窗 tab 不进;两处顺手修:文件树 id 前导斜杠、tab 按序号寻址错位)。合并冲突四文件加法解:BINDINGS 41、Text::ALL 549。验收版 ~~next28~~ ~~next29~~ **`distolio-next30.exe`(= `780fb99`)——next29 回修批全合:面板滚轮归面板 + 运行中命令闪 pane 环(`b900bf7` §7.55 ⑧⑨)、路径识别全角冒号接缝(`4bf81a9` §7.30 ⑩)、快捷终端 `Win+\`` 默认键 + 键表认 Win + 录键即生效 + 手摆矩形按显示器记(session v13)+ 顶距 12px + 托盘图标与常驻(settings v29,bt-platform/tray.rs)(`780fb99` §7.54);此前 **`distolio-next29.exe`(= `c11b72e`,`0.1.1 (c11b72e1f0)`;+ 快捷终端宽度 §7.54a:默认 60% 居中、General 页宽度行 30–100、settings v28、升级写 60 不保留整宽——用户 4K 实机嫌横贯太宽)**。待用户验:①`Ctrl+Shift+P` 面板五段/键盘走/回车直达/Esc 与失焦收;②设置→快捷键页给 Summon quake 录个键→任意程序里按→顶部横贯下拉→再按或失焦收起、前台回原窗;③跨屏鼠标在副屏按键呼出落副屏顶;④IME 开着打字走预编辑。~~待用户 next27 实机验五项~~ 原清单:①插拔电源→活着+日志 rebuilt #N;②最小化≥5s 恢复→TUI 列宽不变;③拖分隔条挤窄停一秒拉回→BT_RESIZE_TRACE 只一条且为松手宽;④外接屏拔掉重开→尺寸收进本屏;⑤外接屏在→原位原样。游戏原型四个在 D:\Developer\folio-games\prototypes\(流星 CSS 冻结修、2048 滑动 120/120、跑酷 ×4.42+飞楔 1.18 凡人中位 105s、排排排暗牌不随段+全家归位判定)。留账:min-columns 下限对 child 说谎与否待裁;显示器晚枚举的 placement 台账;IME 下流星不可玩(宿主切输入状态,正式版解)。_

产品是 **Folio**(仓库目录仍叫 BetterTerminal):原生 Rust + wgpu 的 Windows 终端。
2026-07-15 开工,至今 573 次提交,`crates/` 约 34 万行 Rust(不含 vendor)。

---

## 0. 三十秒版

- **2026-08-21 凌晨合了十条**(见 §3.1b):用户裁过的小单 a/b/d/e、`test-env-immunity` 收尾、用户当晚报的两条缺陷(第三条 URL 不识别、alt 屏点链接没反应)、以及用户两次被迫关窗的鼠标残留。
- **验收版**:`dist\folio-next25.exe`(= HEAD `ebe29de`,08-31 下午,release;**next24 + 冻结前两桩**:①**跨屏拖动棘轮**(用户拦下冻结的那条):winit 处理 `WM_DPICHANGED` 不用系统建议尺寸、自算时补一圈我们不穿的非客户区边框(+22/56@144、+26/71@192),每跨一次缝长大一圈直到永远横跨——现同一条消息内所有 `SetWindowPos` 一律解析到建议矩形,拖动中 DPI 变化记账、松手一次付清;实机 200%/150% 缝上十次往返超出 0,0、停缝六秒零抖动(`ebe29de` §7.50);②**门内打架的真因是设备被移除**:16384² 图集提交即移除 D3D12 设备(非重排撕回读)——设备丢失回调先于一切、三扇门不上尸体、`read_back` 具名返回、门测试钉 8192 天花板量真正的次序,16384² 记为具名隐患(`77c159c` §7.1.3m ⑤)。游戏侧:分拣益智可玩原型两轮修(松弛列、碎度目标打乱)发 Artifact 用户在玩;城池占领(Galcon 族)入中档候补(`d8f6bd5`);孵化仓 folio-games 已建。**冻结已宣(用户,2026-08-31,基于 next25 = `ebe29de`):自此只修不加,直到 v0.1.0-preview 发布。** 发布按 §4.0 七步走,发布件在历史重写后重建。next24 旧行:`dist\folio-next24.exe`(= HEAD `fd80db4`,08-31,release;**next23 + 竖栏/卡顿线**:①竖栏展开时浮在引擎页上,× 与行尾的点击被页面矩形先应答(`press_web_page` 站在 chrome 路由之上,页矩形已减去搜索胶囊/下载单,tab 列表是缺的第三个)——现 tab 列表先于任何页面被减去,页面自身点击/缩放不受影响;②**主线程持有仪表**:窗口线程一次持有超 500ms 即写 diagnostics 一行,按「站点」列时间去向,自愈的卡顿也留痕(旧看门狗 5s 阈值 + COM 等待会泵消息答 WM_NULL 被判 Excused,故此类卡从未被记过);首跑即抓到 `advance_web_page 659ms`(PDF 控制器启动),web 回合两半已分站,下次卡顿直接指名;③用户那次几秒卡**未复现未定因**,如实记(§7.49);游戏盘子与分拣益智规格落档 `docs/plans/games/`(`96950a1`/`0d60569`:3 轻打字流星-门/跑酷/2048 + 2 中分拣益智/待定 + 1 重幸存者-like,临时格=关卡属性+门用游戏解锁,全部 tag 后开工)。**零在飞线。下一步**:用户验 next24(重点:竖栏展开时点预览 tab 的 ×;再卡顿看 diagnostics 的 hold 行)→ **冻结** → §4.0 发布七步。next23 旧行:`dist\folio-next23.exe`(= HEAD `5b3f8b1`,08-30 深夜,release;**next22 验收回修批(六线)+ 两桩崩溃**:①旋转进度环回绕跳帧——真因是弧靠 dashoffset 在固定圆上走虚线而闭合路径的虚线不回绕,改成整环旋转,五面一笔(`d8f8530` §7.1.5b);②**两桩崩溃同根=窗口被拖到 314×50**:一列宽终端遇宽字符即死(列数下限改 2)、右缘 1px scissor 撞 wgpu 校验(全仓 scissor 走同一「求交、无交跳画」门)(`15cecce` §7.48;挂账:gate5 烟测从不把窗拖到最小值之下,所以这类崩溃到用户机才现形);③图片 pane 跨 tab 空白——纹理道是 tab 级字段、搬家两扇门都不写它,现出生与搬家读同一推举函数;视频解码器随 pane 先抱后放不再被当孤儿关掉;GIF 按文件键本就无恙;**聚焦卡对图片/视频画真实缩略**(按图长宽比信箱,只用已持帧)(`dee92c9` §7.1.6k⁗/§7.1.6b⁵/§7.44 ⑮);④**模态下引擎页画最后一帧留存画面**(复用卡片照相通道 `CapturePreview`,一张照两个产品,页离开玻璃即作废在飞抓帧;视频不受影响画在自家玻璃)(`5b3f8b1` §7.8 ⑩/§7.11);⑤btop4win 实测:盲文字形/宽度全对,块状是它自家 tty 默认。**小游戏路线记档**(见 memory:时长三层、agent 叫人自动暂停、借玩法不借皮)。**零在飞线。下一步**:用户验 next23(重点:图片 pane 拖回还在、开设置时页面显示冻结帧、旋转环过顶不跳、极限压窗跑 CJK TUI 不崩)→ **冻结** → §4.0 发布七步。next22 旧行:`dist\folio-next22.exe`(= HEAD `01ede01`,08-30 下午,release;**next21 验收回修批(十线)**:①设置下拉外点穿透到控件(× 一次关)+ 下拉滚动条贴列表内缘(`2d0050c`);②agent 族标:七条同形各配置色、第三方品牌色退出符号表、Codex 灰因=Slate 撞灰显语汇(`b48c6ff`),几何经小样定妆换成**对话气泡形**(C 案,`c9a613c` §7.41 ⑧);③悬停卡元信息一行「类型事实 · 大小」、图片卡补尺寸、类型标签 pdf 不再写渲染道名 web(`328031a` §7.29 ⑬);④卡列替身可见:滚动几何数上替身槽、替身画在手 pane 的画面、松手归还预留(`d0f0993` §7.1.6b⁗);⑤**文件/文件夹拖到 tab 条=新 tab(激活)、悬停 tab 弹簧切入、卡片列同规、跨窗按路径**——07-17 裁决落地,推翻 §7.1.6b′ ③ 等六句(`02bbd68` §7.1.6k‴);⑥**独占 tab 的预览**:无 shell tab 的裸重绘也欠一帧、引擎页走留存呈现并随 pane 跨 tab(不再被当孤儿关掉)、退役只讨一帧(卡死因)、仪表两扇门都记(`01ede01` §7.10 ④‴);⑦exe FileDescription=「Folio」(`90361d5`);⑧CHANGELOG 对齐现状 + release note 中英草稿 `docs/plans/release/release-note-v0.1.0-preview.md`(`fedbbd9`);⑨README:Agent 截图说明数上第四行、悬停卡 PDF 翻页/视频播放(`11ef449`/`4b594ac`);⑩两台干净 VM `clean` 快照改回出厂 Restricted、应答文件删预设行、gate5 对 next21 双机 exit 0(`0acbf91`..`8b51b1a` §3.4d)。**挂账新增**:全宽下双击裸路径因 pane 裂出重排点错行;全宽下单击 .html 行无反应(未查因);弹簧连发时边缘自动滚动变慢;§7.1.6k 卡上/缝间动词随滚动摇摆;跨窗替身不画画面;v0.1.1 逐家核品牌指南换官方图标(有许可才用,原样+声明)。**零在飞线。下一步**:用户验 next22 → 冻结 → §4.0 发布七步(gate5 已对 next21 预跑过一轮)。next21 旧行:`dist\folio-next21.exe`(= HEAD `57485c2`,08-30 凌晨,release;**next20 验收回修 + README 定稿,用户称「这三条跑完基本定稿」**:①预览 pane 独占 tab 卡顿——真因是仓库根的浅目录监视听到 `target\` 的整个构建(655 ms 里 188 ms 空转),现监视回调按文件名过滤、无关变动零唤醒(`57485c2` §7.10 ④″);②预览 pane 跨 tab 移动丢文档——搬了座位没搬缓冲 + 待删 tab 仍答「我还有预览座」,四种手势各一格(§7.1.6k);③空态句「点击带虚线下划线的路径,即可在此预览」并在 pane 内换行(§7.43 ⑥);④拖拽到 tab 条/竖栏/卡片列边缘 24px 带内自动滚动,三面同一常数同一钟(`e8b2417` §7.1.6b‴);⑤选择器收起没找到的 agent(shell 仍灰显)、配置文件页缺席行两行 + WSL 出路组末说一次、修 tooltip 按行序号取表的真 bug(`e190deb` §7.41 ⑦,`Text::ALL` 530→**531**);⑥磁盘线测试靠 NTFS 时钟刻度的抖动钉死(`move_the_disk_forward`);⑦README 定稿:中文现在时主动语态(DeepSeek 第三稿)、「Windows 上的细节」整节删只留「与 Windows 的集成」两条、已知问题删 .mov/.mkv/.avi 旧话只剩 .webm 扩展、后续方向四短语(手机=远程使用终端)、悬停卡 PDF 可翻页视频可播放。**零在飞线。下一步**:用户验 next21 → 冻结 → §4.0 发布七步;AI 小助手(台面/便签)方案已讨论,见 memory `desk-assistant-plan`。next20 旧行:`dist\folio-next20.exe`(= HEAD `452bfc6`,08-29 深夜,release;**next19 + 图集真因**:长会话多档字号下 glyphon 的货架打包器(etagere)耗尽的是**架子几何不是面积**——每层被本帧在用的一个字钉住、永不回收,glyphon 从不重排,首次拒绝即终身;修=丢字帧结束后换一张新图集重排,每次发作至多一次,重排写 `diagnostics.log` 一行 `Folio glyph atlas was repacked after a textless frame (refit #N)`(**N 的疏密就是诊断**:几百帧一次=正常磨损;每秒一次=单帧真装不下,要 `BT_GLYPH_CENSUS`);1200 帧十档字号压力红证(最忙帧 19.8%),4000 帧拒绝 2015→8 且无连续(`452bfc6` §7.1.3m ③)。**零在飞线。下一步**:用户验 next20(next18/19 结论沿用)→ 冻结 → §4.0 发布七步。next19 旧行:`dist\folio-next19.exe`(= HEAD `3f3be76`,08-29 深夜,release;**next18 + 五线**:①预览内 md 链接读同一张点击表——平点窗内开页、Ctrl+点浏览器、被拒出声走 toast(`b3da717` §7.1.5g ⑦);②**预览池跟随磁盘**——真因:监视只订阅 pane 正在显示的文件,池里其余从不订阅;现按池订阅,干净重读(滚动保持)/脏提示条 Reload·Keep/删除提示,内核监视 + 焦点回来时比对两条路(`571760c`;`Text::ALL` 526→**530**;挂账:浮窗预览有磁盘变动不显示提示条);③**中文预览正文不画字**——真因是字形图集满→整条泳道不画(用户日志 `glyph atlas reached the device limit on the overlay lane`);修:页面正文先于 chrome 拿图集、无字重画封顶 2 次(原来原地空转拖响看门狗)、按泳道各记一行(`d085722` §7.1.3l);④图集用量:量出 4K 全中文一帧只占 8192² 的 3.3%——**l 的「一帧太多字」假设不成立**;做了被裁掉的标签不铸位图(720→30)、同字号同字跨泳道一份被钉、纹理上限问 GPU(8192→16384,占用 0.8%)、`BT_GLYPH_CENSUS` 仪表(`3f3be76` §7.1.3m);**用户那次拒绝的真因未复现,长会话累积假设的排查线在飞**;⑤README 两份重写:英文按能力短语分组、一句结论+事实+截图、不比较;中文由 DeepSeek 按书面语体逐句写,Claude 只核事实(`d3a15b4`/`59106e5`);hero 积分上下限让开;`check-doc-words` 进 CI。**下一步**:用户验 next19(next18 的验收结论直接沿用)→ 长会话图集线落地 → 冻结 → §4.0 发布七步。next18 旧行:`dist\folio-next18.exe`(= HEAD `f827508`,08-29 晚,release,`--version` 报 `0.1.0 (f827508588)`;**next17 验收回修 + 同学新机反馈版**,九线全合:①设置下拉点空白关+Git 页空态换行(`d1eecd5`);②OSC 10/11 取证——通路无错,深色输入条是 Codex 启动时问一次不订阅 2031(`eb3459d` §7.46);③文案重写:Profiles=「配置文件」、Agent 页产品语气、整合行写明所给、灰显 agent 行指出 WSL 出路、内部词源门、四条 agent 配置 Kimi Code/pi/Hermes/OpenCode(`ccdbac5`);④卡片列空白撕新卡——推翻 08-23 那半句,与 tab 条同规、替身同画(`a235456`);⑤主题单一来源 `settings.theme_mode`,建窗前解析 OS,`session.theme` 退休一次补迁(`8087946` §7.46②);⑥中文换行断点表 `linebreak.rs`,九个面共用(`9a7222f`);⑦**打印出的 http(s) 地址平点从 W2 起一直是空臂**——现平点=窗内开页、拒绝出声(`5701d37` §7.1.5g ⑥);⑧PSReadLine 开关在出厂 Restricted 策略下静默——现行下说策略、点灰项弹卡给 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`、七种拒绝各有其句与日志(`f827508` §7.47);⑨README 三处更新+hero 积分上下限让开(`099a18f`/`c545848`,不在 exe 内)。**在飞**:预览内 md 链接读同一张点击表(§7.1.5g ⑦);中文 README 在预览 pane 里正文不画字(排查中)。**挂账新增**:gate-5 VM 应答文件预设 RemoteSigned,复跑前把 `clean` 快照改回出厂 Restricted;`BT_PREVIEW_TRACE`/`BT_PTY_DUMP` 值是文件路径;内置配置吃掉六色,第三条自建配置开始撞色;当日 D 盘一度满(target 294 GB),合完线即清其 worktree 缓存。**下一步**:用户验 next18 → 冻结 → §4.0 发布七步。next17 旧行:`dist\folio-next17.exe`(= HEAD `b66776f`,08-29,release;**next16 总验收回修版**——用户验收 next16 报四条,全部合入:①hover 卡首次悬停空白(`b66776f` §7.45 ②:真因不是缓存冷热,是**会话恢复后池里的文件**——答案落池时只问 `preview_panes` 谁在读,悬停卡的 view 不在那张表,`changed=false` 无重绘;修=`surfaces_reading` 把卡按文件身份算进读者、`settle_landed_head` 一个出口)、②视频换文件后残留紫块+控件条(`e59b577` §7.44 ⑬⑭:紫是 `.wmv` 夹具自己的颜色;真因=清扫只问图片道,md 答 None 被读成「还没说话」保座;修=`SurfaceSubject` 问页/图/buffer 三道;顺带新纹理不当作已持帧)、③`dist\x.exe(0.1.0 …)` 不识别(`98fb891` §7.30:ASCII 开括号本身是接缝点不看后继;裁决:切出的读法不许以 `/`、`\` 结尾——目录前缀是没人写过的名字;场景 7/8 改判画 `src/app.ts`,场景 44 保住)、④任务栏自动隐藏时闪窗把整条任务栏顶出来(`0a8d680` §7.1.5o ⑩:`ABS_AUTOHIDE` 时第二档不可用直接 toast;`WindowPlace` 三事实一值;被拒的 toast 是静默不是降级成闪)。**未变**:`Text::ALL` 526、BINDINGS 39。**挂账新增**:`window_focused` 出生种子 `true`,从未拿过焦点的新窗在首次焦点事件前被当「正看着」(reach=nothing);`BT_MOUSE_TRACE=1` 会在 cwd 写名为 `1` 的文件(值被当文件名,给完整路径);拖出浮窗后换文件只有红门覆盖无实机照。**下一步**:用户再验 next17(重点四条回修),过则 preview 冻结进发布流程。next16 旧行:`dist\folio-next16.exe`(= HEAD `84d843f`,08-28 下午,release;**v0.1 preview 功能面闭合的总验版**;= next15 + 十一线:路径/域名(`1772fd6` §7.38:`dist\x.exe` 不划真因=反引号;裸域名短 TLD 表;接缝改「类」)、md 斜体(`3afbb4a`:合成倾斜,字族有斜体即真斜体)、浮窗七件(`f7b2526` §7.39:html/pdf 卡拖头转带引擎浮窗、圆角、可缩放、悬停不穿透、滚动条入界、浮窗内可选、无预览卡;总则「一份内容在浮窗与 pane 同一张画同一套手势」)、UI 五件(`bb3c33d` §7.37:头标题同字面、tab× tooltip、菜单对比度经派生≥4.5、文件夹箭头(真因:浮窗树不被动画钟盯)、缺文件卡文案)、启动慢(`5b69894` §7.40:静默门/注册表/懒探/首帧不等,窗 6s→40ms;挂账:一次运行首个 WSL pane 无整合)、小批四件(`f442d56` §7.41:解绑 Ctrl+Shift+P 40→39、参数示例、三条 agent 档案+`.cmd` 走解释器、跨行引用整条高亮)、**视频①②**(`bf0e468` §7.42 + `84d843f` §7.44:IMFMediaEngine+wgpu 视频层、CPU 回读、一座三表面 `VideoSeat`、原生控件条、拖头带引擎、A 壳页退役、GIF 按帧、七种容器全播(六种 Windows 自带解码,`.webm` 靠扩展);冻结根因=窗口线程等一张没人要的解码图)、小尾巴四件(`a2989f9` §7.43:缺席卡换行、`javascript:` 拒绝卡(真因:引擎不发 NavigationStarting,现每次导航先过自家门)、通知条按钮完整文案截断、panic 走 `leave_process`;**任何线程 panic 都终结进程**)、GPU 测试串行锁(`1d060a9`:六进程压力 16/30→0/30)、md 图片(`bcc9c53` §7.1.3k:`![]()`/`<img>`/`<picture>` 按主题、远程不取只画 alt+链接;顺带修 SVG 字体库空、切主题不重排)、atlas 线(`42dd578` §7.45:hover 卡字空白→拒收统一出口+lane trace;**未等比复现**,再撞开 `BT_PREVIEW_TRACE` 看 `refused=`/`layer_drawn=`)。图标候选五版(`80958d5`,用户裁**维持现状**)。**HANDOFF §5 第 17 条与 §3「heal_preview_scroll」两条挂账销账**(atlas 线核实)。`Text::ALL` 526、BINDINGS 39、schema 不变。**验收后 preview 冻结只修不加**;发布流程:两台干净机复跑→历史重写(邮箱 gmail/trailer)→建仓 CI→tag v0.1.0-preview→release note→Win11 真下载过 SmartScreen。next15 旧行:`dist\folio-next15.exe`(= HEAD `36439653`,08-28,release,73 MB,`--version` 报 `0.1.0 (36439653f8)`;= next14 + 门 5 干净机三红修复:①开过预览座的窗关不掉(`3643965` §7.35:关窗排了等浏览器的钟却同调用里把窗扔了钟没走+过 WebView2 的进程不能从 main 正常返回=Chromium DLL 在停泵的单元卸载卡死→关窗保留一拍让循环转完、main 末尾释放完一切后 TerminateProcess 自己;连带释放 §7.34 本机 HWND 幽灵)、②无 WebView2 时预览座什么都不画(`0a85f8a` §7.36:`open` 尾一个 `?` 让座根本没建、卡是座画的→座带 fault 插入、回调不来给 10s deadline)、③干净机 chrome 文字超框(§7.36:无显卡驱动落 WARP,一窗两文字视口 uniform 串扰→一窗一视口)。**门 5 通过**(两台各 exit 0,web 关闭 exit 0,缺席卡/真网页/chrome 文字全对;判定见 `docs/plans/release/clean-vm-results-2026-08-28.md` 文末)。**next12 实测其余仍在原处**(文本嗅探§7.32/视频控件§7.23⑪/头部四件§7.33/关窗幽灵§7.34 均在 next14 已含)。**挂账**:panic 出 main(101)仍留窗;缺席卡细节行超座宽被裁;`javascript:` 无拒绝卡;SmartScreen 需真下载 MotW 人工过一次;bt-term shell 整合测试跑门**不许设 `PSModulePath=''`**(否则 spawned PS 找不到 PSReadLine→folio.ps1 不装不发 OSC7→假红,见 [[betterterminal-project-decisions]])。**待用户**:tab 条 × 提示字(推荐加)、「两处文件名字体不同」指哪、图 132 路径是悬停无卡还是点无反应;老账:商标检索/权利链/裸域名/菜单对比度/图标。next14 旧行:`dist\folio-next14.exe`(= HEAD `7c6d487`,08-27 晚,release;= next13 + 四线:**文本按内容嗅探 + HTML 源码可编辑**(`859525d` §7.32:白名单外的名字由预览 worker 一次读头判 UTF-8/UTF-16+无 NUL(8000 字节,git 的数);`</>` 翻到自家编辑器,Ctrl+S 后文件监视自动重载;顺带修 PS 5.1 写的 UTF-16 被当二进制)、**视频填满 pane + Folio 控件条**(`0a24122` §7.23 ⑪:`object-fit:contain`;去 `controls`,壳页自带播放/进度/时间/静音/音量/四档倍速,主题令牌注进 `:root`,图标从符号表出 `#i-pause`/`#i-speaker-mute`,250ms 出 2s 隐,motion 登记;证据图含账户名未入库)、**头部四件**(`ea9dc75` §7.33:头/轨每枚可点必登记 tooltip 且门走 runs 不走清单,`■`=Stop playing、`↗`=Open in a floating window、pane `✕`=Close pane;DevTools 改取景框+光标;`⌄ 🗀` 头与角落同序 `PANE_DOOR_RUN`;头举着菜单或浮窗都显形 `head_that_raised_a_layer`;`Text::ALL` 523)、**关窗幽灵 + 浮窗吞键盘**(`7c6d487` §7.34:开过页的窗关后进程 0.2s 退而 HWND 不死→5s 被 DWM Ghost→幽灵窗抢前台且无 IME 上下文=「换版后打不出中文+警告音」;修=所有终结路径先 `set_visible(false)`;浮窗 `focused` 三门写无门清→出生不给键盘、`raise` 唯一入口、点 pane `blur`)。**挂账**:开过页的窗 HWND 为何不死(杀光 WebView2 子进程也不释放;folio 退出后卡 rundown 数分钟)、panic 退出仍留窗;tab 条 `×` 无 tooltip(推荐一并收);撕出预览浮窗自己的头不在 tooltip 表;html/pdf 卡不能拖成浮窗(撕出窗要有引擎);行内公式源码格留白(`$\sigma$` 8 格只画 1 格);`ChromeTarget::PreviewSave` 丢 seat(同 PreviewFlip 旧病)。**门 5**:两台机首次装机白跑(EFI 引导 Windows 安装盘要按键,`OsNotFound`),ISO 重制免提示引导线在飞(`new-install-iso.ps1`,IMAPI2+efisys_noprompt)。next13 旧行:`dist\folio-next13.exe`(= HEAD `7f6a28b`,08-27 下午,release;= next12 + **崩溃修复**(`7f6a28b` `paths.rs` `names_a_dos_device` 四字节名切字符串→按字节比较;`*⑥` 一印就 panic、会话恢复重印每开必崩,用户实证 14:53–14:55 四次;回归测试 `a_four_byte_stem_that_is_not_ascii_is_not_a_device_and_not_a_panic` 红证=原 panic 文本)+ 截图 14 张 2x(`7c6c2e0`/`296eb21`,4K 原生 200%,3200×2000,oxipng;`terminal-math` 对为 README 首屏)+ 建机脚本可续跑(`3f09fa8`)。**next12 用户实测新报(在飞三线)**:`.ps1` 无预览+HTML 源码不可编辑(文本按内容嗅探;`</>` 翻到自家编辑器,§7.32)、视频不放大+Edge 原生控件(object-fit contain;Folio 风格控件条,§7.23 续)、头部四件(两枚头按钮无 tooltip→注册表红门;DevTools 图标重画;⌄ 统一在文件夹左;文件夹浮窗开着头显形,§7.33)。**待派(浮窗/焦点/IME 一族)**:html/pdf 卡不能拖成浮窗(撕出窗要有引擎)、视频浮窗后打不了字(焦点被页吞)、关旧 folio 开新 folio 后 IME 坏+警告音。**待用户答**:「两处文件名字体不同」指哪两处;图 132 路径已划线——是悬停无卡还是点了没反应。next12 旧行:`dist\folio-next12.exe`(= HEAD `b7894a6`,`--version` 报 `0.1.0 (b7894a65ce)`,代码同 `2af70f4`;08-28 凌晨,release,73 MB;= next11 + 视频块①②(`406d398` MF 首帧进程常驻会话冷 210ms 热 10ms/`a918409` A 壳页 `%LOCALAPPDATA%\Folio\player\play-<hash>.html`+tab 小喇叭+mov 只首帧;§7.23)+ next11 回修九件(✕ 光学缩/文件夹「动作列描边、对象位实心」/面包屑 › 回字符/菜单头整排显形/HTML `</>` 源码翻转/PDF 卡页列滚动/设置文案 80 行重写+Agents 页+Codex/Copilot 安装器/pane 头名用满行悬停渐变覆盖/亮色浮签 `b7fda29`)+ files 列 DirWatch(`bf70fe0` §7.24)+ Cards 一个求解器(`0145df1`)+ Cards 卡头钉/✕ 同 tab 规矩(`b6beaf2` §7.26)+ 首启 shell=第一个在场的+整合提示一次运行一问(`d745848` §7.27)+ 撕出卡顿 3146→167ms(`b499c21`)+ **hover 卡族统一**(`400bd1c` §7.29:终端引用出同款卡、文件夹出浮窗、拖卡头 6px 转固定预览浮窗;html/pdf 卡不转正——撕出窗无引擎)+ **md 强调符整行定界栈**(`9878f31` §7.1.3i″:四趟切片→一次裁决,Unicode P∪S 标点;`*a*` 也出粗体,屋里没有斜体面)+ **路径接缝**(`29da478` §7.30:`a.md,这里` 半角分隔标点→非 ASCII 一次字符类转换即接缝,从长到短逐个问磁盘)+ **md 渲染视图选择与复制**(`2af70f4` §7.31:拖选/双击取词/右键 Copy·Select all/表格制表符/链接上拖只选单击才开;Shift+方向键留块级编辑片)+ 发布工程门 0–4/7 全落(版本 0.1.0 一源/静态 CRT/`package.ps1` zip 七件=exe+conpty 两件+两 LICENSE+NOTICES+**TRADEMARK**(README 不进 zip)/CI 五 job/corpus 脱敏/README 终版 `5f0dada` 按定位三条:公式钩子·不切换正题·现代只秀不说,hero=纯 SVG 高斯积分/许可整改 `3a6ff1c`:版权行带真名·CONTRIBUTING 许可节·微软 Terminal NOTICE 固定·`publish=false`)+ 建机脚本 `new-vm.ps1`(`c11c0b1`)。**在飞**:截图 14 张按 2x(4K 屏原生 200%,3200×2000)重拍。**待用户**:商标检索公开前做否(要律师)、权利链自查(umich 邮箱/学校 IP 政策)、裸域名无 `https://` 认不认、菜单文字对比度提 4.5、应用图标签字、ISO 两张路径→门 5、Cards 提示箭头截图。旧行:`dist\folio-next11.exe`(= HEAD `f95b34c`,08-26 21:06,release,**总验版**;= next10 + 图标 P1/P2 + 动画两片:P1(`34fd9da` 两张名单归零/15 新形/字符退役/框裸语义随拆分消解)、动画一片(`d97f782` `arrival.rs` 离场=淡出的图,15 带子一套机制,退场期交互死,Reduced 单帧)、P2(`4984e6b` fill/stroke 政策 `FILLED_WITH_A_REASON`/品牌 12 单位 chassis/展开两语言/符号表 `--ignored the_symbol_sheet` 写 target/icon-sheet.png;挂账:对象标直写画名处未全走表)、动画二片(`f95b34c` `settling.rs` 墨 90/室 140/布局 200 一句定全部;tab 交接同帧;git pending/foot 回执/Advanced 200 clip/drag ghost 90;cmdrail 950 Reduced=平持;**覆盖 §7.18 的 pin 单 140 → 140 宽/90 墨**);**待用户裁**:手势露出块取舍(丙 1/2/3+菜单 accel 列+tab 右键菜单+三只虫+复制开关+下载卡 ×);设置三句说明缩短 vs 省略号。**图标统一块 + 动画补齐块全部闭合**。next10 那段:= next9 + 七线:hover 卡公式(`e38b584` overlay 层补 rasters 通道,撕出预览窗同修)、copilot 第四家(`2a3d74e` 真源码取证 13 hook/5 行/第三安装器/版本门 1.0.26;焦点门一格因 org 策略未跑)、清尾束(`8ba4962` 隐形按钮画命同源/四只墙钟测试改确定性代价钉满载 40/40/toast 点击回 pane 实证+`open` trace 行)、快捷键面板补齐(`1dbf9b4` 冲突可换行/keybindings.json 自写自拒真虫/页脚「按了没到」/webhost 派生;**Scope 分组未做**:小样与 slice 2 裁决皆扁平行)、图标 P0(`4786368` `icons.rs` ActionIcon 注册表+MarkSlot 槽位表+光学红门;pane 头 1.95×→1.2×;过渡豁免 6 形/一形多义白名单 14 组待 P1;**待裁:紧凑头 12 与带子算不拢,倾向走 13**)、motion 令牌(`7a07e3a` 三档 90/140/200 登记表红门/意图延迟族独立命名/toast+光标接 Motion/WM_SETTINGCHANGE 不重启即变/通知开关拆两门)。审计双报告+定稿+隐形手势审计在 `docs/plans/ui-style/`。`Text::ALL` 505、schema v23、BINDINGS 40。旧行:= next8 + 八线一晚:md 公式两族(`f26a155` `\[..\]`/`\(..\)`+23 裸环境)、PDF hover 首页缩略图(`5a5ae37` hayro 零新包)、B7=Ctrl+Shift+X+Attached 判乙(`ac81713` 表内 Enter 绝迹;`is_agent_seat` 按账本记忆,alt-screen 非参数)、三件小 UI(`54c0847` 菜单图标单色线版/单窗无 Move to window 行/戴轨浮窗整脚退役→轨右手+角落浮签;无轨浮窗保脚是解读)、设置页三件(`f03895f` 说明三行换行量画同源/Agents 段/Codex notify 安装行 toml_edit 装卸逐字节)、git 面板家具(`88237ec` 标题无底无 tip 键盘跨过/教学句下放行 tip/覆盖 08-19 九行皆落点)、布局 MoveSeat(`7ed3045` 拖放=一个编辑,未触兄弟像素不变,ratio 只分 flex 空间;旧 session 一次性重解不迁移)、侧栏弹层归属(`ed44997` PopupOwner 穷举,轨的菜单撑住轨)。**新虫入册**:files pane 头 ⧉/✕ 隐形仍可命中;dir_watch 浅监视与三只墙钟比值测试负载下反复咬人待加固。UI 审计双报告已落 scratchpad\ui-audit(图标集中在 marks.rs/ChromeMark、分配散在三处;stroke 逐像素表已量),定稿方案待呈。待裁:三句说明文案缩短 vs 戴省略号;通知开关拆分已裁拆待施工。`Text::ALL` 495、schema v23。旧行:= next7 + 注意力块收官与缺陷清账:A1(`7a0c9dd` OSC 前门到账本:weak 凭据/OSC 9・777 事件级借词/一 pane 两产者一 episode)、A2(`38f3bf1` 旧点点退役、BEL 出队为宣告、七值用户输入 `is_answer()`、Acknowledged 不动点实机验证)、C 片(`ae4963c` 三档触达 IsIconic+CLOAKED、真 Windows toast+程序原话、失败卡、`turn_end_notification` 开关 schema **v23**、红线 3 空心铃环)、看门狗+md 公式+跨 DPI 光栅权威+全 BT_* 空串=关+浮窗地址门与撕出整轨(§7.7 ⑩⑫ 销账)。**注意力块 A0.5/A-core/A1/A2/A3/C 全片闭合**。待裁除 next7 三条外新增:C 片偏离②(turn_end 开关连带静音 OSC 9/777 消息,拆需给消息支单独判据)。`Text::ALL` 490、`BINDINGS` 40、schema v23。旧行:= next6 + 五线:角落门(`483240f` 单 pane 角落 🗀+⌄)、九件束(`c8667f0` 预览轨 tooltip 全挂/`…`=资源管理器式折叠菜单深在前/开文件软定位不重根/New-terminal 行戴 profile 记号/VS Code 行撤/网页脚撤=悬停浮签/Document 脸 Reveal in Explorer)、注意力 A3(`7bf302e` 每 pane 门铃管道 logon-SID 独享/`folio attention` 动词/CC+codex 真映射表/hooks 安装器可逐字节撤回/计数守恒律封死丢帧竞态)、看门狗修理(`81c0ade` 用户报「输入框闪 folio 串」定性误报:泊车语义+问话定罪+控制台前门归还,常驻诊断落 %APPDATA%\Folio\diagnostics.log 4MiB 轮替,CTRL 事件已防连坐)、交互七件批(`a0157f3` Cards 改名/退出卡逐文件行+Save all/中心投=真交换含独子边界/Ctrl+Shift+Enter 缩放/Move to window▸ hover 亮环/切 tab 清弹层/面包屑尾段+files 行 Rename 真改盘)。**待裁三条**:B7 和弦 08-19 曾实测不达(表已答,硬件未验);B10 原意若非「切 tab 清弹层」请再说;Ctrl+Shift+Q 全局卡仍跨窗汇总(改每窗各弹要重写 quit.rs 四相,未擅动)。`Text::ALL` 484、`BINDINGS` 40、schema v22。旧行:= next5 + 大批:预览头 v2(地址行/面包屑/OS 路径形)、files 菜单三脸(文件夹右键/Reveal/默认 app)、快捷键提示层(长按修饰键 800ms)、单 pane 缩放(双击头)、横滚台阶一整块(Line wrapping 设置+Shift 滚轮+横条)、六裁决束(脚静默/引擎失败卡/否判过期/原图直用/空串关/Lanczos 并行)、显示双修(头名截断/光标冻结)、引擎四案(坏 fixture 换新/弹窗定位/浮窗页/↗清白)、划线钉、挂死自报器、注意力 A0.5+A-core(焦点上报已生效!)。注意力方案 v1–v5 四轮过审全通过
  用户自己在跑的是 `dist\folio-next.exe`(08-22 01:25 的 `18dcfb6`,**被占用、不许动、不许 kill**)。
- **之后又合了两条**:`DirWatch::start` 竞态(`b6a8698`)与**单 B 裸文件路径**(P3-8,见 §4)。十条合完三门 3250/0。
- **下午又合了五条**(见 §3.1c):橙圈不灭、目录平点开 files 列、`1004`+X10 抬起、PowerShell 整合安装提示、聚焦卡高度+顶对齐+瞄准。十五条合完三门 3290/0。
- **2026-08-22:Web 预览块(W2)六片全部落地**,见 §3.1f。这是 M5+ 的 P3-6/7 被用户提前的那一块,现在**网页真的能用了**。
- **深夜到 08-22 凌晨又合了六条**(见 §3.1e):小样清账、聚焦列平滚轮归列、预览头 ↗、`path:line:col`、卡片自己武装读取。**二十四条合完三门 3345/0,没有线在跑。**
- **傍晚合了三条**(见 §3.1d):`⌄` 菜单在标题栏拖拽带不收 + 聚焦态拒布局一瞥、滚轮瞄准丢余量+限流、**f spring-loaded 拖 pane 过 tab**。**十八条合完三门 3311/0,没有线在跑。**
- **等用户裁的新东西**(§5 第 6–10 条):**PowerShell 没有 shell integration 所以鼠标残留那条修复在用户机器上不生效**(最要紧)、
  `1004` 该不该在提示符归零名单里、聚焦列 280 让竖栏 Expanded 进聚焦时舞台左移 60px、命令面板那一行算不算第三扇门、`Restart shell` 与 M2 契约的出入。
- **下一步**:§4 的队列(c/f/g/h/i + 新增)。新缺陷仍然优先。
- **已替用户改过一处机外配置**:`~/.claude/settings.json` 写入 `preferredNotifChannel: "terminal_bell"`
  (备份 `settings.json.bak-20260820`),让 Claude Code 等输入/等批准时响 BEL → Folio 亮橙点。新开的 Claude Code 会话才生效。

---

## 1. 怎么干活(先读这一节,比读代码重要)

**分工。** 用户裁决 + 验收;Claude 协调、开单、合并、把关;**实现与排查一律派 Opus subagent**
(`Agent` 工具,`model: "opus"` **必须显式写**,`isolation: "worktree"`)。一张单一个分支一个 worktree。
用户原话:「实现/排查默认派 agent/Codex,Claude 只管决策、验收、讨论」。

**一张单必须包含**:权威文档的出处(读哪几节)、要做的事、**红测先行**(先写会红的测试并**验证它真的红**、
报告里贴红证)、实机证据要求、纪律与禁区、并发中的其它线在动哪些文件、交付报告要有什么。
写单用中文,篇幅不要省——今天所有一次过的单都是长单。

**三门**(合并前必须全绿,在 main 上再跑一遍):

```bash
# `PSModulePath` 的 export 于 2026-08-21 退役:bt-pty 的 build.rs 自己给 5.1 设模块路径。不带也能跑,带着无害。
cargo test --workspace --no-fail-fast
cargo clippy --workspace --all-targets -- -D warnings
cargo fmt --all -- --check
```

**合并流程**:`git merge --no-ff --no-commit <branch>` → 解冲突 → 三门 → `git commit --no-edit -m "<主题>"`
→ `rm -rf` 掉 worktree + `git worktree prune` + 删已合分支。
**提交主题是一句长的小写英文句子**(看 `git log` 前几条的调子),结尾带
`Co-Authored-By: <你的模型名> <noreply@anthropic.com>`;主题**不加** `claude:` 前缀,README 不提 AI。

**权威在哪**:
- `docs/DESIGN.md` —— **裁决记录**,不是说明书。每条裁决连同它的理由、被否掉的方案、以及为什么。
  新落地的东西必须在这里留下现在时的一段。它很大,用 `grep -n "7\.1\.6"` 这类定位,别整篇读。
- `design/ui-mockup.html` —— **设计权威**(13000+ 行可跑 HTML)。原生与它不同形时,默认是原生错;
  但小样也会欠账,清账要单独开单(今天清过一批)。带读取器的分支(`?lonehead=`、`?icons=`、`?web=`)不要动。
- `docs/plans/` —— 大方案。**大方案必须过 Codex 审**(skill `codex`,`-ReadOnly`),用户原话:
  「他通过了基本就没问题了」。
- `memory/`(`C:\Users\Weiyi\.claude\projects\D--Developer-BetterTerminal\memory\`)—— 跨 session 的裁决与教训索引。

---

## 2. 禁区与坑(全是踩过的)

**硬禁区**
- **不许碰 `dist\`** 里用户正在用的东西;**绝不许 kill 任何你没起的 folio 进程**。出新版就覆盖
  `dist\folio-next.exe` 或 `folio-next2.exe` 中**没被占用**的那个(被占的会 Access denied,
  而 `| tail` 会把这个错误吞掉、退出码还是 0 —— 用**时间戳**确认拷贝真的成功)。
- **不许改 `~/.cargo/config.toml`**:`jobs = 19`(24 核留 20%)是用户裁决,别删。
- **不许对 `.rs` 用 `sed`**(CRLF 会被毁)。要脚本就用 python(`io.open(..., newline='')`)或 Edit 工具。新文件一律 LF。

**反复咬人的**
- **「用户环境里有没有装 X」一律问那个程序自己,别拼路径(2026-08-21,PowerShell 整合线现场)**:
  用户的 Documents 重定向到 `D:\`,`$PROFILE` 在 `D:\Documents\...` 而不是 `C:\Users\<user>\Documents\...`,
  后者那两个 2 字节空文件从来没被 shell 读过——一个自己拼路径的 build 会对一台早就装好的机器报「未安装」。
  判据改成 `pwsh -NoProfile -Command '$PROFILE.CurrentUserCurrentHost'`。同一类:装没装、装在哪、用的是哪个 profile
  ——**问,别推**。

- **`PSModulePath` 那条 export 已退役(2026-08-21,`test-env-immunity` 结案)**:当初「带 export 跑全量会挂、不带单跑就绿」是**两件方向相反的事**——
  (A)挂的两条测试(`real_powershell_input_reaches_a_viewport_owned_frame`、`sidecar_resize_keeps_history_navigation_on_a_clean_prompt_line`)
  是**墙钟总额被负载吹爆**,与 env 无关(改前/改后二进制同环境交替 7/8 vs 0/8);(B)export 真正救的是 **bt-pty 的 build.rs**——
  5.1 继承 pwsh 7 的 `PSModulePath` 会 `Get-FileHash` 找不到,现在 build.rs 自己算 5.1 的模块路径。两支探针改成**静默预算**(30s 静默 / 180s 天花板),
  `full_ring_blocks_writer` 的 20ms 假设也换成生产者自己的报告。规则进了 `CONVENTIONS.md`(超时按静默不按总额;A/B 必须交替)。
  **仍会抖的**:`dir_watch`(真竞态,已开单,§4)、`a_pathological_dollar_screen_arms_nothing…` 与 `finalized_line_ingest_stays_linear…`(时间比值门,多 cargo 并发必挂,单跑绿放行)。
- **i18n**:`crates/bt-app/src/i18n.rs` 的 `Text::ALL` 现在是 **502**,数字**手工维护**。
  新串一律**追加在 enum 末尾与 ALL 末尾同一块**。合并冲突的固定解法 = keep-both + 只留一个
  `ALL: [Self; N]` 头 + 重新数 + 加重复变体断言。**keep-both 会把函数或字符串字面量截断在半路**
  (今天发生过两次),解完必须 `cargo check`。
- **schema 版本撞车**:两条线同时占同一个版本号时,**后合的那条让号**(改迁移函数名、文档行、
  `Value::from(N)`、迁移表、常量、fixture 文件名与内部 `schema_version`、测试名与断言句子)。v16 是先例。
- **ui-probe**(`scripts/dev/ui-probe.ps1`):
  - 截图**校验中心像素归属**,别的 agent 的 folio 窗口盖上来会被拒绝(这是它该有的表现)。
  - **前台会被用户自己的窗口抢**,拒绝盲发按键也是对的;不要跟用户抢前台。
  - **中文 IME 会吃键**(`[Console]::Write` 被打成 `[Console: : Write`),要么切 en-US,
    要么走 `BT_PROBE_INPUT` 喂 fixture,要么只用 Ctrl 和弦。
  - `BT_PTY_DUMP` 命名的是**一个文件**,不是文件夹(给文件夹会报 Access denied,伪装成 ConPTY 失败)。
  - 隔离 `APPDATA` 的目录名**要 agent 独有**——用通用名会互相恢复对方的会话(今天发生过)。
- **A/B 必须同秒交替**:先跑一组再跑另一组会被机器负载差异混淆(用错了一次,结论翻案)。
- **DESIGN §1.2 的 workspace 划分是规划态,不是现状**:那里写着 `bt-richrender` / `bt-session` /
  `bt-shellint` / `bt-config`,这四个 crate **在 `Cargo.toml` 里根本不存在**,职责被并进了
  `bt-detect` / `bt-app` / `bt-persist`。凭那一段去找代码会扑空;以 `Cargo.toml` 的 members 为准。
  (今天开 F3 单时,「`bt-shellint` 这个 build 里不存在」就是这件事的一个实例。)
- **`vendor/alacritty_terminal` 必须留在 workspace.members 且版本精确锁**:否则 crates.io 上的
  0.26.x 会 out-resolve 掉本地 patch,**静默**把捕获钩子从构建里拿掉(`vendor/.../CONVENTIONS.md`)。
- **诊断开关(环境变量)**:代码里没有集中登记处,**这一条就是登记处**——新加一个就加在这里。
  下面全是 `folio.exe` 自己会读的。
  - `BT_MOUSE_TRACE=<文件>` —— **鼠标点击全程取证**(`crates/bt-app/src/mouse_trace.rs`)。
    与 `BT_PTY_DUMP` 同款:**命名一个文件**,不是文件夹;追加写、每行 flush;未设置(或设成空串)
    时闭包不被调用,零格式化开销。一行一站,带单调毫秒 + `window=<id>`:
    `mouse_input`(state/button/物理指针/`metrics_scale`/`swapchain_size`/`inner_size`/当前 route)、
    `chrome_mouse_input taken=1 at=<臂名>`(每个吃掉事件的出口 + `chrome_target_at` 的 Debug)、
    `pane_press`(命中的 cell、`pressed_cell_target`、该 cell 的 `HyperlinkHit` 与图片引用、
    以及 `forwarded=1` —— 开着 1000/1006 的程序把按下抢走时就是这一行)、
    `begin_local_selection`(press armed 的 route 与它许诺的东西)、
    `finish_local_selection`(origin/release 两个 cell、`single_click`、press 与 release 两次
    `HyperlinkHit` 的完整 Debug、`same_link`)、`activate_hyperlink`(uri、走了哪个臂、
    `file:` 解析出的路径与它是否存在/是不是目录)、`preview_landing_surface`(两个 `None` 出口
    分别带标签)、`open_preview_file` / `open_preview_image` 的进入与 `leave=no-landing-surface`,
    外加 `dpi stage=…`(与 stderr 上那行 `BT_DPI` 同样四个数,写进同一个文件好对时)。
    **它一条行为都不改**;`mouse_trace_station_tests` 三条钉子盯着「新加的 return 旁边不许没有一行」。
  - `BT_ATTENTION_TRACE=<文件>` —— **注意力账本(§7.1.5b P1-8;`attention` 方案 §11.1.5)每一个决定各一行**
    (`crates/bt-app/src/attention.rs` 造句、`crates/bt-app/src/attention_trace.rs` 落盘;
    文件/时钟/闭包门这套机器在 `crates/bt-app/src/trace.rs`,`BT_MOUSE_TRACE` 与它共用一份,不是第二份)。
    同款:**命名一个文件**、追加写、每行 flush、未设置零格式化开销。
    行首恒是 `<单调毫秒> <站名> tab=<下标> seat=SeatId(<n>)`(`attention::Site`),其后按站带字段。
    **凭据与来路分两栏**:`src=pipe|osc|bel`(**只有裸 `0x07` 才是 `bel`**)说它走的哪条路,
    `via=stop|stop-failure|bel|osc-1337|osc-9|osc-777|osc-99|notify|agent-settled` 说是谁说的
    ——加一个 CLI 只加 `via` 的名字,不动 `src`,这就是它们没合成一栏的原因(§13.2.2 闸门 ⑱②)。
    **按「决定」写行,不按「看见」写行**,现行站名一览:
    `mint … episode=… src=… gen=… grounds=… prev=…`(一次新请求开张)、
    `upgrade … [ticket=…] episode=… grounds=awaiting src=pipe gen=…` 与
    `downgrade … [ticket=…] episode=… grounds=requested src=pipe reason=…`(强弱凭据升降)、
    `admit … ticket=… episode=… grounds=… active=… focused=…`(铃响在没人看的地方 → 发号)、
    `refuse … episode=… reason=watched active=1 focused=1`(铃响在你正看着的 pane → 不发号)、
    `answer … [ticket=…] episode=… by=keyboard|ime|paste|files-row|mouse-button|mouse-wheel weak=… strong=…`
    (用户答了,两条水位线一起推到当前世代)、
    `withdraw … ticket=… episode=… reason=program src=…`(程序自撤)、
    `clear … episode=… src=… gen=… reason=…` 与 `drop … episode=… reason=…`(凭据消掉,后者手上没号)、
    `expire … ticket=… episode=… reason=leaf-gone|torn-out`(号没了:pane 关了,或跨窗撕出——**号不跨窗**)、
    `toast … why=awaiting ticket=… episode=… reach=…` 与
    `toast … why=turn-end episode=- reach=nothing|flash|toast src=… via=…`(桌面那扇门,一 episode 一次)、
    `claim tab=… episode=… was=… now=…`(这一趟把该 tab 的点改成了什么)、
    `jump queue=… from=… to=tab=…,seat=…,ticket=…`(`Ctrl+Shift+A` 走队列,消费不了任何东西)、
    `open tab=<下标> seat=… id=<TabId> by=toast restored=0|1 [leave=no-seat]` 与
    `open route=w=…&t=…&s=… leave=unparsed|no-window|no-tab`
    (**点了 toast 之后落在哪**——2026-08-26 补,C 片当初无法实机复验就是因为这一路一行不写;
    `restored=1` 表示窗当时是最小化的,`tab=` 是下标而 `id=` 是 toast 里写的 `TabId`,两个数都给是为了能和上面那行 `toast` 对上)。
    **什么也没决定的一帧一行不写**:那一趟每帧都跑,一枚长驻的闩若按「看见」写会每秒六十行同一句话;
    `the_attention_trace_writes_one_line_per_decision_and_none_otherwise` 两头都钉着。
    退役的旧词:`look … kept=1`(A2 把铃从这扇门拿掉后没有了)、`answer … door=enter`(改成 `by=`/`weak=`/`strong=`)。
  - `BT_PREVIEW_TRACE=<文件>` —— **预览正文从缓冲到玻璃这一路的取证**
    (`crates/bt-app/src/preview_trace.rs`;2026-08-21 立,由头是「markdown 预览打开时只画出两条横线、正文空白,
    往下拉一段就好了」那条报告)。三站:`build <surface> scale=… body=[…] scroll=[…]`(**造 body 用的输入,
    写在造之前**——一个在 scale 为 0 或在解算器还没给出矩形时造出来的 body,它的每个段落都是没有 clip 留得住的框,
    而画面上了就再也问不出这些数)、`built <surface> paragraphs=… quads=… blocks=…` 或
    `built <surface> leave=<no-rect|no-buffer|picture>`、`frame bodies=… paragraphs=… quads=… drawn=… prepared=<0|1>`
    (渲染器那一帧的账,`bt_render::PreviewTextFrame`)。**2026-08-23 它把这条报告钉死了**:病灶不在排版而在**取答案的那一层**——四条 worker 的答案通道是进程一条,而每扇窗各自 `try_recv`,开得最早的那扇窗把别人的答案取走后丢掉(`TabId` 是每扇窗自己的计数器发的);于是除第一扇窗外,预览的正文永远没有到过,画面上就只剩 pane 自己的头脚发丝线。见 `docs/DESIGN.md` §2.4 与 `CONVENTIONS.md`「共享队列上的地址必须一路唯一到取答案的那一层」。**那张截图能来自四个地方,这四个数把它们完全分开**:
    `bodies=0`(根本没造,屏上那两条横线是 pane 自己的头脚发丝线)、`paragraphs=0`(造了但文档是空的)、
    `drawn=0` 而 `paragraphs>0`(段落全被 `shape_preview_body` 的过滤器挡下——空 run、出了 clip、
    或 `crop_to` 拒绝的零高/倒置/NaN 框)、`prepared=0` 而 `drawn>0`(字形图集拒收整批,那一帧画了所有填充、
    一个字都没有)。**frame 站只在答案变了才写一行**(同 `BT_ATTENTION_TRACE` 的规矩),所以静止的 pane 一行,
    不是每秒六十行。**它一条行为都不改**。
  - `BT_WEB_DEV=<url>` —— **这一版进到网页的唯一一扇门**（Web 预览块 W2 片①，DESIGN §7.8）。
    设了就在活动 tab 的预览座位上开这一张网页（走 `preview_landing_surface`，与双击一个文件落地同一扇门），
    没设就一行代码都不跑。**没有地址栏也没有钮**——那是片④。导航只放行两个目标：这一条 URL，
    与宿主自铸的 `about:blank`；别的一律 `SetCancel(true)`。**测试与取证靠隔离 `LOCALAPPDATA` 拿隔离 UDF**
    （profile 在 `%LOCALAPPDATA%\Folio\WebView2`，不删）。它不写日志文件；引擎报的错误走 stderr 的 `BT_WEB ` 前缀。
  - `BT_PTY_DUMP=<文件>` —— ConPTY 原始字节 + `.chunks` 到达边界(见上一条 ui-probe 的坑)。**0823 起**:同进程第二段录音起自动 `<路径>.2`/`.3`(不再互相截断);`.chunks` 首行 `# SESSION ordinal/pid/started` 尾行 `# END chunks/bytes`——0 字节文件从此自报「开过没成流」还是「录了没声音」;目录项尺寸由发布线程每 200ms flush(以前程序活着时 `dir` 恒报 0 是假象)。**一次跑一个 dump 路径**(跨进程同路径仍会撞,只可见不避免)。空串=关(2026-08-25 已裁已落)。
  - `BT_PROBE_INPUT=<文件>` —— 不起 ConPTY,把文件字节直接灌进 Term。
  - `BT_STARTUP_TRACE=1`、`BT_RESIZE_TRACE=1`、`BT_LAYOUT_EVENTS=1`、`BT_IME_TRACE`、
    `BT_GIT_TRACE`(**空串 = 关**)、`BT_PERF_TRACE`(**空串 = 关**)、
    `BT_CHROME_DUMP=<文件>`、`BT_FOCUS_THUMB_DUMP=<文件>`、
    `BT_PSREADLINE_PROBE=<version>[,<policy>]`、`BT_BG`。
  - `BT_PANIC_SELFTEST=<秒>` —— **debug 构建独有**(release 半边是空函数)。窗口起来这么多秒后,
    在窗口线程上故意 panic 一次,用来在真机上量 §7.43 ④ 那条出口(panic 也走 `leave_process`)。
    `BT_HANG_SELFTEST=<秒>` 的对子,同一个形状:一次性、非正整数=关。
    **release 不许有受控 panic 入口**——`scripts/release/cleanvm/in-guest.ps1` 跑
    `folio.exe --panic-selftest` 并要求它按未知参数被拒,所以这个开关只能是 debug 的。
  - `BT_DPI` 不是开关:create/show/first-present/resized/scale-factor-changed 五个对账点**无条件**
    向 stderr 各打一行(`docs/M0-beta.md` §3)。
  - 仓库里另有一大批 `BT_*`,但它们是 `bt-replay` / `bt-repaint-oracle` / `bt-zoom-perf` 这些
    探针二进制和测试 fixture 的,**不是 `folio.exe` 会读的**。判据:
    `grep -rn 'env::var_os("BT_' crates/bt-app crates/bt-pty crates/bt-render`。
- **语义合并破坏**:文本上干净合并 ≠ 语义正确。今天多次:字符串表计数、快捷键表行数、
  cross-tab 的键类型变了、`WindowRuntime` 新字段没在新窗路径上被设。合完先 `cargo check`,再看新加的字段/行有没有被两边都照顾到。

---

## 3. 已经做了什么

### 3.1 今天(2026-08-20)合入的

| commit | 是什么 |
|---|---|
| `5236af6` | **多窗 D**:`session.json` v9,`windows[]`,一个写入口;重启只问**一次**恢复;非最后关掉的窗离开会话文件、以一行待在 Recent(第四种 seed) |
| `7875883` | **聚焦 F2**:整 tab 活缩略图 + 写下来的预算(四道门/10Hz,实测 0.0094ms 满负荷、0.0032ms 闲时) |
| `540a02c` | **OSC 8 Ctrl+点击**:五臂路由,`file:` 不再被挡 |
| `9629560` | **git 表头窄宽**:让位次序写死,`⑂` 永不落单 |
| `aa76da7` | **小样清偿**(五笔) |
| `99e3121` | **Web W1**:网页内容类 + 五张失败态 + 切换器网页行,画进小样,DESIGN §7.7 |
| `fd47133` | **缩略图说谎修复**:files 座位在 git 页时画「地方 + 类型」的面 |
| `17a3cc9` | **聚焦 F3**:橙圈脉动、`Ctrl+Shift+Z` 装上机器(**这份文档原先写成 `Ctrl+Shift+A`,那是 `jump-attention` 的键**;焦点模式的行是 `shortcuts.rs` 的 `focus-mode`,用户裁决 2026-08-19)、进场 FLIP —— **聚焦线收官** |
| `e8c9e04` | **一条点击总规矩**:平点 = 留在窗内,Ctrl+点 = 交给系统;`.html` 走浏览器 |
| `38ed1d8` | **折行链接**:活屏改按逻辑行识别(修掉**会开错网址**的正确性缺陷)+ 跨应用换行的重接规则 |
| `4ca9f01` | **文件改名光标**:`open_file` 把 caret 播在名字末尾而选区是主干,`Cargo|.toml` 归位;两扇门共用 `TabRename::seed` 钉住「有选区时 caret = 选区末端」;**Ctrl+A 落地**(裁决五当初靠它立论,此前手势缺席),路由在 `rename_key` 内、不进 `BINDINGS` |
| `a8f2ad2` | **呼吸备用屏豁免**:`SessionStatus.alternate_screen`,`fleet_working = working && !alternate_screen`;alt screen 上发的 133;C/D 不改 `working`(只认 primary)。Claude Code 等长驻 TUI 不再呼吸一整天;环/点通道未动。DESIGN §7.1.5b、UI-UX 第八节各补一段 |
| `51cb987` | **聚焦卡片拖动排序**:DESIGN §7.1.6b′ ④ 的留白由用户解禁——`focus_rail_run` 填 slots,卡片列直接吃 tab 条那一套(阈值、夹持、钉住分区、FLIP);②(pane 拖到卡列不撕 tab)改写成 `seats::TabRun::hosts_tear_out`,③(拒文件)原样。顺手修掉「按住卡拖 6px 切 tab 然后静默」 |
| `ca4493a` | **file 链接跨应用换行重接**:`run_across_break` 接缝允许外来墨迹(TUI 右侧的 `(25.4KB)` 列),拒绝的权力在 label 证据不在几何;label 证据对 `file:` 有第二种拼法 `file_uri_printed_form`(bt-viewport 私有,不依赖 bt-platform/bt-term)。DESIGN §7.1.5h ④ |
| `656d234` | **`BT_MOUSE_TRACE=<file>`**:按下→预览这条路的 20+ 个出口各一行 flush 日志(`mouse_trace.rs`),行为零改动。§2 新增诊断开关登记处 |
| `4cefda7` | **聚焦卡片第二投影加强**(用户复议:否决像素缩放,「左侧就是一个还在跑的终端,只是字小一点」):卡身 92→160、行数按格高(`FOCUS_MINI_*_ROWS` 常数删除)、按真实列位置缩排不从左截断(grapheme + 列宽)、preview 席位画已加载文档的头几行(`SeatSource::Document`,damage 带缓冲身份)。满负荷 0.0164ms/帧(预算 3.0)。与重排线唯一的缝是一条测试夹具高度(`rail_paint_of_in` / `TALL_FIXTURE_HEIGHT`) |
| `c35b8b0` | **new-tab 菜单三件**:① 聚焦+竖栏下菜单开了画不出(`profile_menu_layout` 的 `rail_geometry_now(now)?` 在 `column.unwrap_or` 之前把函数带走)→ 纯函数 `profile_menu_anchor`;② `New terminal in folder…` 行(复用 pane 头那行的选目录投递,`FolderPick::NewTabIn`,零新串);③ Recent 空白行:cwd 走 §7.1.4 阶梯 OSC 7 → `LeafSession::spawn_place`(运行时字段,不落盘),金库两扇门用 `Seed::names_itself` 拒收,窗口种子由第一颗说得出名字的 tab 命名。DESIGN §7.1.4a |

**并行线的纪律(今天验证过的)**:一次最多五条 Opus 线各自 worktree;合并一律 `--no-ff --no-commit` → 三门 → 提交;
三门里的负载敏感测试(`real_powershell_input_reaches_a_viewport_owned_frame`、`dir_watch`、`full_ring_blocks_writer`、
`a_pathological_dollar_screen_arms_nothing…` 的计时比)在多个 cargo 并发时会抖,**单跑绿即可放行**;
三门 + release 编译别塞进一条 600s 的后台命令(会被超时杀掉,合并卡在暂存区)。

### 3.1b 2026-08-21 凌晨合入的(八条,全部 Opus 线 + 用户裁决)

| commit | 是什么 |
|---|---|
| `0e254fa` | **单 d `˅` 按 `MenuSide` 翻不翻**:`MenuSide::turns_the_chevron()` 一处判据、三个面只读;`Below`(水平条)翻,`Beside`(竖栏/聚焦列)不翻、按钮用自己的 hover 底色说 open,tween 在 `Beside` 下根本不驱动;小样 rotate 收窄到 `.tabs-inline`;DESIGN §7.1.6e′ |
| `52d886d` | **裸 URL 止于首个非 ASCII 字节**(用户报第三条 URL 不识别):`is_url_terminator` 加 `!byte.is_ascii()`,校验器里永远为假的 `!candidate.is_ascii()` 删;DESIGN §7.1.5h ⑤ |
| `969ddb5` | **单 A alt 屏链接点击归本窗**(用户裁决撤 §7.1.5f 闸门 ③):`press_belongs_to_the_window` 去掉 `!modes.alternate_screen`,`modes` 参数消失;旧钉子反转保留;HANDOFF §6 第一条结案(DPI 嫌疑撤销) |
| `9c0e677` | **`test-env-immunity`**:见 §2 那条;`CONVENTIONS.md` 两条【事故】;export 退役 |
| `4761dd2` | **单 e 聚焦列 220→280**:新常量 `FOCUS_COLUMN_WIDTH_LOGICAL_PX`,rail 留 220;卡身 263、单 pane 59 列、分屏半卡 21→28 列(实机测得 advance,用户当初估的 ~28 是准的);`BUDGET_COLUMNS` 44→59 预算差在噪声里;**代价**:竖栏 Expanded 进聚焦舞台左移 60px(§5) |
| `00a7044` | **提示符起点清鼠标残留**(用户两次被迫关窗):OSC 133 `A` 在主屏到达时 `retire_program_input_modes`——1000/1002/1003/1005/1006/1004 归零,2004/1049/1007/DECCKM 不碰;走 vendor 的 `unset_private_mode`;真 pwsh + 真 ConPTY A/B 证明病与药;**但 PowerShell 要先装 `folio.ps1` 才发 133 A**(§5);DESIGN §7.1.5i |
| `78d3224` | **单 b 独 pane 幽灵 `⌄`(A 案)**:`Seats::seat_wears_ghost` 一个谓词管画与命中,角落 22×22、右 11 = 滚动沟槽 8 + rail 车道 3、静息墨 = `cmdrail::TICK_REST_OPACITY`(.45),开的是同一张 pane 菜单;胶囊开着整体让位(顺手修了 `after_search_change` 不刷 chrome);小样删 B 块与 `?lonehead=`;DESIGN §7.1.6i 改写 |
| `eecf29e` | **单 a 撤聚焦 Exit 头行/钮/Esc**:`ChromeTarget::FocusExit`、`FOCUS_BAR_*`/`FOCUS_EXIT_*`、`Text::FocusExit`(**`ALL` 419→418**)全删;卡片从面板顶边距起算(夺回 33 逻辑 px,不是 40);Esc 阶梯在聚焦态与态外逐字同义;`focus_mode_door_tests` 两条源码钉子;DESIGN §7.1.6b′「门」段改写 |

合并时的语义撞车(都已处理,下次照此):单 b 的幽灵 `⌄` 让单 d 的「找第一枚 chevron」夹具找到了幽灵——夹具现在交出幽灵的框、读取器绕开;单 a 删了 `exit_caption_width`,单 d/e 新加的夹具与测试各有一处引用要跟着删;
DESIGN §7.1.6b′ 的「卡片说什么」与「切片线」两段被单 a 与单 e 同时改过(字符级三方合并,两边改动都保住)。

### 3.1c 2026-08-21 下午合入的(五条,全部用户当场裁决)

| commit | 是什么 |
|---|---|
| `7dde89e` | **`1004` 撤出提示符归零名单 + X10 抬起**:真 ConPTY 字节流证明 `?1004h` 是 ConPTY 在会话第 42ms 自己开、退出时自己关的,「提示符时还开着」零信息量;`focus_reporting` 位保留。`MouseRoute::Forward{button,sgr}` 在按下当场记编码,跨过提示符的抬起以 SGR 收尾——**抬起回答的是那次按下,不是此刻的模式** |
| `e2bf081` | **目录平点 = files 列打开**(用户裁):`HyperlinkActivation::FilesColumn`,已有 files 座位换根、没有则开一列,两条都是 `Browse…`/拖文件夹既有的门;Ctrl+点仍 Reveal。由头是 §7.1.5j 让每个提示符的 cwd 都挂了虚线却点不动 |
| `44f9f2d` | **橙圈不灭**(用户实机报):入队现在也过 `attention_is_consumed` 这道门——旧代码有段注释论证「先发号后退闩这个顺序就是机制」,**那句话把因果说反了**;两个决定现在取自同一对事实一趟做完,顺序不再是任何人要重建的知识。出队一字未改。新 `BT_ATTENTION_TRACE`,`mouse_trace` 的机器抽成共用 `trace.rs` |
| `1070414` | **PowerShell 整合安装提示**(§7.1.6j):判据是**问 shell 本人** `$PROFILE.CurrentUserCurrentHost`——现场逼出来的,用户 Documents 重定向到 `D:\`,自己拼 `C:\Users\<user>\Documents` 的 build 会对一台早装好的机器报「未安装」。写入前逐字节备份、当日不覆盖、UTF-16 拒写;`Don't show again` → settings v18;OSC 133 A 一到提示条自撤 |
| `1913616` | **聚焦卡高度三档 + 顶对齐 + 每座位瞄准**(用户裁三件):`focus_card_height` 160/240/320(12/20/27 行)进 settings **v19**、`rail_posture` 每帧现读所以热切零 fan-out;短尾巴从贴底改**顶对齐**(推翻 08-20);每个终端座位一个 `card_skip`(滚轮 ±1、session **v10** 持久化、K>0 时格底一条发丝线),`BUDGET_ROWS` 13→27 仍在 3.0ms 预算内 |

**合并时的语义撞车(这批特别多,下次照此)**:三条线同日加 settings 键——**后合让号**落实为「整体重命名迁移步」(v17→v18 归 PowerShell,卡片高度改名 `migrate_settings_v18_to_v19`、fixture 更名 `settings_v19_*` 并改内部 `schema_version`、round_trip 的句子从「八次」改「九次」);
`SETTINGS_SCHEMA_VERSION` 与那条钉版本的断言**两边都写 18,会被 git 干净合成 18**,是语义破坏的典型,靠 round_trip 那条钉子抓住。
更隐蔽的一类:两条线**写了同形状的函数/测试/match 臂**(两个 `migrate_settings_v17_to_v18`、两个 `default_*`、两个 `apply_*`、两条 v17→v18 round_trip 测试、两组 i18n 臂),
git 切成 hunk 后 keep-both 会**让前一个的结尾被后一个的开头顶掉**(函数缺 `}`、测试体交错、match 臂缺 `),`)——`cargo test --no-run` 一路报 `unclosed delimiter` 才现形。
**解法**:同形状的东西不要逐 hunk 拼,把公共尾巴复制给两份,或整段重写成两个完整体。

### 3.1d 2026-08-21 傍晚合入的(三条,全部用户当场报/裁)

| commit | 是什么 |
|---|---|
| `aa6a725` | **`⌄` 菜单在标题栏空白处不收 + 聚焦态不出布局一瞥**。前者根因很值得记:`⌄` 右缘到 caption run 之间那条标题栏**不是客户区**——`title_bar_app_run_right_px` 把地界断在 `⌄` 右缘,`custom_frame_hit_test` 从那里起交回 `HTCAPTION`(窗口自己的拖拽把手),而 **winit 的 Windows 后端不处理 `WM_NCMOUSEMOVE`**:手落进去只收到一条 `CursorLeft`,此后一个动都没有。而 `pointer_left` 通知了 rail/搜索/tooltip/peek/超链接……**唯独没通知 `⌄` 的钟**,宽限从没起过,没起过的宽限永远走不完。修在**门**不在钟:`observe_chevrons` 改收 `Option<position>`,`pointer_left` 交 `None`,照原样落到 `Away`。后者:`peek_strip::eligible` 的文档里那句「refuses a tab whose cards are already unfolded beneath it…**This window has no card rail**」终于有了主语——聚焦卡列就是它说的那个东西 |
| `cdc42d2` | **滚轮瞄准要滑很久才动一行**。三层错:①余量被丢(精密滚轮一格拆六份,每份单独取整成 0);②**`round` 而不是 `trunc`**——半格轻推白走一整行,双向都错;③「不足一格」的判断站在「命中哪个座位」**之前**,那一下轮子既没被瞄准收下又漏给了卡列。外加 10Hz 限流:门是 **skip 不是 defer**,瞄准后若无别的事件再来一帧,图**永远追不上**已改的 `card_skip`。修:余量按座位跨事件累积(换方向/换座位清零,**不设超时**)、手势通道只跳最后一道门/只给一个座位/只算一次,`MIN_INTERVAL` 与内容通道一字未动 |
| `68de2dd` | **f — spring-loaded 拖 pane 过 tab**(DESIGN 新起 **§7.1.6k**):悬在别人的 tab 上 250ms(复用 `CHEVRON_HOVER_OPEN`,**常量共用、状态不共用**——`observe_chevrons` 第一句在拖拽时会 `clear()`,弹簧长在 `Drag` 上)→ 切过去继续拖;松手 = 移入,落点是目标树的 **rim 追加**(`plan_drop(LayoutAim::Rim(trailing_edge(split_axis(...))))`);原 tab 空掉走 `absorb_tab_into_strip` 而**不是** `close_tab`(那枚 shell 活在别的 tab 里);装不下就不亮、**不回落到撕出**;卡片列拒的是**源**(`hosts_tear_out` 改名 `hosts_pane_drop`);两步路一字未动。`move_seat_content` 一处写两处用,顺带补齐了合并那条路漏掉的 `git_scroll`/`git_graph_view` |

**顺带**:`scripts/dev/ui-probe.ps1` 的 `drag` 多了 `-HoldMs`(走到远端后按住停住再松),因为 f 的钟是**帧钟**,必须能在指针完全不动时到期,而原来的 `drag` 到达即松够不到它。默认 0 = 旧行为逐字不变。

### 3.1e 2026-08-21 深夜 – 08-22 凌晨合入的(六条)

| commit | 是什么 |
|---|---|
| `f537732` | **小样清账 i**:分屏 pane 头 `⊞`→`⌄`(与角落幽灵合成一枚按钮、两口钟 250/150);聚焦卡片列拿到重排手势——**「重建就销毁元素」这个难点不存在**:tab 条自己每次交换也重建 innerHTML,它活下来靠的是每帧按 `data-ws` 重查,**身份是 tab 的 id,比任何节点活得久**;`.pin-seam` **不做**——Q187 明裁「这条线只归竖栏」,卡片头上有 pin 图标,小样本来就守着 |
| `f45491f` | **聚焦列平滚轮归列,瞄准归 `Alt`**:`scroll_rail` 那句「innermost scroller under the pointer」引错了对象——瞄准不是 scroller。`Alt` 在 chrome 里零占用(`Shift` 已占两处:预览另一根轴、「这一下归本窗」)。**`max_scroll` 从来没错**:用户四卡的形状实算 208.5,第四张卡超出视口正好 208.5。发现性走 `Focus card height` 说明句(改既有串,`ALL` 不变)。**顺带纠正**:本产品**没有**字号缩放绑定,图片缩放走裸滚轮——`Ctrl`+滚轮其实是空的 |
| `a6c7537` | **预览 `.html` 头加 ↗「交给浏览器」**(§7.1.5g ②″):一个断言同时管「画不画」与「交哪条路径」;坐在 `Save`/`Edit source` 那一格(**W1 三钮 = 本类动词槽的后退/前进/刷新,不是 pop-out/pin/close**);**按下不印确认**——浏览器会走到前面,一扇刚失去前台的窗没有读者可以对他印字。`ALL` 426→427 |
| `6a448ed` | **`path:line[:col]`**(§7.1.5j ⑨):整条含行号是一个链接,点了预览**开在那一行**(新 `PreviewPane::goto_line` 是**意图**不是数字——开文件那一帧无行可量);盘符冒号永不被读成行号;**尾部非 ASCII 标点放开**(一条类不是清单),ASCII 点仍在名字里;图片线共用同一套词法,于是 `D:\shots.png。` 从「不是图片」变回「是图片」 |
| `6a448ed` | **软折行复查:病不在软折行**。五条真 vendor 网格的测试(活屏、真 reflow 变窄、冻结、alt 屏、CJK 散文)**全部原样通过,不是修好的**——识别按逻辑行读。真凶是**应用折行**(Claude Code 自己按宽度硬断,两行之间是真换行)。**没有擅自去接**,理由见 §5 第 17 条 |
| `<本次>` | **卡片自己武装读取**:preview 与 files 两个座位在聚焦态下被投影 = 「在屏幕上」的充分理由,该读就读。**投影层一字未改**(仍只从内存取文本),只是把「我缺什么」说出来(`focus_thumb::UnreadDir`),由拥有 worker 的调用者决定去不去。三条边界:只在聚焦态 / 一次(`PreviewBuffer::claim_head_read` + `DirCache::mark_pending`)/ I/O 在 worker。**回程也算一半**:读回来要为卡片要一帧。Git 页不在此列(它的脸另有三条理由) |

**这一批的教训**:三次「我在单子里写错前提,而线上的人查出来了」——`.pin-seam` 是明裁不是欠账;`Ctrl`+滚轮没被字号缩放占;W1 三钮不是我以为的那三枚。**单子里的前提要写成「先查证」而不是「已知」**,这三次都是靠这句话救回来的。

### 3.1f 2026-08-22 合入的:Web 预览块 W2 六片(用户点名提前做的一整块)

| commit | 片 | 是什么 |
|---|---|---|
| `ed297f0` | **W0′** | 十扇门在主仓复验:**6 pass / 3 caveat / 1 fail**。fail 是诚实的一条——照方案 §3 字面装强制器,**产品自铸的 `about:blank` 会被自己的安全门取消**;绕法落在片②。顺带修了探针自己**三处会说谎**的地方(门 7 杀错 renderer 却无论如何印「到了」、`shutdown` 只在事件到达时删 UDF、写完 `done` 八个进程都不退)。复验途中 Runtime 自己从 .93 升到 .101,把「自更新」那扇门从「以防万一」变成「一定会用上」 |
| `783c684` | **②** | 导航安全**纯策略模块** `webnav`:两扇门(地址栏 / `NavigationStarting` 复查)问**同一条规则**;宿主自铸目标走 `Mint` 账本(只认那一个字符串,`about:blank#evil` 都进不来);localhost **纯语法**判定不问 DNS;25 条合同测试。平移途中逮到探针把 `localhost:5173/app`(每个 dev server 都印的那行)读成 scheme 拒掉 |
| `90467b5` | **①** | WebView2 **visual hosting** 装进产品:visual 落在 pane 合成树**最底层**(`AddVisual(insertAbove=TRUE, NULL)`),窗口打一个**预乘零的洞**让它透出;§4 恢复状态机整体平移(含 W0′ 修出的两条:UDF 收尾**超时是唯一兜底**——优雅关闭路上 `ProcessFailed` 根本不来;`Closing` 态崩溃**不复活**已关 pane);键盘 30 和弦从**生效表推导**而非转录,Tab 走到边界交回键盘。实机逼出:**先导航后给尺寸**会得到「有底色没有字」的页面 |
| `4a612fe` | **③** | 网页 = **预览缓冲的一种**,进同一个池 / 切换器 / Recent / 会话恢复;身份只有**一本账**(`recoverable_url()`,只被 `NavigationCompleted(IsSuccess)` 写);`session.json` **v11** 带 URL(**明文条款**:含 token 原样存,恢复失败走登录属正常);钉**两次验证**(创建时 + 按下时),手工改成 `javascript:` 的 pin 加载后不导航。实机逼出三个真缺陷:web visual 是单数的、**尺寸与露面被当成一个问题**、背景 tab 的页面把行写进别人的池 |
| `35a2931` | **④** | 头上六件(地球标 / 名字即地址 / 三钮 / DevTools / 查找 / pop-out-pin-close)、脚的外交带、**五张失败卡**(理由直接由 `Refusal` 与 `Fault` 驱动)、`Ctrl`+滚轮缩放、搜索引擎(存**名字**不存模板)。`BINDINGS` 34→**37**(`Ctrl+L`/`F12`/`close-search`),`Text::ALL` 427→**445**,settings **v20**。**这一片的合并是这批里最重的**:它基线在 ③⑤ 之前,49 个编译错误的语义移植另开了一条集成线 |
| `f605089` | **⑥** | 卡片上的网页 = **它最后一次站在玻璃上的那一帧**。核心是把「52.7ms」拆开:**那是延迟不是成本**——同步调用 0.115ms、等待在浏览器进程、读回字节 0.014ms。发起后**不等**答案,渲染循环与基线逐毫秒相同(8.297 vs 8.299ms,180 帧零超时)。图**挂座位不挂卡**(否则滚一下列所有网页卡永久变白);编码字节与上一张相同就在解码前丢掉(实机:照 27 张存 1 张)。**`FULL_BLAST_BUDGET_MS = 3.0` 一个字未动**,0.0247→0.0250 |
| `305d2fb` | **⑤** | 本地 `.html`/`.htm`/`.pdf` 从 files 列进网页座位,URL 由**磁盘铸出来**而不是字符串拼的;四条安全开关逐条落实(补上了唯一真缺的 host object bridge);**通用 preview-file watcher**——外部存盘自刷,markdown 预览一并受益,去抖复用 git watch 那口钟(300ms 静默 / 2s 地板)。顺带清掉 `.htm` 那笔早就欠着的账 |

**这一块的三条方法论教训**(值得下次照抄):
① **先量再设计**——片⑥ 若信了「52.7ms 进不了 3ms 预算」就会走退路画静态图,量完发现退路的前提根本不成立。
② **单子里的前提要写成「先查证」**——这一块里我(Claude)写错过三次前提(`.pin-seam` 是明裁不是欠账、`Ctrl`+滚轮没被字号缩放占、W1「三钮」不是我以为的那三枚),三次都是靠这句话被线上的人捞回来。
③ **「重复定义」不等于「写了两遍」**——集成线发现两个 `go` 其实是两件事(一个是「新地址进这个座位」,一个是「问页面要它去过的地方」),删任何一份都是删一件功能。

### 3.1g 2026-08-23 合入的(下划线三案 + 页名 A 案 + Icons 栏 + 多窗 E1)

| commit | 是什么 |
|---|---|
| `bb27812` | **下划线验证饥饿两因齐修**:账本记「否」(`verdicts: BTreeMap<PathBuf,bool>`,答过不在的名字不再每帧重问吃预算)+ 预算花光的帧自己讨下一帧(`printed_path_budget_full`)。词法嫌疑被红测证伪排除。预算纪律 256/512/4096 未动 |
| `bf9b209` | `.pdf` 的 ↗ 亮灯(页名 A 案配套) |
| `7226b5b` | **页名 A 案**:`.html`/`.htm`/`.pdf` 从每扇门都以渲染页打开(`path_opens_as_a_page` 单一判据) |
| `7fc53a0` | **划线加固方案定稿**(`docs/plans/underline-hardening/`):Codex 对抗场景库(57 条带风险标注)全盘采纳——截断门罩路径也罩 URL、写满整行压住不画、app 折行案例终局=诚实不画线、`;` 仅在 `PATH=` 类标签上下文拆、边界表加 placement 轴、A–F 用例一条不减转测试。施工线已派 |
| `5d285b4` | **Icons 栏拖拽中照常滚开**:`drive_rail_zone` 提到 `pointer_moved` 门口,排在一切吞移动的手势之前(区域判定是「指针在哪」的事实,不因手里有东西失效);dwell 0、overlay 不重排舞台。聚焦模式「有图标栏」疑点核查为**无缺陷**(卡列 280px,焦点下 rail 不画)。DESIGN §7.1.6k 补裁决句。挂账:`RAIL_REACH`/`RAIL_KEEP` 宽容带仍在 sidebar ticket;`reset_advanced_row` 的死分支 `Row::Sidebar` 可清 |
| `9a68e73` | **多窗 E1 一进程一主题 + 光标传播**:`ApplicationChange` 第三个位 `caret`(不复用 `look`——四个像素不值得作废全窗公式栅格);flag 名单从声明上读出(加第四个 flag 不接 sweep 就红);`about_to_wait` settle 先于 `wake_deadline`。实机两窗同变有截图。DESIGN 新增 §2.8。Q5#2(标题栏几何双账)确认 A1 时已闭。挂账:per-window 主题(三条 static 注即开工首页);两个多窗探针脚本在 scratchpad 未入仓,片 F 实机验收要用 |

| `6932e5a` | **答案带窗名回家(routing 大修)**:四条 worker 答案通道由 App 抽一次,各窗只取写自己名字的(`answers_for`)——治好**第二扇窗**的三处永久哑症(files 列恒空、md 预览只剩头脚发丝线、裸路径永不亮);请求/回执加 `window: WindowId`,math/scale 换 `ShellAddress`。第二缝:`heal_preview_scroll` 钳动过的帧重造 body(=「滚一下才恢复」那半)。`BT_PREVIEW_TRACE` 加 `bytes`/`owed` 分开「文档空」与「答案没回」。CONVENTIONS 添【事故】共享队列地址必须一路唯一。挂账:`next_tab_id`/`epoch` 仍每窗一份(升 App 归 F1b);preview 文字 atlas 拒收无重试路径(同族雷,未动) |
| `d8f89e7` | **跨 tab 落点 + 聚焦卡收 pane + 中心顶人**(§7.1.6k′ 新立 + §7.1.6b′ ② 改判):外来 pane=单叶子树,走 `DropCargo::Layout` 既有三臂零新臂;`TabRun::hosts_pane_drop` 裂成 `PaneOffers{adopt,extract}`,卡列=ADOPT_ONLY(空白仍拒、自卡沉默);中心对子树=`ReplaceSeat` 顶人,标题改口 `Replace pane`,被顶者成 tab 落目标右邻(Claude 定的读法,用户可否决);提交一条路 `move_pane_across_tabs`。实机四组截图,M147「装不下就不亮」顺带实机验证。挂账:小样「卡列不接 pane」注释过时未跟;中心顶人实机未拍;`legdrag.ps1` 在 scratchpad 未入仓 |

| `c5004df` | **多窗 F1a WebView2 重挂 spike:GO**。活页跨两真窗搬家,boot id/scroll/时钟原样,零 navigation;九步序列 `REHOST_SEQUENCE`+按「每步改了什么」折叠出的补偿,死 HWND 实机注错→补偿把页放回源窗继续跑;补偿也失败→有损重建如实说。**DPI 答案:WebView2 自己是 scale 的 owner**(`ShouldDetectMonitorScaleChanges=true`,Folio 永不写 `RasterizationScale`;200%↔150% 混挂实测)。`WebSeat::rehost`/`SeatAddress` 带 `#[allow(dead_code)]` 等 F1b 调用。spike 档 `docs/spikes/spike-webview-rehost.md`。挂账:IME 真人打字归 F1b 实机验收 |
| `6a7785b` | **多窗 E2 Quit 四阶段事务**(`Ctrl+Shift+Q`,DESIGN §2.9):新模块 `quit.rs`(顺序=类型);①应用级脏门汇总卡(每窗都画)→②只读拍照(零 teardown)→③`flush_judged` 可判原子写(失败=留窗+告警卡+lock 不摘)→④`Quitting` 转 `WebSeat::tick` 至页尽或 6s。不复用 `exiting`(三条代码实证理由);关窗不进 Recent;未答复的恢复问题经 Quit 存活。`BINDINGS` 37→38、`Text::ALL` 446→450。**两条 Claude 裁决待复议**:汇总卡每窗都画;按钮序 `Save|Cancel|Discard` 不用 Windows 惯例序。它报的两条「待查」(第二窗 files 恒 Loading、窗 3 裸路径不亮)**已结案**=routing 大修(`6932e5a`)治好的共享队列病,基线旧所致,不立新单 |
| `8d650dc` | **划线体系化加固落地**(方案 `docs/plans/underline-hardening/`+DESIGN §7.1.5k):①末列截断门两面接(edge-suspect 不亮不问,旧 yes 也点不亮,URL 同罩)②五道闸重接(第⑤闸严守,`C:\WINDOWS\system` 案终局=不画;行号不进探测;回执带双 span+base,每帧重验几何)③path-list 模式(`;` 仅在声明的列表值内拆,只认字面盘符根段)④A–G 73 条对抗用例全转测试+边界表 placement 轴(11 行 edge 压住含第 12 行 URL)。顺手修一个非 ASCII 字段名越界 panic。**三条诚实冲突 `#[ignore]` 待裁**:无引号空格切断前缀仍指错(词法无分辨力,语义修法要加磁盘预算)、scrollback 相对引用不随 cwd 改绑(要按行携带 base)、否定判决永久 vs 文件后建(与 bb27812 账本合同冲突,要给「否」加 generation)。另:重接只装实时面(冻结面=不认,方向安全);内联图片未共用六条形状拒绝;用例 19 比场景书更保守(含 `\` 的 URL 两者都不画) |

| `5fd5a0c` | **独 pane/preview 动词面**:独 pane 右键长三行 pane 段(行=`PaneMenuRow` 本人,零新串;`Move pane to new tab` 按 DESIGN 裁掉并把小样自打旗的那行删齐);独 preview 戴回自己的头(小样明文,非推定)。挂账:子菜单展开态实机照片欠(桌面拒注入指针,几何钉着);`Move pane to new tab` 若要回右键段翻案点=`TERM_MENU_LONE_PANE_ROWS` 一处 |
| `f0f7d1d` | **OSC 133 A/B 真 shell 常驻测试**(bt-pty,双代 PowerShell 各一遍,非 `#[ignore]`):裸 shell 证静默、`. folio.ps1` 证 A/B 成对有序;脚本路径不许 canonicalize(`\?\` 被执行策略当远程路径拒签名)。挂账:C/D(命令结束/退出码)未盖,PSReadLine 上游风险其实在 C,值得低成本补一条 |
| `d9d57b2` | **拖拽一条协议**:转发中的移动也用按下时的编码闩(程序拖拽中翻 `1006` 不再劈成两种方言);抬起那半查实 `7dde89e` 早修。滚轮直读模式是对的(独立 Press 无手势),记档防误判 |
| `aadfbf3` | **图片 zoom 三症**:①②同因——每格重采样问「手路过的尺寸」,十问全废+六级陈旧台阶;修法=zoom 接 resize 既有 settle 边界(pan 故意不接),每格 2.0–3.8ms→0.55–0.72ms、手势中换栅 6→0。③滚轮累积**查实不存在且不应做**(倍率连续指数,碎报文数学上自累积,实测 1.2500 精确)。挂账:单趟 Lanczos3 0.2–0.8s 并行化是独立片(~8–12×);近 100% 倍率浪费属 `image_raster_cap` 政策题待裁 |
| `65e841b` | **`file:line:col` 相对形态查实陈账**(早已亮),落地的是行号×四道新门七格组合钉、盘符冒号 vs 行号冒号判据(紧跟分隔符 vs 只有十进制)、散文时钟/比分零候选。挂账:单引号裹引用丢行号(绝对同、方向安全);绝对形态 `a.md:12:3:4` 会拿 `a.md:12` 问磁盘(Windows ADS 语法,旧账记档) |

| `9bf15f0` | **BT_PTY_DUMP 空录音三缺陷齐修**:目录项不发布(0 字节是假象,`dir` 读的是缓存项)、多 pane 同路径截断式 create 互毁、spawn 失败留残骸。修法=同进程第二段起 `<path>.2`/`.3` + `.chunks` 首 `# SESSION` 尾 `# END`(0 字节自报「没成流」vs「没声音」)+ 200ms 发布线程 flush 目录项。待裁:空串值致 spawn 失败(与 `BT_MOUSE_TRACE` 空串=关不一致) |
| `3fcbb9b` | **多窗 F1b 身份簇+转移事务**(DESIGN §2.10):TabId 全 8 处铸号升 App 永不复用(A 案),权威=tab 站在哪扇 strip(`owner_of` 线性扫描,拒绝第二作者);回执按 `AnswerOwner::Tab/Window` 认领(浮窗 epoch 保持窗路由);pane 升格清四本 pending 账新号重发(保留已有答案不闪 Loading);drain 红测如实记「已被 6932e5a 偿付」收常驻钉;`transfer_tab(tab,into)` 三段事务,实机 boot id 不变、源窗关后 PTY 续吐 31 drain。**发现现行缺陷**:窗级 `web`/`web_thumbs` 裸 `SeatId` 做键,两个独立 tab 同开一页第二页导航第一页——同号页移交先诚实拒(`AmbiguousPage`),重键单已派。纪律违规自报:一次 sed(diff 核验无伤) |
| `1f47726` | **小样三欠账清 + 第三枚钉改锁**:nt-chev 双 `⌄` 一副钟(顺手修子菜单被判离开的真洞)、pane 菜单六行(方向图+五行一线,一张表读两遍)、卡列滚轮裸归列 Alt 瞄准;三枚「钉」查清——tab 钉与行钉同义有意复用,预览头那枚改 `Lock/Unlock`(锁梁=动作轴、填充=状态轴,tooltip 随状态换字;磁盘键留旧词不付 schema)。`Text::ALL` 450→452。**Claude 定可否决**:Lock 词/挂锁形/改名止于磁盘键 |

| `43006d5` | **多窗 F1c 动词面**(DESIGN §2.11):`Move pane to new window` 一行三扇门(pane 头 `⌄`/角落幽灵/右键段,右键段三行变四行按 §7.1.6i 自点名),实现=升格→`transfer_tab` 组合零第二条路;新窗带 `TearOut` 差事,被拒自关不记种子;拒绝各说一句(座位号不上卡)。`BT_TRANSFER_DEV` 退役。`Text::ALL` →455(合并后 457,再 456 见下)。**Claude 定可否决**:mark 用裸箭头 `External`;「Move to window ▸」不做(窗没有名字,那扇门归 F2 拖拽);tab 级动词面仍挂 DESIGN 1134 |
| `be11edc`+`1e1cfcb` | **web 表按 LeafId 重键**(DESIGN §7.12):`web`/`web_thumbs`/compositor visual 三表 + `SeatAddress` 带 tab;同根三症齐修(地址串台、地球标记前台 tab 功、缩略图共格)+实机暴露的第四症(恢复两带页 tab 时 preview buffer 误清);`AmbiguousPage` 整门退役(键性质保证可命名)。**合并事故记档**:F1c 的拒绝测试引用被删变体,门红(exit 101)时误提交了 `be11edc`,`1e1cfcb` 当即清残留回绿——**教训:先读 CARGO_EXIT 再 commit**。挂账 ⓑ:`PreviewSurface::Seat` 仍座位号(预览块的单) |
| `1bd7771` | **pin 分区当日清**(§7.12 ⓐ):`transfer_tab` commit 在目标 push 后补 `settle_pin_partition`,钉进欠单测试 |

| `7d943f6` | **多窗 F2 App 级拖拽 broker(多窗块收官)**(DESIGN §2.12):源窗只发布屏幕坐标,App 问 `WindowFromPoint` 谁的玻璃,目标窗用自己的几何/缩放作答并自画访客落点(`ForeignDrag` 信箱);落点表四支(本窗 Local/别窗 tab 条 Into/别窗正文 Nothing 按 J120/无窗 NewWindow 撕出);弹簧同一口 `CHEVRON_HOVER_OPEN` 钟经 `next_deadline` 折进 wait-until;撤防七因一坟(`finish_drag`);撕出 `tear_out_rect` 纯函数按点问 dpi/work area,抓握点跟手只缩装不下的。实机四场景真输入全过。合并时补跑 `cargo fmt --all`(该线只 fmt 了部分包——**教训:fmt 必须 --all**)。挂账:带活页跨窗拖没实机跑(门已通);跨窗落别窗舞台挑位置=§7.1.6k′ 的跨窗版未做;单 tab 窗拖到桌面=关旧开新(如实记) |

| `c9ce437` | **favicon 正式单**(DESIGN §7.13):`ChromeIcon` 本来就是像素+键(「第二条管线」的传说被考古推翻),favicon 在 `icons_for` 一处换掉地球;头/strip/幽灵/卡/脚/切换器/Recent 一张表带活,失败卡与恢复行**裁决为永远地球**(本窗的报告不是服务器的);按站点缓存 64 上限。`Text::ALL` 456 不变。挂账:切换器/Recent 行只有测试没照片(IME 改写探针标点);同站两页后说话赢;淘汰按最近学到 |
| `0e2803b` | **舞台文字整片失画=图集 trim 泄漏**(用户实机两截图,验收前最后一雷;DESIGN §2.2 第 4 条):`present_frame` 三个早退出口跳过 `atlas.trim` → `glyphs_in_use` 只增不减 → `AtlasFull` → `PresentWithoutText` 且无人讨下一帧=永久白屏;旧代码就地 trim 使「每轮饱和恰一个输家」,重排=重新抽签(与两张截图逐点吻合)。修:每帧出口无条件 trim(外壳无 `?` 无早返)、降级臂不再就地 trim、掉字帧=仍欠的帧(讨重画且不记 revision);顺带三处 `.is_ok()` 吞错并进同一条 `accept_text_prepare`。收敛=算术(单帧墨 <2.5Mpx² vs atlas 67Mpx²,重试至多一帧)。**协调者的共享队列假说被排除**(四条 lane 无一碰字形)。挂账:真机等比复现没做出来(需连续 acquire 失败);`handle_surface_failure` 全沉默值得加常驻诊断计数 |

**零碎批已清,`folio-next5` 已出(2026-08-24 午)**:`ce1444b` Sidebar 归位(距离=1 的钉,四行切片正是放楔子进来的洞;Focus 两行=同族非依赖的判准入册)| `55c2f7e` scrollback 字节顶(行数×2KiB 推导;洪水 859MB↗ → 688MB 平台;拒绝 256B 建议有算术;欠:ASCII 边界快路可再省 4×、tombstone 无界、文案「保留」→「最多」已裁待落)| `2ec1f15` 预览按叶(读问前台写全窗搜的分叉收敛为 preview_tab_id 唯一作者;待裁:切换器浮层随 tab 走要不要换 tab 即关)| `022bb9c` 无界清缴三件(resize 去抖同门、写侧 InputRing 一笔整进整拒+粘贴不再切块、fsync 出线程;欠:ResizePseudoConsole 时长未量、四个小 json 仍同步写待裁、一支过期 PSReadLine 探针该重写)| `fa547ec` 地板+飞行层(每页一纹素地板常驻,premultiplied-linear 编码有钉;flight 第三层整组换层+0.18 环;欠:整窗 resize 张开的带仍透明=swapchain 落后窗口,§7.1.6c-4b「不透明装刷」半句已作废,建议单开一片)。

**验收首批回报已全清,`folio-next4` 已出(2026-08-24 晨)**:用户总验收报了六件,全部当日修完合 main——① pane 出走后回不了自家 tab(`48073f9`:落点问「还站不站在里面」而非「是不是它的」);② 网页头「排布怪」定性为无病(悬停留位是裁过的),真病=离窗后悬停行不熄(`ef337e6`:离开门两个字段同清);③ 切换器 `file:` 行钉失灵(`fd5e5fc`:钉与按下回到同一扇门 `page_destination`,拒绝立可见卡);④ 开着大页拖大窗整程序挂死=PTY drain 活锁(`45791a5`:「读到环空」永不退,改一轮一份 quantum 回泵+自抬 wake,DESIGN §1.3 的旧契约终于落实);⑤ 无页开网址的门(`3debb02`:`Ctrl+Shift+L` 全窗动词,无页铸空白页、空框、Esc 未导航连座位撤回;`Ctrl+L` 一字未动);⑥ 白屏=图集 trim 泄漏(已在 next3)。**新增待裁**:回家后自条目再动又变撕出(有意语义);`Ctrl+Shift+L` 和弦/未导航即撤/空框即关框三条;钉拒绝卡文案。**挂账新队列**(活锁线审计出的同族):scrollback 按行不按字节(洪水一分钟 2GB)、非焦点 pane resize 不去抖直调 `ResizePseudoConsole`、`place_web_visual` 实时 resize 每帧新建 clip、一次性 `SetBoundsMode 0x8007139F` 未追。**零条线在跑(2026-08-23 深夜)——本块全部完工,总验收 build 已出**:`dist\folio-next3.exe`(08-23 23:17,HEAD `0e2803b`,74.8MB)。多窗 E1/E2/F1a/F1b/F1c/F2 全链 + 划线加固 + routing 大修 + favicon + 全部零散单,今日合 main 共 31 条。验收单已呈用户(实测清单 + 八条待裁);机器上有此前线残留的 ghost folio 窗占前台,用户手关或重启即愈。**机器现场注意**:桌面上有此前线残留的 hung folio/dwm ghost 数只(E2 线与 release 档,pid 42644/44848/52516/54964 一带),占前台拒切换——用户若发现焦点不听话即此;按纪律未 kill,重启机器或用户手关即愈。**裁决记档**:方案 v4 §① 的 App 级 drain/按 owner 投递红测归 **F1b**(身份簇一体工程,不归 E2)。

### 3.2 现在这个 build 里能用的东西(粗线条)

终端本体(ConPTY、转录、软折行、选区、搜索、命令标记轨、内联图片、公式渲染、Markdown/表格预览)、
tab 与分屏、拖拽换容器、files 列与预览 pane、git 面板与提交图、设置(多页、profiles.json、schemes)、
多窗(第二扇窗 + 会话恢复)、聚焦模式、通知(toast)、最低对比度地板、pins、i18n 中英双语、
资源管理器右键「在这里打开 Folio」。

### 3.3 关键版本号 / 计数(改之前先确认)

- `settings.json` schema **v25**(v23 = `turn_end_notification` 拆分,v24 = Cards 提示,v25 = `copy_on_select`)、`session.json` schema **v11**、`pins.json`/`profiles.json`/`keybindings.json` schema **v1**
- `Text::ALL` = **531**(08-30 凌晨,预览池磁盘提示四条 + WSL 组末说明;`i18n.rs` 数组长度即计数,合并冲突后必对齐);`shortcuts::BINDINGS` = **39** 行(`shortcuts.rs:3154` 断言钉着);`docs/shortcuts.md` 由 `scripts/generate-shortcuts-table.ps1` 生成、`check-shortcuts-table.ps1` 守漂移;`THIRD-PARTY-NOTICES.md` 由 `generate-notices.ps1` 生成、`check-notices.ps1` 守漂移(**合并任何动了 Cargo.lock 或版本号的线后必须重生成**,08-27 已两次漂移)

---

## 4. 接下来做什么

### 4.0 发布流程与发布后路线图(2026-08-29 用户裁决,凌驾本节其余旧序)

**发布前(顺序即执行顺序)**:① 08-29 在飞线落地合并 → `next18` → 用户验收 → **冻结**(只修不加);② 两台干净 VM 对 next18 的 zip 复跑烟测(`scripts/release/cleanvm/`);③ **历史重写**——全历史邮箱改 `weiyishi197@gmail.com`、去 `Claude-Session` trailer、留 Co-Authored-By,先打 bundle 备份,**零在飞线时做**(重写后旧 worktree 全部作废);④ 建仓 `folio-terminal` 推上去(**等用户定账号**:`gh` 现登录 lulu-loopp);⑤ CI 跑绿;⑥ tag `v0.1.0-preview`,Release 挂 zip + SHA256,release note 草稿**用户审稿**;⑦ Win11 VM 从 GitHub Release 真下载过 SmartScreen,把「怎么点开」写进 README。等用户的只有账号与审稿。

**v0.1.1 首项**:agent 装在 WSL 内的探测/启动/通知——懒探测「在 WSL 中找到」(打开配置页时问一次 `wsl -e which x`,缓存)、`wsl.exe -e x` 启动、hook 装进 WSL home 经互操作回 `folio.exe attention`;preview 先靠文案指路。

**v0.2 前段(用户 08-29 点头)**:
- **Typora 式 markdown 编辑**(所见即所得,预览 pane 内)。
- **快捷键呼出的临时终端(Quake 式)**:全局热键 `RegisterHotKey` → 从屏幕顶沿滑下的独立窗,失焦/Esc 收回,再呼出内容还在;它就是一个住在会藏起来的窗里的普通 tab,**零新规**。「承担多种功能」= 呼出时选配置:单按开默认临时 shell,热键+数字或菜单选「监视」「Claude」「便签」等;CPU/内存监视第一步**不造原生面板**,给一条内置配置跑 `btop`/`ntop` 类 TUI,原生仪表等真实需求再裁。两处要守:默认键避开 Win+`、Ctrl+` 等已占用键并给设置项;收回时把前台还给呼出前的那扇窗(用户按热键那一刻系统准许我们前台,收回不许把焦点丢错)。
- 聚焦搜索(用户裁定不急,v0.2 后段)。

**v0.3:多端**。平台无关层(渲染/布局/公式/转录/tab-pane-聚焦)不动,换 `bt-platform` 这条缝,按难度:PTY(ConPTY→openpty,vendor alacritty_terminal 自带 Unix 实现)、shell 整合(bash/zsh/fish 各一份,OSC 133/7 同协议)、Web 预览(WKWebView / WebKitGTK,`wry` 盖三家但 Linux 重)、视频(AVFoundation / GStreamer,路线 B 的「解码线程回读帧给 wgpu」结构保留)、注意力(Dock 弹跳/系统通知)、IME(mac 干净,Linux Wayland/X11 另一片坑)、注册表读平替。**mac 先,Linux 后。**

**裁决留档(08-29)**:聚焦模式上台卡的高亮**维持现状**(小样对照过描边强调色/外圈环/只留头/台前调度式消失,用户选现状,等真实用户反馈再动);卡片列空白**改判可撕新卡**(推翻 08-23 的那半句,与 tab 条同规,线已派)。


**第一优先永远是:用户实机验收后报的缺陷。** 他现在看的是 `dist\folio-next3.exe`,报缺陷时通常带截图。

**下一块(用户 2026-08-22 已同意方向,待他验完这一版就开)**:**首启「系统类」一屏 + 通用一次性提示机制 + 三条手势动画**。判据是用户自己划出来的——
**这个开关的效果发生在 Folio 窗口之外,并且「该问的那一刻」不会在窗口内到来** → 首启问一次;否则 → 就地提示。
按这条判:资源管理器右键 / PSReadLine patch / 桌面通知 → 首启;PowerShell 的 `folio.ps1` 整合**留在就地提示**(那一刻真的会到来);主题、字体、聚焦模式、卡片高度 → 都不进(有默认值、窗口内可发现)。
一屏不是向导、每行只陈述事实、全部默认在安全的那一侧、`Skip` 是一等公民(跳过 = 什么都不开)、设置里能再打开这一屏、永远不超过五六行。
**首启问卷式的「问一堆偏好」明确不做**,理由写进 DESIGN 当一条「不做」裁决:那把决定提前到了用户最没有依据的一刻。
提示条机制 = 把 §7.1.6j 那条抽通用:由「事实成立」触发不由「打开程序」触发、一句话且必须是此刻能做的动作、一次可关记在 settings、同屏最多一条、总数有上限。
**三条手势动画**(就地播、循环两三次就停、reduced-motion 退回文字、不阻塞):`Alt`+滚轮瞄准、spring-loaded 拖 pane 过 tab、卡片高度那一行。
**新表面 → 先小样定妆、用户审过再落原生**(memory `m2-style-first-roadmap`)。

之后是大件 **多窗 E→F**(Quit `Ctrl+Shift+Q` 挂在 E 一起做)。

**已完成(2026-08-21 凌晨派出,交单后照 §1 流程合):**

- ~~`DirWatch::start` 竞态~~ **已合**:`start()` 现在阻塞到监视线程第一次 `ReadDirectoryChangesW` 挂上;首读被拒从 `start()` 返回 `Err`;测试专用闸钉死竞态,24 自旋下 6/20 → 20/20。**纠正**:files 列没有 watcher,`DirWatch` 的调用方是 `git_watch` / `scheme_watch` / `storage_watch` 三处——files 列要不要长一个 watcher是产品裁决(§5)。

- ~~单 B 裸文件路径识别扩全部文件~~ **已合**:`bt-transcript::paths` 一个共用词法(图片扫描交出自带的 567 行边界规则改为委托),投影层把验证过的路径变成 `CellHyperlink::implicit("file:///…")`——与 OSC 8 的 `file:` 链接**分不出来**,五臂路由零改动;验证是 `SessionDecorationTask::VerifyPath` 走装饰 worker,账本记两种答案(4096 FIFO,一帧最多 256 问,pane 最多 512 待问);相对路径基准 = `reference_directory()`(OSC 7 → `spawn_place`,None 不认);alt 屏免费;密屏 113µs/帧。边界表 17 行在 DESIGN §7.1.5j。P3-8 三样已完成。

**用户 2026-08-20 已裁、还没开的小单(照单开,各一条 Opus 线):**

c. **预览 `.html` 的 pane 头加一枚 ↗「交给浏览器」**,照 Codex 地址栏右侧那枚,与 W1 三钮同位,走 `open_local_path`;先小样后原生。
~~f.~~ **已合**(`68de2dd`,DESIGN §7.1.6k)——原文留档:**spring-loaded 拖 pane 过 tab**:拖着 pane 悬在 tab 上 250ms(复用 ⌄ 的 dwell 常数)切到该 tab 继续拖,直接松在 tab 上 = 移入该 tab(追加为树末尾分屏);「Move pane to new tab」两步路保留。先只做普通模式,聚焦卡片列不做(§7.1.6b′ ②)。
g. **提示三档 + 「Claude Code bell」设置行**(用户要求先专题讨论再开;要点已进 memory):有焦点→不加;可见无焦点(副屏)→窗内信号 + `FlashWindowEx` 任务栏闪,不弹 toast;不可见→toast。要新引「窗口可见」事实源并把 `attention_is_consumed` 与 `reaches_the_desktop` 解耦。设置行:写 `~/.claude/settings.json` 的 `preferredNotifChannel=terminal_bell`,On 前存原值、Off 恢复不删,状态从文件读,默认 Off。
h. **Quit 动词**(`Ctrl+Shift+Q`,所有窗口整体写会话再退出、下次全部恢复)挂在多窗 E 一起做。
i. 小欠账:卡片列画钉住分区的 `.pin-seam`(归小样);小样补卡片拖拽手势(`paintFocusRail` 一变就重建 DOM);卡片短尾巴贴底的空白(裁决后果,用户看过 exe 再定);小样分屏头仍画 `⊞` 而原生已是 `⌄`(单 b 留档)。

**今天新冒出来、待开的小单(不需要裁决,按序开):**

- **PowerShell shell integration 的安装/提示**——**先等 §5 第 6 条的裁决定形态**,但不管哪种形态都要做:用户的两个 `$PROFILE` 是空文件,`folio.ps1` 是 opt-in,不装则 PowerShell 永远不发 OSC 133,鼠标残留修复、呼吸、退出码点全部拿不到。
- **转发中的抬起用按下时的编码答**:按下已转发、抬起未到时模式被清,`Released` 臂会发一次 X10 六字节(闩不卡死,已钉);正解是把编码闩进 `MouseRoute::Forward`。
- ~~`file:line:col` 引用的相对形态~~ —— **陈账,2026-08-23 查实早已在 `6a448ed` 落地**(`docs/a.md:12:3` 一直亮;`65e841b` 补了行号×四道新门的七格组合钉与盘符/行号冒号判据)。
- **独 preview pane 没有动词面**(单 b 发现):`seat_wears_head(Preview)` 独处时为 false,pane 菜单又是终端专属;§7.1.6h 让独 preview 可达之后这里是空的。
- **pane 独处时终端右键菜单追加 pane 动词段**(§7.1.6i 两案共同的「地板」,小样有原生无)。
- **真 shell 的 OSC 133 A/B 探针落成常驻测试**(鼠标残留线写过、按「别扩战场」撤了;bt-pty 现在空闲)。

之后按这个顺序(理由都在 DESIGN 里):

1. **多窗 E(statics)→ F(tear-out)**。F 会带来「把 pane 移到新窗口」这个动词,
   而那正是**独 pane 上「Move pane to new tab」是空动作**这条挂账的诚实兄弟(见 §5)。
2. **单 pane 缩放**(小件)。双击 pane 头那个手势**特意留空**给它,布局原语
   `LayoutMode::Focus { stage }` / `solve_focused` 也一直留着不删。
3. **横滚第一步**(flatten-and-window)。方案已过 Codex 四轮,`docs/plans/horizontal-scroll/`;
   前置是 Windows 落地那一批。硬性能门与类型契约都写在方案里,别绕。
4. **Web W2 六片**。W0′ spike 已执行(`spikes/webview2-w0/` + `w0-evidence.md`),W1 小样已交付;
   W1 留了五条待裁(见 §5)。片① 的第一条红测是 `AddVisual(insertAbove=TRUE, NULL)` 落到视觉树最底层那件事。
5. **命令面板**(`Ctrl+Shift+P` 现在是存根)+ **注意力队列本体(P1-8)**。
   F3 的「等你回答」只覆盖到 BEL 那一半,另一半等一个诚实的信号。
6. **Windows 落地余项**:run-as-admin、默认终端(需要用户的机器做 VM spike)、
   Win11 顶层右键菜单(需 package identity)、发行件方向(便携版 now,公开发布时双件 + 可选 sparse MSIX,
   代码签名是前置)。
7. **小件队列**:files/preview pane 头的 ⌄ 菜单(新表面,已报告未做)、行拖到 strip 的手势(未写)、
   两个「钉」同名同图形的命名单(W1 报告)、**awaiting 的真信号源**(让 Claude Code 一类 agent
   主动说「我在等你」——DESIGN 的「显式 side-channel 优先」,P3-4 可编程 API 的最小切片。
   2026-08-20 的呼吸单把备用屏上的呼吸关掉了,**那个 tab 现在什么都不显示**,而它本该显示「等你回答」;
   在这条信号源落地前,长驻 TUI 的 tab 是诚实的沉默,不是完整的答案)。

---

## 5. 待用户裁决(逐条带推荐,他随时会挑一条回答)

**2026-08-20 下午用户把上一版的 16 条整批裁了**(记录在 memory `rulings-0820-batch`,裁决原文进 DESIGN 时请引):
1/2/3 确认(BEL 两断言;退出聚焦无动画,且 Exit 钮整个撤——见 §4 a;`.html` 走路径桥);4 做(§4 c);5–9 按推荐;
9.5 → 280(§4 e);10 → A(§4 b);11 等多窗 F;12 保持 On + 设置行(§4 g);13 Quit 挂多窗 E(§4 h);14 等抱怨。

**还开着的:**
1. **图标 A/B/C 三变体**(用户说等全部做完再挑,变体活在 `?icons=` 后面)。
2. **files 列要不要改成单击就开**(今天是双击/Enter/Space;VS Code 是单击开临时预览、双击钉成 tab)。用户提过一嘴,没定。
3. **提示三档的细节**(§4 g)——用户明说「要详细讨论,尤其副屏上的 Claude Code」;推荐方案已给,等他点头。
4. ~~**卡片短尾巴贴底留白**~~ **已裁并落地**(`1913616`):改顶对齐,理由=「同一个终端的小图」,推翻 08-20。
5. `awaiting` 的真信号源:短期 = Claude Code 的 `terminal_bell`(已替他设上);长期 = 可编程 API(M4)。标题转轮停止**不推荐**当 awaiting。**本 build 里能标「在等你」的仍然只有 BEL**;「没响铃却站着等输入」那一半还缺一个诚实的信号(`bt-shellint` 不存在)。
6. ~~**【最要紧】PowerShell 拿不到 OSC 133**~~ —— **前提是错的,已结案(2026-08-21)**:用户的 Documents 重定向到 `D:\`,真正生效的 `$PROFILE` 是 `D:\Documents\{PowerShell,WindowsPowerShell}\Microsoft.PowerShell_profile.ps1`,**各 112 字节、2026-08-14 起就 dot-source 了 `folio.ps1`**;`C:\Users\Weiyi\Documents\...` 那两个 2 字节空文件不是 shell 读的那份。**用户机器上的 PowerShell 一直在发 OSC 133**,呼吸 / 退出码点 / 鼠标残留修复对他一直生效。**教训**(已进 §2):任何「用户环境里有没有装 X」的判断,**问那个程序自己,别拼路径**。安装提示单仍然做了(`1070414`),它服务的是没装的人。
7. ~~`1004` 该不该在名单里~~ **已裁并落地**(`7dde89e`):撤出。
8. ~~§7.1.5i 的「只在主屏」~~ **保持**(用户按推荐)。
9. **聚焦列 280 的代价**——仍**等用户看 exe**:竖栏 Expanded 下进聚焦,舞台左移 60 逻辑 px(以前 rail 与卡列同宽、舞台不动)。收法:rail 也 280(推翻本单)/ 卡列「窄名字列」偏好。
10. ~~命令面板那一行算不算第三扇门~~ **保持**(用户按推荐)。
11. **`Restart shell…` 与 M2 契约的出入**(鼠标残留线顺手发现):`restart_shell` 造全新 `LeafSession`、看不到转录移交,而 `docs/M2-restart-shell-contract.md` §0/§1.3 承诺 per-seat 重启**保留**旧转录并插一条边界记录。**没实测**,值得一条只读审计。
12. **files 列要不要长一个目录 watcher**(`DirWatch` 线发现):files 列现在**没有** watcher,外面改名/新建要重开文件夹才看得到。`DirWatch` 真正的三个用户是 `git_watch` / `scheme_watch` / `storage_watch`。
13. **一个 tab 有两列 files 时,点目录换根的是座位序第一列**——照既有行为取齐写进 DESIGN,不是用户当场裁的;要「离点击最近的那一列」是新单。
14. **标题栏「N waiting」药丸**仍不存在,所以 §7.1.5b「设置只关 UI 不关能力」在本 build 无 UI 可关。
15. **图片缩放(Ctrl+滚轮)也在丢余量**(滚轮瞄准线发现):它与瞄准共用 `wheel_zoom_notches`,精密滚轮同样「滑好久才动一档」。它有自己的裁决(`a detent is one step`),那条线按纪律没碰。**推荐**:给它开同样的跨事件累积(累积在调用方,`wheel_zoom_notches` 本身不动)。
16. **应用折行的推断引用接不起来**(`path:line` 线坐实):用户报的「pane 变窄链接就没下划线」**不是软折行**(软折行五条测试原样通过),是 Claude Code 自己按宽度硬断成两条逻辑行。§7.1.5h 的重接靠**标签证据**(两段拼起来正好拼出链接自己声明的目标),而推断引用**没人声明过目标**。**Claude 的推荐:做,但上五道闸**——①上半截结束在最后一列 ②下半截从第 0 列起 ③拼起来能被同一套词法读成一个 token ④拼出来的路径在磁盘上存在 ⑤两半各自单独都不是已验证的路径。那条线担心「只给路径开不给裸网址开会各说一套」,**我不同意**:它们不一样是因为**可用的证据不一样**(路径有磁盘可问,网址没有任何证人),写下来就是诚实的。**等用户裁。**
17. **`AtlasFull` 被三条道吞掉**(卡片武装线顺带查出):终端那条道对图集满有明写政策(打一行、`trim()`、下一帧重试),而 **chrome / preview / overlay 三条道全部 `.is_ok()` 吞掉**——不 trim、不重试、不请求一帧。与「预览首帧空白」同族但**没有证据说就是它**。要推广终端那条政策,需要 `PresentOutcome` 多一个「degraded,再给我一帧」。**等裁。**
18. ~~**`.htm` 有两扇同门的钮**~~ —— **已清**(W2 片⑤,`305d2fb`):`htm` 补进 `preview.rs` 的 `TEXT_EXTENSIONS` 与小样 `previewFtype`。原文留档如下——
18. **`.htm` 有两扇同门的钮**(↗ 线顺带查出,**早就存在**不是新长的):`preview_ftype` 的文本扩展名表有 `html` 没有 `htm`,于是 `.htm` 落「No preview for this file type」卡(卡上自带 `Open in default app`),而头上那枚 ↗ 按 `names_an_html_page`(两种拼法同一个对象)也出现。补法很小:把 `htm` 补进 `preview.rs` 的 `TEXT_EXTENSIONS` 与小样 `previewFtype`。
19. **预览席只读文件的头**(`PREVIEW_HEAD_BYTES`),所以 `docs/DESIGN.md:573` 这类指向大文件深处的行号会落在头的最后一行。要不要提高是另一件事。
20. **图片缩放仍在丢滚轮余量**(见第 15 条)。
21. **小样欠三笔**(清账线报的):tab 条 `+` 旁那枚 `⌄` 在小样里只认点击、没有 250/150 两口钟;小样 pane 菜单只有三行而裁决是六项;卡片窗口仍挂裸滚轮(原生已改 `Alt`)。
22. **Q187 只活在 Rust 注释里**(「`.pin-seam` 只归竖栏」),`docs/DESIGN.md` 全文搜不到。要不要给它补一段现在时。
23. **跨 tab 的「落在布局上」没开**(f 单的政策位,§7.0 法则③):弹簧切过去之后,指针移到舞台上不给落点、不画框、松手什么都不发生。裁决只给了「松在 tab 上」这一扇门。要开是独立一片(从一棵树摘下、落进另一棵)——**未做**不是做不了。


### Web 块(W2)自己的待裁与挂账(2026-08-22 逐片交回)

**待裁(逐条带推荐):**
- **W1 五条已裁**(2026-08-22 用户全按推荐):三钮维持右侧工具格 / 下载失败用覆页 sheet / 加 `Ctrl+L` 与 `F12` / Recent 与 session 扩到 URL(**明文条款**:URL 原样含 query、fragment 进 `session.json` 与 `pins.json`,可能含 token,恢复失败走登录属正常)/ 两枚「钉」的命名另开小单。记档在 `docs/plans/web-preview/plan.md` §5 与 memory `web-preview-block-rulings`。
- ~~**`.pdf` 的分类缝**~~ —— **已裁并落地**(2026-08-23 用户选 A,`7226b5b`,§7.10 ⑥ 改写为现在时):页面名(html/htm/pdf)从**任何入口**都开成渲染页,`File(.html)` buffer 经缓冲池自己那扇门转回页面道而不是 `Refused(Type)`;悬停卡改说「Opens as a page.」(`Text::ALL` 445→**446**);顺带修活了对渲染中本地页面一直是死的 ↗。~~**新留一条小裁决**:↗ 对本地 `.pdf` 页面不亮(判据只认 html/htm),要不要扩到 `path_opens_as_a_page` 等用户说。~~ —— **已裁并落地**(2026-08-23 同日第二裁,§7.10 ⑥ 末段 + §7.1.5g ②″ 改判标注):`preview_page_hand_off` 改问 `path_opens_as_a_page`,两条臂(`File` 源与本机 `file:` 的 `Web` 源)都按**页面类**判,所以本地 `.pdf` 页面的头上有 ↗;远端 `http(s)` 页面照旧没有(它没有文件可交,脚才是外交带)。判据只有一个作者,没有新写第二张扩展名表。
- **本地网页钉不住**(片⑤ 欠账 2):`switcher_pin_is_allowed` 未放宽,`file:` 走 `address_bar` 被拒,所以切换器行上的钉对本地页面无效;而「按下」那条路加了 `page_destination` 重铸,于是**手工改过的 `file:` 行能开**。**Claude 判为可接受**(`PinKind::File` 本来就完全不设门,同样的暴露换了条道,且重铸是从 canonicalize 过的 `PathBuf` 造的),已放行;要收紧是一条政策题。
- **`Alt+Left`/`Alt+Right` 归引擎还是归本窗**、**页面持有键盘时裸 `Esc` 归谁**(Folio 的 Esc 逃生梯在网页座位上够不着,实测退不出聚焦模式)——两条由片④ 按判据自定并写进 DESIGN,结论交回后转述。

**挂账(不裁也能过):**
- **触摸/笔没接**(片①):本机 `SM_DIGITIZER = 0`、`InjectTouchInput` 恒 `E_INVALIDARG`,不往仓库塞没人能证伪的代码。W0′ 门 4 已证「每接触点一个 `PointerInfo` 可行」,`spikes/webview2-w0/src/host.rs::send_pointer` 是现成参考。**要一台有数字化仪的机器**才能做。
- **取消导航 ≠ 没发请求**(片① 实机):Chromium 在 pointerdown 上就预取,`NavigationStarting` 取消收不回那一发。拒绝的是「去」,不是「碰」。写进 DESIGN §7.8。
- **网页缓冲不能撕成 float**(片③):一个 controller 一个 visual,float 没有引擎;今天没有入口能把它撕出去,故未设门。
- **头/strip/tab 那一格的记号还是 `#i-file`**(片③ 欠账 1):`seats::pane_mark` 按 `SeatKind` 判、看不见缓冲;切换器/Recent/恢复行已走 `marks::preview_row_mark`。**归片④**。
- **页面的名字是站点不是标题**(片③ 欠账 2):`DocumentTitleChanged` 未接。**归片④**,接上后只需在 `commit_web_page` 换一处 name。
- **`site_label` 对 `file:` 答自己**(片⑤ 欠账 3):没打开过的本地页面在切换器/Recent 里印整条 `file:///…` 而不是文件名。
- **图片不自刷**(片⑤ 欠账 4):watcher 不收图片,解码道是另一扇门。
- **`BT_GIT_TRACE` 兼作 preview watch 的开关**(`preview watch:` 前缀),没有新起环境变量。
- **favicon 立为正式一单,排在多窗 E→F 之后**(2026-08-23 用户同意):切换器/Recent 里网页行全是地球标、只能靠读文字区分,而 `FaviconChanged` 给的是**位图**,本窗记号管线全是按名缓存的矢量符号——接它要开第二条管线(位图进 chrome 图集、按站点缓存、失效),不是接个事件的事(片④ 记档)。地球标是对的只是不够分辨,故不插队。
- **人工验收项顺延**(W0′):IME 候选窗跟不跟 pane、autorepeat、跨 DPI(本机只剩一台显示器)、受保护媒体表面、GPU reset / TDR。

---

## 5.5 不许违反的不变量(动代码前先确认你没在推翻其中一条)

这些是产品的地基,写在 `docs/DESIGN.md` 里。它们不是风格偏好,是**推翻了就要重做一大片**的东西。

**§1.1 核心不变量(五条)**
1. **Canonical Transcript 是真相**(对已冻结内容);活动网格的真相是 grid 本身;富内容是转录区间上的**可撤销装饰**。
2. **双平面 + 单向冻结 + 可变尾部单一所有者**:稳态 alacritty scrollback = **0**;任一时刻只有一个 mutable owner;
   Live→Frozen 严格单向,**无 thaw**。其中拆出的两句要分清:
   **终端坐标不变量(不可动摇)**——ConPTY/vendor 永远持有固定 N×M 网格,**富内容绝不触发 PTY resize**;
   **投影布局不变量(可放宽)**——视口怎么把逻辑行映射到像素是我们的事。
3. **文档与投影分离**:`HistoryDocument` 无布局;每视口一份 `ViewportProjection`。
4. **核心逻辑与 GUI 解耦**:纯 crate + 语料回放。
5. **Damage 驱动调度,完整帧合成**。

**§7.0 内容×形态三法则**
6. **状态属于内容,随行不分叉**——容器不持有状态。
7. **拖拽换容器,落点定容器**——载荷 = 内容身份。
8. **一切限制是政策不是结构**——不开放的组合必须是**显式政策记录**(可翻案),不许变成结构性阻塞。
   今天「无终端 tab」那一片就是这条被违反了一年后的修复:拆墙,而不是翻案。

**§7.1 贯穿性纪律**
9. 预览与提交**共用同一个布局求解器**。
10. 手势期间**命中目标保持身份**、未变化的状态不得丢失。
11. 弹出浮层由**集中控制器互斥**。
12. 实现为独立纯布局求解器,**不模拟 CSS flex**。
13. **hover 原则**(被两次升格为产品核心差异点):查询 hover 化、**提交必点击**;高频任务 ≤2 次滑动 + 2 次点击。

**§9 三道退出门**(验收硬指标,不是软目标):G1 resize 生命周期回放矩阵、G2 多投影一致性、
G3 锚点与版本协议。原文:**「三门任一不过 → 按 R10 降级砍功能,不顺延。」**

---

## 5.6 里程碑坐标(我们现在站在哪)

**没有独立的 roadmap 文件**,排期总表在 `docs/PROBLEM-LIST.md` 的「里程碑映射」一节:

- **M-1 地基 spike** —— 已完成(`docs/spikes/README.md`:七项全 go / go-with-caveats)
- **M0 能跑的终端** —— 已过(`docs/M0-alpha.md` / `M0-beta.md`)
- **M1 正确性攻坚** —— 已过(宽度、字形、输入、resize、滚动选区、视觉稳定,`docs/M1*.md`)
- **M2 块模型 + shell 集成** —— **现在这里**。今年做的绝大部分(布局、tab/pane、files/preview、
  设置、多窗、聚焦、git 面板)都属于它
- **M3 核心卖点** / **M4 agent 时代** / **M5+ 扩展** —— **没有开工的证据**。
  注意 Web 预览属于 M5+ 的 P3-6/7,但因为用户先要,方案已经先行到 W2 待开工

DESIGN 标题现在是 v3.7。

---

## 6. 如实挂着的缺口(不是忘了,是做不了或已裁不做)

~~**副屏上单击本地文件链接不开预览**~~ —— **已结案(2026-08-20 晚):不是副屏,也不是 DPI。**
`BT_MOUSE_TRACE` 日志(`D:\folio-mouse.log`,**只读,别动**)把三个嫌疑一次判完:全程 `scale=2`,
`preview_landing_surface` 与 `activate_hyperlink` **一次都没被走到**——**跨屏 DPI 那条嫌疑就此撤销**,别再当活嫌疑。
真因在更早一格:用户点的是 **Claude Code,alt 屏**。日志是 `pane_press … pressed_target=Ours link=Some(…)`
紧跟 `pane_press forwarded=1 bytes=11 route=forward` —— 本窗认出了链接,却因 DESIGN §7.1.5f 闸门 ③「只在主屏」
把这一下整份转发给了程序。用户当晚**推翻了闸门 ③**(alt 屏现在也认 OSC 8 与裸 URL,下划线那句承诺在两块屏上一样欠着),
`press_belongs_to_the_window` 已去掉 `!modes.alternate_screen`,`modes` 参数随之消失。裁决、代价与 `Shift` 反向无解见 DESIGN §7.1.5f 段末。
**连带的两件**:alt 屏程序现在收不到压在链接上的那一下左键(用户知情接受);https 链接平点仍是 `arm=None`——§7.1.5g 平点 = 留在窗内,而网页预览(W2)没到货,所以**网页链接目前只有 `Ctrl+点`(交给浏览器)有动作**,W2 落地后平点才有去处。

- **PowerShell 整合未装 → 鼠标残留修复对用户不生效**(§5 第 6 条)。这不是代码缺口,是安装缺口,但后果一样。
- **X10 抬起残留**(§4 新小单):只在按键被物理按住跨过提示符时发生,六字节一次。
- **`1004` 的去留**(§5 第 7 条)。
- **`1015`(urxvt 鼠标编码)没关**:vendor 的 `NamedPrivateMode` 里没有它,这台终端进不去那个态——不是漏项。
- **verified 图片引用在 alt 屏上的点击没有实机证据**(单 A 线):要装饰 worker 真解码,`BT_PROBE_INPUT` 够不着;OSC 8 与裸 URL 两条腿已实测,图片那条只有代码同构做保证。
- **release 档的聚焦缩略图预算数拿不到**(单 e 线):本机 `cargo test --release` 编译 `main.rs` 时 rustc 栈溢出 `STATUS_STACK_BUFFER_OVERRUN`;DESIGN 上一段的 0.0164ms 是 release 数、本次 0.013ms 是 test 档数,两组不可直接比。
- **聚焦卡片列数是字面相关的**:59/28 列按默认 Consolas(0.5498em)算;换 Cascadia Mono(0.6em)同一条 114px 字行只有 26 列。测试把字行宽精确钉住、列数按默认字面钉住;「换字体后分屏卡还读不读得出来」没有规矩。

- **「等你回答」只有 BEL 这一半**:`bt-shellint` 这个 build 不存在,OSC 133 只给退出码与忙碌边界。
  一个卡在等输入却不响铃的 agent,队列看不见它。
- **标题栏「N waiting」药丸**这个 build 根本没有,所以「关它只关 UI 不关能力」暂时没有 UI 可关。
- **命令面板**没有,所以「聚焦模式的退出动词进命令面板」那条随它一起欠着。
- **一次 16ms 内响铃又被看到的窄竞态**:那一次不进队列(可论证是对的,已记录而不是糊掉)。
- **横滚**:产品**没有** Line wrapping 这个设置(小样里有一行,原生没有),CSI 14t 目前被适配层丢弃、需裁决。
- **`.foot-path` 截断方向**按内容分岔(路径截头 / URL 截尾),规则写在两处(小样侧)。

盘点又翻出这几笔(出处都在 DESIGN 或专门文档里,今天没动):

- **设置对话框没有 tooltip 这条通道**(§7.1.6c):`Move up` / `Already first` 两枚箭头的
  tooltip 因此一直欠着;profiles 的三条**幽灵行**(`TERM_PROGRAM`/`COLORTERM`/`FORCE_HYPERLINK`)
  预填、虚线、灰、**不可按**——领养一条没有任何东西读的变量会撒两个谎。
- **files 列图标**(§7.1.3f):DPI 下六个徽章落地时 `marks.rs` 要按物理像素重校;
  「图标能不能关」要不要成为设置里的一行,留给 A/B/C 选型同一张单。
- **浮窗树不给切换器**(§7.1.3g,R2)——观察项,未落实现。
- **公式线的尾巴**(`docs/HANDOFF-formula-rendering.md`):`\hbar` 一类输入 live 渲染失败、
  **边界滚动族**(明确写着「值得独立一单」,重印窗口与 bridge/occlusion 整合)、
  Codex CLI 主屏 TUI 路径未开工。那份文档里带删除线的项目是**已修**,别当欠账。
- **M2 布局求解器规格定稿未实现**(见 §7 的规格债表)。
- **小样欠聚焦卡片拖动**(`focus-card-reorder` 那条线记的):原生已经能拖卡重排,
  `design/ui-mockup.html` 还没有——它的 `paintFocusRail` 一遇结构变化就重建 `#focus-rail` 的
  innerHTML,拖到一半那个元素会被销毁,所以不是加个选择器的事,得单独一单。
  三处注释已改成「这是欠账不是裁决」,别再照着它们判「卡片不能拖」。

---

## 7. 地图

```
crates/                     (workspace 共 33 万行 Rust,不含 vendor)
  bt-app        22 万行  应用层:窗口、tab/pane、files/preview/git、设置、聚焦、快捷键、i18n
                         —— 二进制 crate(`folio.exe`),没有 lib.rs。
                         **main.rs 一个文件就 7.9 万行**,seats.rs 3.2 万、settings.rs 2.0 万、
                         profiles.rs 1.5 万。别整篇读,用 grep 定位。
  bt-term        3.4 万  终端语义:解析、转录、会话、内联图片、OSC(+ alacritty 兼容缝)
  bt-render      2.4 万  wgpu + cosmic-text:文本、chrome、主题、几何
  bt-viewport    8.5 千  每视口的投影、布局缓存、滚动锚定、超链接判定
  bt-persist     5.9 千  settings/session/profiles/pins 的**带版本**持久化 + 迁移
  bt-detect      6.7 千  `$$...$$` 块级检测与双生命周期/版本门
  bt-pty         5.9 千  ConPTY 所有权 + 有界的 PTY→Term 传输(DESIGN §1.3)
  bt-platform    5.9 千  winit 安全 API 之外的、**经过审计的**原生桥
  bt-layout      2.6 千  M2 布局求解器:给它一棵树和一个矩形,交回每个座位的矩形
  bt-corpus / bt-transcript / bt-doc / bt-math / bt-unicode(字宽唯一裁决者,96 行)
design/ui-mockup.html   设计权威
docs/DESIGN.md          裁决记录
docs/plans/             大方案(horizontal-scroll、web-preview)
spikes/webview2-w0/     W0′ 十扇门 spike(独立 workspace)
scripts/dev/ui-probe.ps1  实机探针
vendor/alacritty_terminal 改过的 vendor
dist/                   用户在用的 exe —— 只写没被占用的那个
```

**历史 handoff**:`docs/HANDOFF-formula-rendering.md`(公式渲染线,2026-07-28 收官)。
里程碑文档:`docs/M0-*.md`、`docs/M1*.md`、`docs/M2-*.md`。

**规格债(DESIGN §7.1.7)**——四块形式化工作,各有专门文档:

| | 是什么 | 文档与状态 |
|---|---|---|
| ① | 布局求解器的输入/输出与不变量级形式规格 | `docs/M2-layout-solver-spec.md`,**2026-08-03 定稿,文件头写着「未实现」**。而 `crates/bt-layout` 已经有自己的 `solve`/`LayoutMode` 在跑。**下个 session 若要动布局,先核对这两者的差距**,别假设规格已经落地 |
| ② | 持久化 schema v1 全字段定义 | `docs/M2-persistence-schema-v1.md`(草案);实际已走到 settings v17 / session v9,文档落后于代码 |
| ③ | Restart shell 的完整进程契约 | `docs/M2-restart-shell-contract.md`(草案) |
| ④ | 极小窗口下浮窗约束冲突的优先级 | `docs/M2-tiny-window-priority.md`,**2026-08-03 已闭合** |

另有 `docs/M2-preview-matrix-and-verbs.md`(预览内容准入表 + 统一打开动词,2026-07-28 用户裁决)——
动预览/打开动词之前必读,今天 `.html` 那条裁决就落在它的射程里。
