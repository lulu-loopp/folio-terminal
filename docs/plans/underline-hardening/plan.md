# 下划线体系化加固方案(骨架,2026-08-23;待 Codex 扩场景后定稿)

## 由头
裸路径/URL 识别(DESIGN §7.1.5f–j)分五批长成,每批修一个现场:折行重接、非 ASCII 终止、行号、验证饥饿。用户 2026-08-23 裁:**不再一案一修,把形态×情境考虑清楚一次完善**。触发案例(最重的一条是「指错」):

- **A. 折断的前半截碰巧存在**:`C:\WINDOWS\system32\...\Modules` 被应用硬折在 `system|32`,前半截 `C:\WINDOWS\system` 是真实目录 → 通过 verified 门亮线,点开的是没人指的地方。违反「指错比不认严重」。
- **B. 引号内 `;` 路径列表**:`PSModulePath="A;B"` 整串当一个 token 验不存在;拆开各是真路径。无既定规矩。
- (已修入册的先例:软折行本来就通、全角前导排除、验证饥饿两因、`path:line:col`、尾部非 ASCII 放开、ASCII 点连认——见 §7.1.5j 边界表 23 行。)

## 方案四件
1. **折断截断门**:token 结束在行最后一列 = 疑似被折,**单独不验证不亮**(宁不认不指错)。治 A。
2. **五道闸重接**(应用折行的推断引用,此前挂账 #16):①上半截止于最后一列 ②下半截起于第 0 列(或与上半同缩进?待 Codex 议)③拼接后被同一套词法读成一个 token ④拼出的路径磁盘上存在(第二证人) ⑤两半各自单独都不是已验证路径。裸 URL 无磁盘证人,明写不重接(证据不同说不同的话)。
3. **引号内列表**:引号 token 整体验不存在时,按 `;`(PATH 语义)拆段逐验?或 `;` 直接为引号内终止符?待议——注意引号是「边界声明」的既有裁决,拆开是否背叛它。
4. **对抗性用例库**:边界表升级为组合矩阵(app 折行 × 软折行 × 引号 × 列表 × `:line:col` × CJK 前后 × 宽字形 × alt 屏 × 存在/不存在/前缀存在),每格一条测试;新形态先进表再写码。

## 待 Codex 补全的
- 还有哪些**没被想到的形态**(对抗视角:什么输入会让现规则「指错」而不是「不认」)。
- 四件方案各自的反例与边界(尤其 ① 会不会把合法的「恰好写满一行」的路径误伤;② 的第 ⑤ 闸与案例 A 的关系——A 的前半截单独已验证,⑤ 会拒绝重接,那 A 靠 ① 治,two gates 的分工要说清)。
- 现有 23 行边界表哪些行在新门下要改判。

## 定稿(2026-08-23,采纳 codex-scenarios.md 全部裁断;该文档与本方案同为施工规范)

- **①截断门**扩为:inferred 路径**与 URL** 的候选 span 触到物理行最后一个 visual cell = `edge-suspect`,压住不画、不进「可点 yes」;OSC 8 明示链接不受此门;「恰好写满一行」同压(不拿存在放行——截断前缀恰可能存在);候选后还有闭括号/全角标点/实际打印字符则不算末列;resize/reflow/行变 → generation 失效。
- **②五道闸重接**首版极保守:第⑤闸不放宽,案例 A 的终局是「不画线」;下行只认第 0 列起(同缩进是 peer 行常态,不是续行证据);第③闸写成精确断言(拼接交回词法器得到**恰好一个**覆盖全部非装饰字符的候选);第④闸验词法拆出的磁盘目标不验显示串,回执带拼接 generation + 两行 id + 两段 span + resolution base,任一变即丢;URL 不重拼但上半截 edge-suspect 压住、下半截自判。
- **③列表**:不做全局 `;` 拆(分号是合法文件名字符);新增显式 path-list 模式,仅在 `PATH=`/`PSModulePath=`/明确标签上下文且值语法完整时逐段(段级独立引号/解析/探测/画线,分号本身永不画);首版只认字面盘符根路径段,空段/变量段/相对段不认。
- **④矩阵**:每形态三套 fixture(完整目标存在/仅危险前缀存在/两者都存在);oracle 断言 span+printed target+解析 PathBuf/URL+行列+resolution base+generation+点击路由;加状态维(unknown→pending→yes/no、晚到回执、cwd 变、resize、scrollback 再现、存在性翻转)、所有权维(四种 span 互斥优先级)、cell 维(末列/倒数列/候选后闭括/全角/宽字占末两格/80→120/soft-wrap vs CRLF)。
- **边界表**:23 行字面判决不改,加 placement 轴(interior 保持 / edge+硬换行压住 / edge+软折先重组再判);行末形态下第 1、2、10、11、12、13、15、16、18、20、23 行条件改判为压住;表后新增双物理行组与分号三行组(细目照 codex-scenarios.md「边界表改判」节)。
- **用例清单**(codex-scenarios.md 第四节 A–F 六组)整体转为测试,一条不减。

施工排一条线,等当前五条腾出槽位即开。
