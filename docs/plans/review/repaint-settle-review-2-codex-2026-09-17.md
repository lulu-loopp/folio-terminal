# Repaint settle review, round 2 — 2026-09-17

Verdict: **HOLD for 0.4.2**. The feed-commit repair works, but resize still mixes coordinate generations; pending-worker body edits also remain stranded.
Scope: git show reviewed for 04cedbf6, c305cb18, 15fc7ef1, bcb514b6. References are at bcb514b6, relative to crates/bt-term unless qualified; rows are zero-based. Bare session.rs and line references mean src/session.rs. Product code read-only.
Validation restricted to the three user-authorized cargo targets; no application launch or process termination.

- Budget verified at bcb514b6 (tests/lifecycle_matrix.rs:2080,2213,2306): lifecycle 43/43; old primary arm 16 allocations / 21,304 B (120x40), 21,504 B (120x80); new 120x16 carried arm 644 / 566,946 B, with one off-band record 1,017 / 761,621 B per cycle. No allocation regression versus 15fc7ef1; independently confirmed below.
- Publication order: session.rs:2990-3040 drains every parser quantum and paused-marker remainder before settlement; grouped feeds defer it to end_feed_turn (2901-2915), called synchronously by crates/bt-app/src/main.rs:84008-84013. Settlement finishes projection, off-band restore, and primary hold reconciliation (3114-3125) before alternate re-snapshot (3135-3140); primary re-snapshots on its next feed (2965). There is no publication callback in this path; callers must honor the documented no-publication-inside-group contract.
- Trigger cost: any changed fingerprint, even an unrelated status row, advances the revision (session.rs:6859-6875); an existing window then runs the full close at turn end. Ordinary spinner/echo bytes inside an uninterrupted DEC 2026 block are withheld, so they cannot do this until commit; marker/overflow commits are real commits, not mid-atomic settlement. Reopen requires a boundary observed in the current turn (3135-3137), not merely a deadline. Re-arming adds O(changed rows * resident records + covered band lengths), no allocation (6918-6930); renderer bookkeeping does not write terminal cells. Fingerprints include style, wrapping, hyperlinks and producer provenance (src/cell_capture.rs:39-80), not just glyphs. No self-sustaining re-arm/detect/damage loop found.
- **P2 — pending work still strands a changed body:** rearm_live_bands_containing walks only resident records (session.rs:6918-6923); before the first worker completion there is none. Scratch: schedule a three-row x^2 block, hold its tasks, rewrite only the body to y^2 with cursor returned to the prompt, return the old result (correctly rejected source-changed), then advance stability and complete all work: primary remains 0/1 rasters. Candidate signatures survive (3786-3791); rejection only counts/logs the refusal (7316-7329). Re-arm the rejected task's candidate/dependencies or track pending dependencies too. This is a pre-existing gap left outside bcb514b6's resident-only repair, not stale-raster acceptance; confirmed on both screens below.
- **P1 — resize preserves the old window and double-projects its newly seated records:** resize_at consumes terminal damage without observe_live_damage (session.rs:3319-3320), projects a separate local snapshot (3313,3350-3351), and leaves alternate_repaint_snapshot alive at its old revision. Scratch: 48x14 alternate grid, identical rendered three-row blocks at 5 and 9, cursor on row 13; BSU+HOME, resize to 48x10, then ESU without any text. Resize correctly moves both blocks to 1 and 5 (2 rasters); ESU runs the old snapshot-to-current -4 projection over those new coordinates (6084-6099), leaving 1 raster on the identical unchanged final grid. Reset/rebase the open window at every out-of-feed grid mutation, after its projection; a revision bump alone still leaves the old snapshot to double-project. This is the round-1 mixed-coordinate fault through resize, so c305cb18 closes the feed-commit path only.
- Follow-up: pending-body liveness also fails on alternate (0/1); resident changed-body and commit/reopen safety+liveness pass once the probe returns the cursor to the prompt. Mixed-coordinate and strict-before-deadline timing probes both pass (2/2), and the committed atomic-scroll control passes (2/2). Original five cases are closed; the broader structural and pending-work cases above remain open.
- Other mutation paths: CSI S/T and insert/delete-line mark full damage (vendor/alacritty_terminal/src/term/mod.rs:1270,1380,2558-2575); screen switches invalidate residents/reset row fingerprints (session.rs:9727-9751); RIS and clears reach damage observation. Scratch commit/reopen matrix preserved complete formulas after S/T/insert, dropped them after clear/RIS/screen swap, and retained only exact clipped suffixes after delete/local-region scroll. Buffered status text remained absent before ESU. No missing content witness found there; resize is the concrete exception above. wrapping_add (6874) handles MAX-to-zero normally; equality aliases only after a full 2^64 increments between comparisons. The row hash itself remains probabilistic (src/cell_capture.rs:34-38).
- Residual explanation (code inference, owner's replay not rerun): stale means an old-layout artifact, while unbacked means the current detector no longer owns its source (session.rs:5975-5992); neither by itself proves wrong text. Fixed-tail clipping (6032-6039,13646-13669) can strand a lone boundary row, and identity fallback requires two supporting rows (13574-13576), so that one-row shape is especially dependent on bounded detection/exact restore. insert_nonoverlapping_live_record is strictly first-wins (13824-13835), with no stale/backed priority: a stale hold seated first CAN exclude a later backed record. Projection checks exact visible rows, not current detector ownership; restore checks exact source plus the code-context gate (6704-6716); publication reads artifacts without another source/ownership check (8553-8589). Thus an unbacked stale hold CAN be published at ordinary ESU, but a one-row record whose sole source row has different bytes is rejected by projection (13653-13669), not accepted merely because it is stale. The fresh one-row x^2-to-y^2 scratch control above a fixed tail, with a stale raster created by width resize, passes (0 old rasters). The supplied two events cannot be pinned on overlap ordering without their competing records/placements; full-grid re-detection ownership is stronger than the existing continuation proof.
- Independent before/after bcb514b6 budget check: temporary detached 15fc7ef1 worktree passes lifecycle 43/43 with exactly the same four allocation/byte figures. Carried 128-cycle elapsed samples: parent 78.44 / 70.63 ms without/with off-band, current 78.51 / 72.09 ms; these single suite runs do not establish a CPU regression bound for many blocks. The new allocation arm covers three blocks, not full-screen block-count scaling; the in-place re-arm scan's CPU complexity remains as stated above. The extra bounded detection is once per close when unresolved records require it, not necessarily once per off-band record.
- Baseline validation: authorized commands cargo test -p bt-term --test repaint_flash_oracle -j 4 (15/15), cargo test -p bt-term --test lifecycle_matrix -j 4 (43/43), cargo test -p bt-term --lib repaint -j 4 (13/13). Six temporary public-API tests expand oracle to 21: 19 pass; pending-worker edit and resize/empty-ESU fail. They include reconstructed round-1 mixed-coordinate/timing and changed-body/commit-reopen probes, an eight-operation matrix, and the stale one-row control; the committed atomic-scroll test supplies the fifth round-1 control. No application, replay executable, or external renderer was launched; no process was terminated.
- Parent comparison: copying only the oracle test file into 15fc7ef1 produces 15 passes / 6 failures. Both new review failures (resize 2-to-1 and pending-body 0/1) reproduce there; bcb514b6 repairs the three committed resident-body tests and the reconstructed resident changed-body probe. Findings describe remaining gaps, not regressions introduced by bcb514b6.

Merge gates: fix the resize/window coordinate lifecycle and add its two-identical-block regression; re-arm source-changed pending completions and test both screens. Keep the owner's one-row residual tracked separately; this review did not reproduce its exact trace.
Cleanup: temporary oracle additions and detached parent worktree removed; restored oracle passes 15/15 again. Product/test diff is empty. Only this LF report is committed on docs/repaint-settle-review-2, directly from bcb514b6.
