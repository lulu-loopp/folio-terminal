# Repaint settle review, round 6

Base: 2b42eab1. Verdict: **HOLD for 0.4.2** on one P2 (clip/resync ownership context).

- Report opened before investigation. Scope: round-5 split/raw-scanner replay, unshared resync state, walker cost, four authorized suites and six budget lines.
- Recovered the exact round-5 harness; verified its old scanner function byte-for-byte (LF-normalized) against e4a6e57b. All 36,396 split checks and 32,120 raw scanner comparisons pass: full blocks/source bytes, ambiguous/refused-table outputs, raw ledger, final neutrality. Baseline bt-detect: 151 units + 1 integration pass.
- P2 / restore gate remains unsound after clip resync: rows [x=y, $$, opening triple-backtick fence, code, $$x^2$$, closing fence (or more code), tail, prompt]. With the existing off-band source seed, both variants immediately hide row 4 and report HeldUnbackedRecord { LiveGrid(4), "$$x^2$$" }, while the ledger rejects row 1 as OpenerAboveWindow and row 4 as CommonMarkCodeContext; the unchanged row returns as source after settling. Scratch oracle: 24 pass / 1 fail (both variants). Scanner consumes the clipped closer at bt-detect/src/lib.rs:1722; walker opens Dollars there, swallowing the fence. session.rs:13475 supplies that wrong prefix to :6895 and bypasses :6946. This is pre-existing unshared-state drift, not a raw scanner regression.
- All four authorized baseline suites pass: bt-detect 151 units + 1 integration (0 doctests); repaint_flash_oracle 24 (including all four submitted variants); lifecycle_matrix 45; detection units 11 (494 filtered).
- G1_TUI_REPAINT 120x40: 16 allocations / 21,304 bytes per cycle.
- G1_TUI_REPAINT 120x80: 16 allocations / 21,504 bytes per cycle.
- G1_CARRIED_REPAINT 120x16 off_band=false: 644 allocations / 566,946 bytes per cycle.
- G1_CARRIED_REPAINT 120x16 off_band=true: 1,017 allocations / 761,621 bytes per cycle.
- G1_RESTORED_REPAINT 100x40 blocks=1: 2,984 allocations / 1,994,690 bytes per cycle.
- G1_RESTORED_REPAINT 100x40 blocks=8: 3,477 allocations / 2,204,756 bytes per cycle.
- All six budget lines reproduce exactly; the corrected whole-cycle cost comment no longer attributes the entire between-arm delta to ownership checking.
- Unshared-state counterexamples confirmed (same P2): [$$, empty line, $$, x=y, $$], ids 1..5, Known prefix. With live_grid_boundary=Some(2), or frozen_resync=true and no boundary, the scanner records PhantomOpenerAbandoned, owns ids 3..5, and ends neutral; the walker ends with Dollars open at id 5. Gates at bt-detect/src/lib.rs:1750 and :1775 therefore do not establish prefix equivalence. No terminal consequence is claimed separately for these phantom cases.
- Real clip witness, without an injected index: [x=y, $$], first_grid=0, Known prefix yields clipped_open_index=Some(1); scanner ends neutral with OpenerAboveWindow, walker retains Dollars at id 2. The terminal repro above confirms this state difference reaches publication. The submitted equivalence test passes no clip/boundary and frozen_resync=false (:3742), and compares only final neutrality, not exact opening/fence identity; it cannot cover these recoveries.
- Walker cost: :1481 calls only current-line structural/pairing helpers; no history scan, body accumulation or growing container. Fixed passes remain O(line bytes), retained state bounded. Thread-local scratch allocator compared 74baff25 walker against submitted walker for 1,000 calls at 16/1,024/65,536-byte bodies: all neutral cases 0 allocations / 0 B; all carried-align cases 1,000 / 12,000 B, identical old/new. Existing environment closer formatting allocates; the walker is not literally allocation-free.
- Scratch resync/allocator probes pass (153 detector units + 1 integration). The first allocator probe compilation hit the workspace unsafe-code deny lint; a test-module-only allow enabled the counter. No product defect is inferred from that scratch setup error.
- Must fix: derive restore ownership prefixes from the authoritative scan including clip/resync decisions, or conservatively decline a restore whose prefix is uncertified. Retain the O(line) streaming walker; do not rescan every growing prefix. Add the closed/open-fence clip restore regression and resync-enabled carried-state checks. Round-5 closer precedence is fixed; this remaining P2 is distinct from it.
- Verdict: HOLD for 0.4.2 on the confirmed clip/restore P2. No raw scanner regression, walker allocation growth, or budget regression found.
- Cleanup: product/test files restored byte-for-byte; clean final detector rerun passes 151 units + 1 integration. Only this English LF report is committed on docs/repaint-settle-review-6 from 2b42eab1. Only the four authorized Cargo command forms ran; no application/replay launched and no process terminated.
