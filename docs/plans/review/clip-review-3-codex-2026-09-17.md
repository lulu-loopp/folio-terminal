# Clipped-tail body proof review, round 3

Verdict: **MERGE** at `6605f7d7`. Read `git show edbbca61`, `git show 6605f7d7`, and the round-2 report; compared with main `ecf98443`.
Release bar: previously correct blocks refused, mismatched pictures, or drawing a sub-range of a PROVEN block block release. No blocker reproduced under the owner's revised occurrence rule.
Method: temporary differential integration probes compiled main and target detector sources against identical inputs; checked exact occurrence endpoints, original bytes and render source. Product code unchanged. Row indices below are zero-based.

**Round-2 B1 closed.** [`\begin{aligned}`, `x&=y`, `\end{aligned}`, `$$`] preserves the environment at 0..2, matching main with output stopped, with `a=b` appended (open), with [`a=b`, `$$`] (valid closure), and with [`ordinary prose here`, `$$`] (refused closure).
Two consecutive environments and the one-line environment followed by `z=w` also match main in all four states. Dollar, bracket, inline (including a wrapped run), and ordinary pipe-table floors preserve their complete occurrences in open / validly closed / refused states.
The one-screen-earlier rows [`\begin{aligned}`, `\nabla \cdot \mathbf{D} &= \rho_f \\`, `\nabla \cdot \mathbf{B} &= 0`, `\end{aligned}`, `$$`, blank, `8. 连续性方程`, `$$\frac{\partial \rho}{\partial t} + \nabla \cdot`, `\mathbf{J} = 0$$`] own exactly 0..3 and 7..8. Removing row 8 keeps 0..3 and leaves the lower block open. The stray row 4 is source in both states.
A body-only remainder ending in bare `\end{aligned}` is unclaimed; the probe emits only the complete wrapped block below it, with no tail sub-range.

**Shared prose bound checked.** `lib.rs:1286` is literally `source.lines().any(body_line_looks_like_prose)`; its predicate is identical to main. `structural_line_step` at 1425 calls the WHOLE-body helper on each logical row, so embedded newlines cannot hide a prose sub-line behind a backslash on another sub-line.
Differential probes cover paired bodies across dollars, brackets and environments in frozen and live scans, plus body-carrying opener/closer rows, punctuation, spaces, tabs, CRLF and embedded newlines. No main-accepted occurrence was lost; exact render source and endpoints were preserved.
Blank/whitespace rows, bare `42`, `=`, algebra and `\text{some words}` are not prose. [`$$`, blank, `x=y`, `$$`] remains a valid block. An aligned environment containing `\text{some words}` remains accepted, as does command-carried CJK.
Bare CJK or `x=中文` is prose on both main and target; a command on a different embedded-newline sub-line does not exempt it. Independently split `alpha` / `beta` stays non-prose, while `alpha beta` is prose. These are existing decisions, not new refusals.
The passing phantom fixture (`lib.rs:6296`) explicitly asserts `PhantomOpenerAbandoned` with a blank row below the phantom, zero orphans and both clean blocks; empty-body recovery still belongs to resync. Empty and over-length validation remains at closure.
The 40-link refusal-chain and generated walker/scanner equivalence tests pass. Inspection confirms no rewind: `ProseBody` abandons once, charges `GuardRejectedBody` to the opener, and reads the current row normally.

**Stray delimiter limit accepted.** DESIGN:106 explicitly leaves the lone `$$` as source when no scroll history proves its partner. Under the revised rule, the complete environment above it is independently proven; it is not evidence of a PROVEN enclosing dollars block. The oracle's previously-proven clipping/preservation and rewritten-source cases pass; no mismatched-picture regression reproduced. No application or visual replay was launched.
**Recorded, non-blocking:** `lib.rs:3340` still describes the removed environment exception; CHANGELOG:11-25 and DESIGN:104 overgeneralize that every visible remainder stays source, despite the independently complete environment above. Align this wording with DESIGN:106 and the passing one-screen-earlier fixture.
**Recorded, cost:** table claiming now builds one endpoint map and fills each occurrence range (`lib.rs:2184`), removing the former all-rows-per-occurrence loop. Prefix occurrence scans, allocations and ordered-map/set lookups remain; this is not an allocation-free helper. Geometry-dependent wrapped tables absent from the prefix scan are still not floors. No lifecycle/allocation benchmark was authorized or run; the supplied 1,013 -> 1,018 count was not independently remeasured, and budget files are untouched.

Validation: `cargo test -p bt-detect -j 4` passed 162 unit tests, the existing integration test, and four temporary differential probe groups; `cargo test -p bt-term --test repaint_flash_oracle -j 4` passed 32; `cargo test -p bt-term --lib detection -j 4` passed 11 (494 filtered).
The first probe attempt failed to compile because main uses the older clip-helper API; the harness adapter was corrected and the full detector command rerun successfully. No product test failed.
Report written early on `docs/clip-review-3` from `6605f7d7`; this LF report is the only committed change. Temporary probes removed. Only the three authorized cargo commands used; no application launched and no process terminated.
