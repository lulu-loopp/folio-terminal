STATUS: IN PROGRESS — phases 1–3 complete; refusals, gate model, open questions and final verification next.

# R-BT-APP-SPLIT adversarial review

Scope: documentation review only, 2026-09-15, worktree `D:/Developer/bt-wt/bt-app-split-plan`, branch `docs/bt-app-split-plan`. No code, plan, or inventory edits; no build/check/test commands.

## Phase 1 — independent dependency reconstruction

Evidence: `python target/review_graph.py`, outputs `target/review-graph.json` and `target/review-graph-output.txt`. Tree-sitter parses all 102 Rust source files with zero parse-error flags; NetworkX computes SCCs. Comments and literals are blanked with line positions retained. Literal `#[cfg(test)]` item bodies are excluded; other target cfg branches are unioned, not evaluated. Macro token trees are scanned for qualified paths. The comparison graph folds `attention_words/` into one physical node and excludes `main.rs`, matching the inventory's 99-node convention. Its line weights use newline count plus one, matching the inventory's off-by-one convention.

The pattern that reproduces the inventory is `crate::(\w+)\b`, including `use crate::foo;`, **not** the stated `crate::foo::` pattern. The latter produces 237 edges and a 42-node SCC; the former produces exactly 242 edges, the stated 43-node SCC and 232,495 lines. The original 33-node/24,572-line set also reproduces.

For this comparison, “extractable” means avoiding the largest SCC; it permits smaller closed SCCs to leave together. It does not mean every listed module can leave individually.

| Cumulative cut (inventory §5.1 order) | Independent avoid-largest set | Independent largest SCC | Inventory |
| --- | --- | --- | --- |
| None | 33 / 24,572 | 43 / 232,495 | agrees |
| `i18n` sink | 51 / 66,273 | 33 / 196,657 | agrees |
| plus `quit -> webhost`, `attention -> attention_wire` | 57 / 79,353 | 25 / 175,842 | agrees |
| plus `preview` sink | 68 / 108,648 | 19 / 157,843 | aggregates agree; **9 outgoing edges**, not 8 |
| plus `seats` sink instead of `preview` | **71 / 163,070** | **16 / 99,609** | says 68 / 156,724; 19 / 106,524 |
| plus `seats`, `settings`, `preview`, `profiles` sinks | **89 / 252,387** | **5 / 25,158** | says 81 / 181,850; 1 / 51,303 |

The last row still has `{cmdrail, git_graph, git_panel, search, tooltip}`, `{preview_edit, preview_text, preview_undo}`, and `{icons, marks}` cycles. Treating all cycles as blockers instead gives 74 / 220,560; neither definition yields the inventory's final row. “`seats` is the last module standing” is not supported by that experiment.

### Findings (numbering continues in later phases)

1. **blocking — The matching headline graph is insufficient to authorize Step 3.** The resolved source graph adds grouped imports, `super::` paths, and root-owned items. `preview_wrap.rs:3` imports root-owned `PreviewDocument`, `MarkdownBlockLayout`, `WrapMeasure`, functions, **and** the `preview`/`seats` modules. `preview_viewport.rs:2` uses `crate::*`. `marks.rs:1703` takes `crate::StatusDot`. These dependencies cannot be satisfied by a library depending on the existing binary. A 43-node SCC forbids separating its current edges among mutually dependent crates; it does not forbid item-level cuts, nor prove everything outside it is ready. **Correction:** retain a reproducible graph with root-item ownership, explicit import/re-export resolution, test topology and a per-crate API/cut manifest; withdraw the ready-to-extract interpretation of 66,273 lines.

2. **should-fix — The later measured cut rows do not reproduce.** See the exact command and table above. **Correction:** specify cumulative deletions as edge lists and regenerate all rows from a retained script; distinguish largest-SCC avoidance from acyclic single-module extraction and grouped-SCC extraction.

3. **blocking — Test files are being sold as independent production surface.** `main.rs:97` cfg-gates `preview_typing`; `main.rs:121` cfg-gates `source_pin`; `focus_thumb.rs:4120` attaches `focus_thumb_restore_tests.rs` to its parent and that file begins `use super::*;`. `preview_viewport_tests.rs:1` likewise imports its parent's private scope. Inventory §5 calls these production/independent nodes. **Correction:** propagate cfg and module ownership from declarations before estimating production mass or selecting crates; preserve each test's parent and fixture dependencies.

The syntax-only variants in the saved output are supplementary source approximations, not rustc resolution: imports are expanded recursively, root aliases to external crates resolve outward, relative paths follow inline/module ancestry, and wildcard imports conservatively retain their owning scope. No macros are expanded and target cfg branches are not type-checked. Concrete blockers above do not depend on the approximations.

Evidence captured so far: `git status --short --branch` returned only `## docs/bt-app-split-plan` before this review was created. The inventory §8 says its scanners are not retained in the tree. Independent reproduction will retain its script and outputs under `target/`.

## Phase 2 — measurement checks

Evidence: `python target/review_stats.py > target/review-stats-output.txt`; `cargo metadata --no-deps --format-version 1 --offline --locked` (saved as `target/review-metadata.json`). Metadata confirms 16 members, no library target, one `folio` bin, three examples, one harness-free integration target and one build script. `git diff 76ca0788 HEAD --stat` lists only the two reviewed documents: the source snapshot matches the inventory's stated source revision.

| Quantity | Independent result | Assessment |
| --- | --- | --- |
| All Rust under `crates/bt-app` | 456,556 physical lines | agrees |
| All first-party Rust under `crates/` | 678,745 | bt-app is **67.3%**, not 65.6%; the latter uses a vendor-inclusive denominator |
| `src/` | 102 files / 455,336 physical lines | inventory 455,438 adds one line per file |
| `main.rs` | 165,815 physical lines | inventory 165,816 uses newline count + 1 |
| Two inherent `Runtime` impls | 63,487 lines; 1,310 methods; 48,879 body lines | agrees; spans 36598–61746 and 62188–100525 |
| Root struct fields | Runtime 2; App 78; WindowRuntime 245 | agrees; definitions at main.rs:13938, :11015, :11826 |
| Syntactic `#[test]` attributes in `src/` | **3,809**; main.rs raw count **1,043** | not 3,749; source counts are not a platform-specific libtest census |
| `include_str!("main.rs")` | 91 AST macro invocations: 53 inside functions, 38 outside | not 91 independent tests; a shared SOURCE can serve several tests |
| cfg(test) item-line union, with inherited external-module cfg | 197,346 lines (43.3% of src); main 59,903 | broad 43% agrees; exact 197,157 / 60,059 does not under this stated counting rule |
| Direct tabs references in Runtime methods | 233 with contiguous `self.window.tabs`; **255** allowing whitespace | the published count is a lexical lower bound |

The cfg count includes item declarations/bodies and their internal comments, excludes preceding attribute/doc lines, counts overlapping item spans once, and includes entire externally declared test-only files. For example the independent test-line counts are i18n 2,278, seats 29,445, settings 14,216, profiles 11,135, preview 6,102. These are source extents, not compile-cost estimates. The code-line count after literal/comment blanking is 263,947; the remaining lines include **blank lines and literals**, so “42% is prose” (plan §10) is not an accurate description of the complement.

4. **should-fix — Correct the counting units before using them as acceptance criteria or savings inputs.** The table above gives reproducible disagreements. Inventory §0 does not retain the ordered theme regexes, so its precise “23 themes” and single-theme ownership table cannot be independently reproduced. `target/review_stats.py` finds **242** window fields and **78** app fields by whitespace-aware direct access, versus 239/72. **Correction:** retain the classification rules and method-to-theme map; report physical lines separately from source spans, test declarations separately from actual registered tests, and lexical field access separately from read/write/ownership analysis.

5. **blocking for Step 4 — Two fields do not make Runtime a narrow facade.** It grants mutable access to both hubs, implements `Deref<Target = TabState>` and `DerefMut` (main.rs:16477, :16485), and therefore also exposes the active tab through implicit dereference. `answer_attention` destructures `WindowRuntime` and mutates `tabs[index]` (main.rs:92786); this does not appear as `self.window.tabs`. The field regex cannot establish exclusive ownership or distinguish reads from writes. **Correction:** keep the two-field shape fact, withdraw the “one-way” ownership conclusions, and audit dereference, destructuring, aliases and callees before designing subsystem contexts.

Artifact sizes (85.8 / 209.8 / 654.6 MiB and 74.5 GiB target) are **unverified here**. The inventory §6 explicitly measured `D:/Developer/BetterTerminal/target/debug`, outside the authorized worktree. `Get-ChildItem target -Recurse -File -Include *.exe,*.pdb` returned no artifacts in this worktree. No out-of-worktree inspection or compilation was performed. The reported sizes can remain historical observations, not current reproducible evidence or measured link timings.

## Phase 3 — step attacks

### Step 0

6. **should-fix — Make profile experiments reproducible and preserve diagnostic acceptance.** Plan §4 calls changing test debug information “Zero behaviour change” and reduces the risk to losing locals. `main.rs:115786` installs a production panic hook which calls `Backtrace::force_capture`; the robustness child only checks that its log contains `unwrap` (main.rs:116431), not that an application frame resolves to a function and line. `hang_watch.rs:1557` captures offsets through `bt_platform::hang`; its report tests use synthetic frames (`hang_watch.rs:2403`), not a real PDB. Green unit tests therefore do not prove usable diagnostics. **Correction:** baseline the actual command sequence with explicit cold dependency, warm no-edit, and app-only-edit cases; record target, toolchain, effective flags, jobs, feature set, peak rustc/linker memory, compiler/link durations and suite runtime. Trial debug info, optimization and linker separately. Require a real contained panic log with a resolved app frame and a matching-image/PDB hang sample, not just a successful harness exit.

   `line-tables-only` is supported by the pinned minimum Rust 1.89 and is intended to retain filename/line backtraces. It removes debugger information; it is not an opt-level change. Test `opt-level = 1` is workspace-wide (`Cargo.toml:206`), so trialling zero changes dependency runtime too. The child at `main.rs:116321` has a 60-second process watchdog, 30-second font warm-up, and 1/5/10-second request budgets; a slower unoptimized run can trip those without changing functional output. Preserve these checks and measure headroom on the Windows CI runner. [Cargo profile settings](https://doc.rust-lang.org/cargo/reference/profiles.html).

7. **should-fix — “rust-lld on the test profile” is not a concrete stable-Cargo configuration.** Cargo config selects a linker by target, not through a `[profile.test] linker` key. A persistent Windows target override also reaches Windows release builds; a trial must instead scope the override to its command environment and record the MSVC driver/flavor. The existing `.cargo/config.toml:39` carries `target-feature=+crt-static`; replacing `RUSTFLAGS` can silently discard it. Linux CI intentionally overrides it with `-crt-static` (`.github/workflows/ci.yml:624`). **Correction:** specify the trial invocation and flag composition before dispatch, preserve static-CRT behavior and resources, record PDB identity and symbol resolution, and assess Windows build scripts/native libraries as well as the harness. Do not apply MSVC linker configuration to macOS/Linux. [Cargo linker configuration](https://doc.rust-lang.org/cargo/reference/config.html#targettriplelinker), [LLD Windows driver and PDB support](https://lld.llvm.org/windows_support.html).

### Step 1

8. **should-fix — The three edges are real; the ticket's call-site and verification claims are wrong.** The only intra-app targets in production i18n are `marks`, `profiles`, `preview`, at the cited lines. There is no hidden fourth intra-app target in the resolved qualified-path scan. But `colour_name` is an **eight-arm** match (i18n.rs:6359), and its only caller is **settings.rs:4083**, not profiles. `profile_entry_fault` is called by **main.rs:36750 and :51453**, and by a profiles test at :23009. Thus “four files, none of them main.rs” and “conflicts with nothing in flight” (plan §5) are false. `picture_shown_at` contains the third edge; it is not itself a type-dependent function to move wholesale.

   **Correction:** list all five current files (`i18n`, `settings`, `profiles`, `preview`, `main`) and pick the destination before dispatch. Move the enum-to-Text mapping toward the picker/type owner; move fault dispatch into profiles while preserving both languages, punctuation, id interpolation, and the timing of `current()`. Prefer one shared pixel formatter with re-export/call-site updates; preserve exactly the spaces and U+00D7 in `preview.rs:4238`. Do not change any Text variant or public string. Compare named formatter outputs and full string tables for English/Chinese, then run the actual receiving-module and app call-site tests. The stated `cargo test ... -- i18n` runs a filtered suite; it neither lists tests nor covers the profiles caller test. A moved test changes its qualified path, so an identical i18n-filter result list is not a viable acceptance rule.

   The relevant table checks are inside i18n, including test-only tables around :5530 and the language coverage tests; `rg` over `scripts` found no dedicated i18n-table script. One existing guard explicitly admits it is ineffective: `no_profile_title_has_been_pulled_into_the_language_table` at i18n.rs:8848 stops at the first `#[cfg(test)]`, near the beginning, and never sees the table. Record this known baseline gap and use an effective comparison for this move; do not treat that green guard as evidence. Also qualify “depends on nothing”: i18n still formats `bt_term::InlineImageDecodeError` (:6579) and `BackgroundImageError` (:6695). The cut makes it an **intra-app sink**, not a dependency-free or wholly type-agnostic library.

### Step 2

9. **blocking — The proposed module split requires visibility changes and a larger retained root.** Root-private types and fields are accessible from descendants, but private inherent methods moved into one child module are not accessible from its parent or sibling modules. For example `open_search` (main.rs:42046) calls `refresh_chrome` (:39271); their proposed themes differ. **Correction:** explicitly permit and review the minimum `pub(crate)`/restricted visibility on methods called outside their defining module, preserve narrow helper visibility, and place tests under the production module when they require private child items. `use super::*` on a sibling test module does not restore access to its sibling's private definitions. [Rust privacy rules](https://doc.rust-lang.org/reference/visibility-and-privacy.html).

   Plan §6.1 also says 2a leaves roughly 40,000 lines while tests have not moved. Its own numbers imply **165,816 − 63,487 = 102,329**, before module/import additions. Roughly 40k is a possible post-2b scale, not a 2a result. Free helpers, static/const declarations, trait impls, and shared fixtures also require explicit destinations; moving only the two inherent impl blocks cannot produce the depicted root. `main.rs:16477` alone supplies a trait impl outside both blocks. Amend the relocation manifest rather than leaving these as accidental leftovers.

10. **blocking — Test names and source coverage can silently disappear while every proposed comparison stays green.** The child at `main.rs:116325` executes `current_exe()` with the hard-coded exact path `tests::hostile_math_is_refused_and_the_real_decoration_worker_survives`. 2b changes that path. A zero-match child can return success, and the parent checks only its exit status. The top-level test still appears in `--list` under its new name. Separately, `the_shell_page_is_gone` (main.rs:161409) scans `src` with nonrecursive `read_dir`; adding `runtime/` silently removes moved code from this whole-program guard, outside the set of 91 include-string sites.

   **Correction:** inventory every source-reading test and every exact-path consumer, not just `include_str!("main.rs")`. Use a one-to-one mapping of old target/full test identity to new identity, including cfg/ignored status and multiplicity; leaf-name sets alone do not prove preserved coverage. Update the child selector and require proof the child body ran (an explicit completion marker or verified one-test census). Preserve root module paths with `#[path]` where a whole test module can remain at its old logical location. Multi-file guards must enumerate the complete declared source set or recursively scan the intended roots, fail on missing/empty inputs, and demonstrate that a violation in a newly added file is caught.

11. **should-fix — The mechanical acceptance rules cannot establish zero behavior change as written.** A byte-identical `--list` is useful for 2a's test topology but cannot detect changed method behavior; plan §6.3 itself concedes that. Sorting source lines loses control-flow order, so swapping two effects passes its proposed multiset check. It also rejects necessary visibility, source-pin and indentation changes. `source_pin::source_region` counts braces in raw text (`source_pin.rs:24`); `code_of` explicitly excludes raw-string/escaped-quote support (:49). Those helpers cannot automatically handle arbitrary newly assigned subjects.

   **Correction:** compare each relocated item's parsed token body in order, with a reviewed allow-list for path/visibility changes, and retain the full behavioral gate per landing. Classify pins by their actual subject and validate the parser/helper against that subject; do not blindly migrate all 91. Run rustfmt over the intended new module tree and review formatting separately; moving indentation and imports can change output. Reassess cfg-scoped imports, inherited lint attributes, dead-code exposure and clippy results on both supported app platforms. The root platform allow-list must gain relevant `runtime/...` paths: `check-portable-core.ps1:248` scans recursively and checks the **relative file name**, so merely keeping the array in main.rs is insufficient.

   Correct the platform claim: macOS CI checks bt-app all targets at `ci.yml:517`; Linux explicitly excludes bt-app at :644 and lists portable crates at :649. Add new portable crates deliberately to CI/package allow-lists. The existing Linux job does not validate an app split. Also scan new libraries for doctests, newly public docs, intra-doc links and package-relative fixture paths; libraries acquire doctest coverage that the current bin does not have. [Cargo test target and doctest behavior](https://doc.rust-lang.org/cargo/commands/cargo-test.html).

12. **should-fix — Freeze affected work at a recorded commit; do not assert conflict-free extraction or rollback.** Plan §6.5 derives a 65% branch-conflict probability from 98/150 historical commits, which is not a probability model for live branches. The in-tree handoff (`docs/handoff/HANDOFF-2026-08-21.md:216`, the dated running handoff entry) records 0.4.1 math UI, input/TSF and card work; these overlap the proposed themes. Step 1 also edits main and settings (finding 8), and Step 3 needs caller edits beyond `mod`/`use` lines (table below).

   **Correction:** inventory actual touching branches at dispatch, record the source commit and ownership window, and sequence one reviewed theme per commit under a bounded freeze of those files. Rebase each affected feature once on the completed split; rerun its tests. At a 60–90 minute reported gate, 26 independently gated landings alone cost 26–39 hours, so prefer review-sized commits within a bounded landing batch where policy permits. A later revert can conflict with subsequent feature edits even if the original patch was a pure move. Revert the exact lockfile delta; `cargo update -w` is not a guarantee of restoring the original dependency resolution.

### Step 3 — all sixteen rows

13. **blocking — The advertised extraction order and “leaf model” API description are not implementable as given.** Evidence below comes from `target/review-stats-output.txt`, which lists each public declaration with its source line, and the resolved dependency scan. “Correction” means a prerequisite design/cut, not permission to change behavior during relocation.

| Row / proposed crate | Actual interface/state crossing, evidence | Concrete correction / row verdict |
| --- | --- | --- |
| 1 `bt-i18n` | `Lang`, `LanguageMode`, `Text`, formatters; process-global language/revision (`i18n.rs:207`, :249), external bt-term error types (:6579) | Preserve one CURRENT instance and exact strings; finish the three cuts first. **Go with changes.** Step 1's cut and this crate extraction are separate commits, despite row 1 saying they are the same step. |
| 2 `bt-web-nav` | URL decisions/mints (`webnav.rs:109`, :127), favicon/raster caches (`favicon.rs:183`), `WebThumbs` keyed by root `LeafId` (`web_thumb.rs:89`, :359), async shrinker (:743); `web_trace.rs:87` imports `trace` | Extract the pure URL/favicon portion first or define shared identity ownership; move trace first if included. Row 6 is a predecessor of row 2. **Rethink grouping.** |
| 3 `bt-preview-text` | Layout/measure interfaces and app-owned document types (`preview_wrap.rs:3`, :523); `table_block.rs:52` returns `seats::PreviewMarkdownMetrics`; **`preview_viewport.rs:994` implements Runtime** | Keep Runtime adapters in bt-app; design owned document/geometry types first. An external crate cannot define this inherent impl ([E0116](https://doc.rust-lang.org/stable/error_codes/E0116.html)). Grouped imports reach the large SCC. Only small helpers such as linebreak/hex are immediate candidates. **Rethink.** |
| 4 `bt-anim` | Decoder/cursor/ring state (`animation.rs:352`, :527, :967); settling imports root `RevealTween` (`settling.rs:58`) | Extract animation alone or move the shared tween with its callers; preserve decoder budgets, clock inputs and reduced-motion behavior. **Go with changes.** |
| 5 `bt-watch` | Live subscriptions/debounce and `EventLoopProxy<AppEvent>` in PreviewWatch::sync (:228), FilesWatch::sync (:106), GitWatch::sync (:99), DirNews::arm (:79) | Define a wake callback/event adapter and retain event variant, coalescing, drop/cancel and deadline behavior. AppEvent is root-owned (main.rs:302). These are lifecycle services, not pure leaf functions. **Rethink until cut specified.** |
| 6 `bt-trace` | Trace/Gate globals and lazy closures (`trace.rs:123`, :152); card diagnostics use root `TabId` (`card_trace.rs:67`); source_pin is currently test-only | Extract generic trace core first; retain card adapter in app or explicitly relocate identity ownership. Put source-pin support in a dev-only dependency/module. **Go with changes.** |
| 7 `bt-keys` | winit key/button/modifier inputs, HostPlatform, protocol byte outputs (`input.rs:160`, :390, :530); APIs currently pub(crate) | Publish only the used translators and enums; preserve platform-specific byte tables. **Go with changes.** |
| 8 `bt-pdf` | Path/page/fit dimensions -> owned raster/page count (`pdf.rs:142`, :218, :247); hayro and filesystem errors | Specify pixel/alpha/error/size behavior and retain pinned hayro features and fixture paths. **Go.** |
| 9 `bt-file-index` | Bounded walk plus `IndexWorker::spawn(EventLoopProxy<AppEvent>)` (`palette_index.rs:202`, :403); requests/responses carry epochs (:378) | Extract walker/model first or define a wake callback without changing stale-response and shutdown handling. **Go with changes.** |
| 10 `bt-formula-tools` | Marks/sprites/MarkSlot/tooltip imported via grouped `use crate::{...}` (`formula_tools.rs:43`); layout/follow model depends on those visual types | Not the graph's independent exception. Cut or move its visual adapters; row 14 and tooltip are dependencies. **Rethink claimed position.** |
| 11 into `bt-platform` | wsl bridge and static app-delegate inbox (`app_delegate_wire.rs:32`, :41) | Preserve queue instance/order and host cfg; audit platform dependency direction and callers. **Go with changes**; this is not a new crate. |
| 12 `bt-text-field` | Single-line editor state with editing operations (`text_field.rs`, public declaration census in saved output) | Explicitly list editor methods/selection units and preserve Unicode tests. **Go.** |
| 13 `bt-shortcuts` | Binding/action/chord table and overrides, i18n text (`shortcuts.rs:42`, :2593), docs generator rooted at manifest (:6551) | After i18n, publish the used binding API and retarget both shortcut scripts, which hard-code `-p bt-app --bin folio --exact shortcuts::tests::...` (`check-shortcuts-table.ps1:31`). Prove one generator test ran. **Go with changes.** |
| 14 `bt-marks` | Marks/icons cycle (`icons.rs:37`, marks' icon registry); favicon handles plus root `StatusDot` argument (`marks.rs:1703`) | `{icons, marks}` is real; **there is no shortcuts/marks two-cycle** in production. `shortcuts` only targets i18n. Move/adapt StatusDot and settle favicon identity first. **Go with changes**, not immediately after i18n. |
| 15 `bt-persist-app` | Stores/leases and write debounce (`persist.rs:198`, :408); global scheme catalogue (:schemes.rs:431); watchdog/diagnostics read retained `version`; hang_watch depends on row-16 seed; watchers require AppEvent | Pass app-supplied build identity, move generic seed primitives first, and cut wake adapters. `version.rs:40` also uses build-script `FOLIO_COMMIT`, so workspace package version inheritance alone is insufficient. Separate watchdog lifecycle from persistence unless an explicit ownership reason joins them. **Rethink.** |
| 16 “small” | update depends on retained version; seed -> i18n; menubar -> APP_NAME/shortcuts; quake -> shortcuts; arrival -> marks/root tween; recent_folders -> seed; focus_thumb tests -> parent private scope | These are multiple placements, not one independent layer. Name exact bundles; keep app adapters/tests with their owners and move prerequisites before consumers. **Rethink unspecified grouping.** |

The sixteen row sums reproduce **65,967** only under the inventory's file-line convention. That does not establish an extractable set: the rows include blocked adapters and exclude necessary root definitions. `66,273 / 456,556 = 14.516%` reproduces the arithmetic, not a time estimate. Rows 2–12 have actual cross-row dependencies and cannot all build independently. The list also contains an existing-crate move and unspecified bundles, so “sixteen more crates” is not an established target count.

### Step 4

14. **blocking — Services/Turn/Intent is a design prompt, not a behavior-preserving boundary yet.** Plan §8 labels it a sketch, but §3.2 claims its seven candidates are already one-way. The code below disproves that for the named themes. Services lacks GPU/worker/notification/window capabilities used by those paths, a shared `&Services` cannot generally hand out mutable renderer access, and the shown `Turn<'a>` has no field using `'a`. Those are sketch incompletenesses; the material problem is that deferred intents have no identity, ordering, error or publication contract.

| Claimed pluggable subsystem | Existing behavior the boundary must preserve |
| --- | --- |
| attention/notifications | `answer_attention` mutates a tab's ledger through destructuring (main.rs:92786); `mark_attention_seen` mutates every leaf (:92814); `raise_attention` calls app notification services (:81853). Separate ledger decisions, delivery and app mutation; a toast-only adapter is a smaller honest seam. |
| search | `seat_can_search` reads sessions through Runtime's Deref (:41989); `refresh_search` branches between transcript and WebView, with deliberate keyboard-focus behavior (:42172). Export a stable search snapshot/result and typed apply operations; a generic scroll intent is insufficient. |
| first run/integration | `answer_first_run` mutates card state, applies settings and closes/rebuilds UI synchronously (:52351); `apply_first_run` funnels through settings (:52414). Separate card model from installation side effects and persistence; preserve cancel/error order. |
| quake/summon | `show_quake_window` reads app settings/quake state and monitor DPI, places and presents a real window (:38061). Keep placement policy separate from re-entrant OS/window effects. A no-op test double cannot certify summon behavior. |
| math/formula | `apply_math_results` routes by owner/LeafId and mutates frozen/live sessions (:81271); stale/closed panes are deliberately ignored. A worker mailbox is a real seam, but it does not make the application half read-only. Preserve generation/owner validation and application order. |
| command palette | `run_palette_row` closes the palette before dispatch, then resolves stable tab/seat IDs against current state (:93538). Re-resolving at execution is required; batch intents must not hold stale ordinals or reverse close/open order. |
| diagnostics/trace | Closure-based lazy emission already exists (`trace.rs:152`); production panic/hang handlers have mandatory behavior (main.rs:115786, hang_watch.rs:1386). Stub optional trace sinks separately from crash reporting and watchdog lifecycle. |

   **Correction:** dispatch a separate design ticket for one bounded candidate after a read/write/call audit. Specify immutable snapshots with stable TabId/LeafId and generations; typed outputs; exact synchronous application points; rejected/stale result handling; cancellation/drop; error propagation; redraw/deadline obligations; and shared resource borrowing. `turn` currently requires wheel flush, deferred-DPI reconciliation, result application and PTY drain in that order (main.rs:99848); draining a Vec<Intent> later would change what subsequent work sees. Record trace-based before/after event sequences and prove a no-op/replacement instance for the chosen seam before promising seven pluggable subsystems. Enforce the boundary through types/module ownership; grepping only for `WindowRuntime` misses aliases, Runtime itself, Deref and callbacks closing over it.
