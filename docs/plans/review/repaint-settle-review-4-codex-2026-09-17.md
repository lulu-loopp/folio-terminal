# Repaint settle review, round 4

Verdict: **HOLD for 0.4.2** pending one P2 ownership-context fix. Base: e4a6e57b.
Scope: detector ownership, canonical resize install, dependency-stable retries, main merge, and four authorized suites.
Reviewed non-merges eb3bded6, 8657ac1f, 89b9e23f, 7f435462, e4a6e57b and merge 88fff24a against main bb35f1ce.

- Baseline oracle: 23/23 pass; round-3 prefix and environment-punctuation failures CLOSED by tests/repaint_flash_oracle.rs:1373; canonical shrink/shrink-grow failure CLOSED by :927. Streaming alternate arm passes 60 frames / 8 scans / budget 12 (:1521); primary and adjacent-row follow-ups also pass below. Paths below are relative to crates/bt-term unless qualified.
- Canonical mapping is real: src/session.rs:3630 snapshots the gesture-reflowed grid before install (:3648), then :3666 projects via segmented_row_mapping (:6172), unique placement (:6234), and exact source-row proof (:13950), before rebasing. mark_resize_settled_unchanged_at (:3596) calls the same settlement. Primary and alternate snapshot predicates are screen-exclusive; the primary branch (:3674) is preserved.
- Lifecycle 44/44; measured repaint allocations/bytes per cycle: primary 120x40 = 16/21,304; 120x80 = 16/21,504; carried 120x16 = 644/566,946; off-band = 1,017/761,621. Existing ceilings pass unchanged (tests/lifecycle_matrix.rs:2227,2451). These arms do not exercise successful off-band detector restores.
- Main merge sanity: diff bb35f1ce..e4a6e57b touches only CHANGELOG, DESIGN, session, lifecycle_matrix, and oracle (1,833 additions / 37 deletions); adapter/XTVERSION and bt-detect are identical to main. Lifecycle imports are additive; CHANGELOG/DESIGN retain main content. Most XTVERSION tests are excluded by the authorized repaint/resize filters, so preservation does not imply all were executed.
- P2 / ownership gate PARTLY OPEN: detector_owns_live_match uses prefixes from advance_detection_context (src/session.rs:6895,6928,13501), which is not the full scanner's state. Scratch: restore a proven off-band dollar x^2 block onto rows [unfinished pmatrix opener, dollar z^2 block, opening code fence, code, dollar x^2 block, closing fence]. Full ledger disowns x^2, but the immediate frame paints it. crates/bt-detect/src/lib.rs:1603-1610 abandons the unfinished environment at z^2; advance_detection_context (:1327-1369) does not, so it misses the fence and the cheap fence gate (src/session.rs:6946) also fails. Share the scanner's recovered prefix state, then assert no picture/no HeldUnbacked here.
- Additional attacks pass: ordinary adjacent blocks, wrapped source beside adjacent blocks, clipped-prefix recovery, combined bullet/heading, two-space indent and styled blanks; spinner below a refused block recovers on both screens, and an older refused scan does not re-arm a newer resident. Primary streaming is also 8 scans/60 frames, ceiling 12. Unchanged-size shrink/grow settlement retains both pictures before ESU.
- Confirmed P2 publication: immediate rendered source is x^2 with one HeldUnbacked at grid row 4; after stability the unchanged grid renders z^2 instead and removes x^2. Final scratch oracle suite 31/32, only this context mismatch fails. Corrected diagnostic-source assertions and an overwritten-fence fixture were test-author errors, not product findings.
- Deliberate parity drop is appropriate for the fully present, truly detector-disowned reprint: scratch reproduces the new empty display/empty HeldUnbacked and recovers y=1 after removing the poison. Do not generalize this to all holds: clipped/projected holds and annotated source damage still need HeldUnbacked (src/session.rs:6107; src/bin/bt-repaint-oracle.rs:1368-1394). The diagnostic is observational, not a production recovery trigger; the ownership ledger must still expose detector failure when no hold remains.
- Restore cost: extent widening (src/session.rs:6863-6881) stops at logical-line boundaries; adjacent complete lines are not scanned together. Each cloned LiveDetectionInput owns text plus cell_boundaries (crates/bt-detect/src/lib.rs:292-302), followed by resolver allocations; this is more than one heap allocation per row. Prefixes are built once per restore, records capped at 128 (src/session.rs:69), but matching records still each scan their extent. Scratch 100x40 close restoring 1/8/32 off-band formulas onto headings uses 1,889/2,337/3,886 allocations and 1,226,905/1,359,856/1,884,006 bytes; all pictures return immediately. These are whole-feed costs, not isolated new-gate deltas; lifecycle with this arm passes 45/45. No existing budget measures this successful-restore path.
- Retry gate CLOSED: resolved bands are source rows (crates/bt-detect/src/lib.rs:3225; src/session.rs:13137), plus the candidate in live_task_dependency_rows (:14223), not adjacent chrome. Only SourceChanged carries the newest clock (:7581,14203); ordinary damage uses :14173 without copying it. Both-screen streaming/quiescence and neighbouring-spinner tests pass; the resident coverage check (:7579) prevents stale-loser rearming.
- Exact P2 screen (zero-based rows 0..7): `\begin{pmatrix}`, `$$z^2$$`, opening triple-backtick fence, `code`, `$$x^2$$`, closing triple-backtick fence, `tail`, `prompt> `. Seed `$$x^2$$` as a rendered block, repaint it off-screen, then return it on this screen using BSU/HOME/EL/CUP/ESU. This reproduces without a renderer or application launch; the bug is the recovered prefix, not adjacent-line extent widening.
- Canonical gate CLOSED, including shrink/grow and unchanged ending: strengthened assertions pass before worker completions after install, not merely before ESU. Primary's canonical projection remains as on 8a62bf9f; exclusive predicates are src/session.rs:6149 and :6407. Rebase is after both projection branches (:3705); no second projection of primary through alternate occurs.
- Final authorized baseline results: oracle 23/23, lifecycle 44/44, repaint units 13/13, resize units 29/29. Resize includes canonical parser/synchronized-update inheritance, copy-once transaction, partial outer-owner preservation, and XTVERSION-during-resize (src/adapter.rs:3021); all pass. Other XTVERSION tests remain present and unchanged but were not selected by the allowed commands.
- Budget ceilings unchanged: primary 18 allocations / 28 KiB and 16 B per additional row; carried 720 / 620 KiB, off-band 1,140 / 832 KiB. Streaming measured 8/60 on each screen against 12, with eventual recovery. Owner replay evidence (wrapped 12 -> 0 flashes; unwrapped 0) was supplied, not rerun; no claim of replay validation here.
- Final gates: original four round-3 failing cases CLOSED by equivalent reruns; canonical install CLOSED; retry stability CLOSED; detector ownership PARTLY closed, blocked by the proven recovered-context mismatch. Require shared authoritative prefix state plus the fence-after-environment regression before merging.
- Cleanup: all temporary oracle/lifecycle probes removed; restored oracle passes 23/23; product/test diff empty. Only this English LF report is committed on docs/repaint-settle-review-4 directly from e4a6e57b. Only the four authorized cargo commands ran; no application/replay program launched and no process terminated.
