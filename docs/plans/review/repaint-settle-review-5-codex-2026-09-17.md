# Repaint settle review, round 5

Verdict: **HOLD for 0.4.2** pending one P2 closer-precedence fix. Base: 74baff25 (17d674eb + 74baff25 over e4a6e57b).
Scope: shared detector structural step, recovered context, successful-restore allocation budget.

- Report opened before investigation; only the four authorized cargo suites ran. No application launches or process termination.
- Scanner audit: crates/bt-detect/src/lib.rs:1348-1382 extracts the same pure guards; :1602 preserves inline_carry.take(), code skips, fence updates and code ledger recording. Between verdict and :1657 only :1640 clip resync runs; it either changes nothing or clears/records OpenerAboveWindow and continues. EnvironmentSwallowAbandoned stays at :1660, before inline handling (:1806); no ordering/output change found in the raw scanner.
- Radius audit: :1377 is delimiter-triggered, with no max_swallow, age, index, lookahead or line budget. A legitimate align body can stream indefinitely across a seam; the same first eligible Dollars/Brackets opener abandons it in whole scans and prefix walks, regardless of origin or a later environment closer. This fixes carried context at clipped_open_index (:2956), session alternate scroll (:9918), frozen checkpoints (:11291), live_initial_detection_context (:6056), and ownership prefixes (:13475).
- Baseline bt-detect: 151 unit tests plus 1 strand_ground_truth integration test pass (0 failures; 0 doctests). Existing scanner behavior remains pinned by the full permitted suite; scratch parity expansion follows.
- Round-4 gate CLOSED on the accepted align opener: oracle regression crates/bt-term/tests/repaint_flash_oracle.rs:1405 passes immediate no-HeldUnbacked/native row 4 and settled outside-picture/inside-text assertions. The old pmatrix screen was invalid evidence for this mechanism (bt-detect/src/lib.rs:2572,2631). Baseline oracle 24/24 and lifecycle 45/45 pass.
- Baseline budget lines (allocations / bytes per cycle): G1_TUI_REPAINT 120x40 16 / 21,304; 120x80 16 / 21,504; G1_CARRIED_REPAINT 120x16 off_band=false 644 / 566,946; true 1,017 / 761,621; G1_RESTORED_REPAINT 100x40 blocks=1 2,984 / 1,994,690; blocks=8 3,477 / 2,204,756. All reported measurements reproduced exactly.
- P2 / ownership-context gate STILL OPEN: bt-detect/src/lib.rs:1403 returns for every complete one-line display, but clears an active opening only for Dollars. The scanner instead closes an active matching environment at :1835. Scratch [begin-align, begin-align x=y end-align, opening fence, dollar x^2 block, closing fence] diverges at splits 2 and 3: tail detects x^2, whole scan disowns it. Sharing structural_line_step has left delimiter-pairing drift intact; same-class bracket forms also diverge. This is pre-existing pairing drift, not a changed raw scanner result; terminal publication repro follows.
- Scratch expansion: 15 directed corpora (requested fence/list/quote/info/long-close/indent/later-close/two-open/CRLF/trailing-blank cases plus self-contained closers and a 300-body-line environment), and 4,000 deterministic eight-line corpora; 36,396 split checks, 148 mismatches. Old/new raw scanner differential: all 32,120 comparisons equal, including full blocks/source bytes, ambiguous/refused-table outputs, raw ledger and final neutrality; modes cover carried environment/Dollars/fence, ambiguous prefix, live seam, explicit clip, frozen resync, and eligible inline sites.
- Baseline detection units: cargo test -p bt-term --lib detection -j 4 passes 11/11 (494 filtered). Scratch initially hit a UTF-8 writer error/compile failure; source restored from byte backup before correcting the scratch harness. No product finding attributed to that harness error.
- P2 confirmed in terminal, both closed and unclosed fences: seed/off-band $$x^2$$, then repaint rows 0..7 [begin-align, begin-align x=y end-align, ``` , code, $$x^2$$, ``` (or more code), tail, prompt]. Immediate row 4 is blank and ledger reports HeldUnbackedRecord { LiveGrid(4), "$$x^2$$" }; after stability the unchanged row shows its source. Scratch oracle 24 pass / 1 fail (both variants), baseline new align regression still passes. Require matching closer precedence in walker (:1403 versus scanner :1835), with this restore regression and bracket pairing coverage, before 0.4.2.
- Budget assessment: lifecycle_matrix.rs:2486 uses distinct heading sources; :2527 runs 64 leave/return cycles and :2543 checks unchanged detection count plus final pictures. Ceilings :2590-2596 are honest ~11.9% headroom (3,340 / 2,180 KiB; 3,890 / 2,410 KiB). Thread-local heap counters avoid other test threads; repeated seeded measurements are exact. Unseeded same-byte control 1/8 = 32/51 allocations, 30,752/33,856 B; the seeded N delta 493 / 210,066 B therefore includes at least repaint/input scaling (control delta 19 / 3,104), not just the ownership call. It is a useful whole-cycle restore-path budget, not an isolated per-record gate price. Also repaint_rows (:49) writes 5 versus 26 rows, not two full 40-row repaints. Scratch lifecycle 46/46 passes.
- Causality checks: restoring only the old walker makes the corrected round-4 align regression red again (same LiveGrid(4) HeldUnbacked), and retains the new pairing failure: oracle 23 pass / 2 fail. Scratch-only repair lets complete-line handling fall through to matching-closer logic whenever a non-Dollar opening remains; all 36,396 split checks then pass, raw differential remains equal (32,120), and bt-detect is 153 units + 1 integration pass. The repair is diagnostic only and will not be committed.
- Exact remaining P2 row text: `\begin{align}`; `\begin{align}x=y\end{align}`; opening triple-backtick fence; `code`; `$$x^2$$`; closing triple-backtick fence (or `more code`); `tail`; `prompt> `. It reaches session.rs:6895 with the wrong prefix and bypasses the fence gate at :6946. The whole scanner closes the active environment on row 1 even if it rejects its body; the walker must reproduce that transition.
- Final scratch terminal checks: proposed pairing repair passes 25/25, with both fence variants keeping source visible and HeldUnbacked empty. Restoring submitted detector and adding explicit original round-4 closed/open-fence probes yields 25 pass / 1 fail: both corrected round-4 variants pass; only the remaining pairing P2 fails.
- Verdict: HOLD for 0.4.2 on one P2. Original abandon fix is sound; the scanner extraction preserves behavior and budget arm is useful. Must fix the remaining walker/scanner closer-precedence mismatch and retain its detector split and immediate-frame restore regressions. No evidence of an origin-dependent abandonment regression was found.
- Final authorized baseline results: bt-detect 151 units + 1 integration; repaint_flash_oracle 24; lifecycle_matrix 45; detection units 11 (494 filtered), all pass. After removing all scratch edits, bt-detect and oracle rerun clean with those same counts. Allocation ceilings and all six baseline budget lines above pass unchanged.
- Cleanup: all three product/test files restored byte-for-byte; only this English LF report is committed on docs/repaint-settle-review-5 from 74baff25. Only the four authorized cargo command forms ran; no application/replay launched and no process terminated. Scratch failures and the uncommitted diagnostic repair are explicitly separated from baseline results.
