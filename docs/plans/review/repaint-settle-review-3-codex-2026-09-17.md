# Repaint settle review, round 3

Verdict: **HOLD for 0.4.2**. Base: eb3bded6; reviewed bcb514b6, a0a5f2e5, 87836684, eb3bded6.
Scope: resize rebasing, display re-anchor eligibility, refused scans, and budgets. Paths are relative to crates/bt-term unless qualified; references are at eb3bded6.
Validation is restricted to the four authorized cargo test commands.

- Original round-2 probes CLOSED; broader resize gate PARTLY closed as detailed below: oracle 19/19; resize/empty-ESU preserves 2/2 (tests/repaint_flash_oracle.rs:830), pending-worker body edit recovers 1/1 on both screens (1230). Rebase follows projection at src/session.rs:3400; SourceChanged re-arms dependencies at 7386-7388.
- P2: a0a5f2e5 adds a different ownership predicate, not a shared detector rule. src/session.rs:12832-12836 rejects non-whitespace prefixes, but crates/bt-detect/src/lib.rs:2441-2470 explicitly permits bullet/heading prefixes. Scratch off-band restore onto a styled-blank-suffixed `• $$e^{i\pi} + 1 = 0$$` publishes 0 pictures, then 1 after stability; whitespace-only indentation publishes 1 immediately. Reuse the detector's ownership predicate; preserve its supported prefixes and environment punctuation.
- P2: eb3bded6 retry is not bounded by dependency stability. src/session.rs:6976-6981 clears signatures/settled flags without changing damage times; 14100-14103 tests only the candidate row. A pending three-row block whose body changes every 16 ms schedules 120 new candidate scans in 60 frames on primary, with SourceChanged every frame, because unchanged delimiter rows already qualify as stable. Gate retry on the newest dependency damage; retain eventual recovery after quiescence.
- Budgets measured unchanged versus round 2: old 120x40 / 120x80 primary arms = 16 allocations / 21,304 B and 16 / 21,504 B; new 120x16 carried arms = 644 / 566,946 B, with off-band = 1,017 / 761,621 B per cycle (tests/lifecycle_matrix.rs:2080,2213,2306). Lifecycle 43/43; repaint units 13/13. Allocation arms do not cover the streaming retry path.
- Follow-up confirms both findings: all bullet, heading, and combined prefixes publish 0 then 1; an environment with a trailing comma does likewise (detector explicitly permits punctuation at crates/bt-detect/src/lib.rs:2509-2521). Both screens schedule 120 candidate scans in 60 streaming frames and recover after quiescence. Wrapped source, two-space indentation and styled trailing blanks preserve immediately; right-hand border/scrollbar glyphs stay undetected (0 then 0).
- P1: resize gate is only PARTLY closed: canonical install still invalidates alternate coordinates before rebase. src/session.rs:3554-3583 increments generation but projects/restores only primary; 3602 snapshots unreconciled alternate residents. Scratch two-identical-block case, BSU/HOME then 48x14 -> 48x10 -> mark request: pictures 2 -> 0 at reconcile -> 2 at empty ESU. Add a grow back to 48x14 before mark: 2 -> 0 -> 1, with both source blocks present. src/adapter.rs:1163-1165 installs the canonical grid/parser; rebase alone is not projection. Project the alternate residents against that install before re-taking the open window; test the publication before ESU and shrink/grow canonical replacement.
- Buffered old-width writes: four primary/alternate x^2 unchanged/y^2 replacement cases across 48x16 -> 32x12, canonical install, and ESU pass exact-source safety and eventual 1/1 recovery. Old queued relayout completions can be refused normally. The close compares the rebased reflowed grid to the actual committed grid; project_live_record checks current source rows (src/session.rs:13734-13798), and exact restore checks source bytes. This does not excuse the alternate canonical-install gap above.
- Overlapping pending scans can re-arm a newer accepted answer: a second context-triggered scan is completed after a body edit, then an older scan returns SourceChanged and clears its signatures again. Scratch passes on both screens: extra work occurs once, then stops on unchanged cells; completions do not mutate source, so no autonomous mutual-refusal loop. src/session.rs:7055-7065 deduplicates queued candidates only, not in-flight work; streaming retries remain limited by worker/event cadence rather than dependency stability.
- Other mutation paths: font/zoom-only invalidate_layout (src/session.rs:11262-11340) changes artifacts/tasks, not terminal cells; any actual dimension change routes through resize_at. Selection changes document anchors only (7999-8005), reader wheel changes ViewportProjection, not the live grid; neither is producer damage. App-side clear-scrollback calls session.feed(ED3) (crates/bt-app/src/main.rs:76141-76151), so it uses normal feed/buffering/settlement, and Saved clear only removes history (vendor/alacritty_terminal/src/term/mod.rs:2887-2902). Park/restore are terminal events mapped by src/lifecycle.rs:187-188 and applied inside feed (src/session.rs:3001,9797-9830); residents/tasks/row state reset, finish_alternate_repaint checks the active screen (6063), so these are not unobserved out-of-feed grid moves.
- Parent comparison at bcb514b6 confirms the committed resize and pending-worker regressions are red there; prefix re-anchors all pass there, and the environment-comma control is 1 immediately / 1 settled. Border/scrollbar rows formerly get an incorrect transient 1, then 0 after detection; current is 0/0. The detector file is identical to main e0f682b8 (`git diff` empty), so main's scanner did not typeset those bordered rows either; 0.4.3 border support is not lost here. Owner replay numbers are supplied evidence, not rerun in this review.
- Old-width overwrite safety also passes on both screens: buffer a 48-column repaint with a 35-plus-character body, narrow to 32, install canonical, then ESU; its old CUP for the closer overwrites the newly wrapped body tail. The old full-source raster is not published and no unbacked resident remains. Exact matching rejects the wrong seat even though the buffered bytes were authored for the previous width.
- Validation at eb3bded6: authorized baseline oracle 19/19, lifecycle 43/43, repaint units 13/13, resize units 28/28. Seven temporary oracle probes produce 22/26 passing: four failing tests substantiate three findings (prefix/punctuation share one root cause); buffered-resize safety and finite overlapping-scan controls pass. Tests use synthetic rasters; no application/replay/renderer executable was launched and no process was terminated.

Merge gates: project/re-seat alternate residents at canonical install before rebasing; share detector ownership for restore; make SourceChanged retries wait for dependency stability without stranding quiet blocks.
Cleanup: scratch oracle additions and parent worktree removed; restored oracle passes 19/19. Product/test diff is empty. Only this LF report is committed on docs/repaint-settle-review-3, directly from eb3bded6.
