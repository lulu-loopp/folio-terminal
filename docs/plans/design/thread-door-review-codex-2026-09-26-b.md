**adopt with changes**

Static review on `design/thread-door` at `80833cc7`, against `78a3699a`; the branch changes only the three design/review documents. I read revision (c), the earlier sections and revision (b) it overrides, the prior Codex obligations P1–P8 and §7/§12 answers, the GLM review, the budget note's admission/accounting contracts and owner rulings, and the relevant ARCHITECTURE, RULES and CONVENTIONS provisions. Source inspection covered the heartbeat and detail ledger, per-leaf resize, quit producer/driver, session and trace retirement, endpoint storage/destructors, PTY transport waits, standalone callers, callback registrations, hand-off control and source-reader interface. No build, tests, compiler probes or mutations were run. Only this file is written; no commit is made. Counterexamples below concern the proposed contract, not an implemented admission system.

## P1 — not met

(c)1 says “The worker half of M4i′ moves to A1b” and “A1d has no A1b prerequisite.” The main dependency repairs, A3 landing after all A1, spawn-failure preservation and unchanged D-2 closure are sound. However, A1a's “M4a–M4h” still includes M4g, whose retained §9.3 sequence requires `spawn_at_priority` to produce `Worker`, supplied by A1b. (c)8 also leaves owner-side M5's worker execution in A1d without naming whether it uses A1b's spawner or the new standalone entry. **One change:** make the test allocation explicit: assign every spawn-produced-worker arm to A1b, excluding M4g from A1a and moving the worker-refusal arm of owner M5 there; retain the Window control in A1d. This preserves the stated dependency graph without an implicit producer substitution.

## P2 — not met

(c)2 says “`enter` returns `Cookie::OVERFLOW` and pushes nothing, `leave` restores nothing for it,” while requiring that “the 17th nested call counts an overflow and restores its parent correctly.” These conflict. `hang_watch::enter` calls `Heartbeat::enter_at`, changing station, current node and scope; neither the cookie nor the adapter retains that call's returned `Location`. After call 17 returns, call 16's remaining work is attributed to the wrong context until a later restoration. `during_pane` and the detail ledger's `ROOT` overflow do not recover the discarded context. The four-clock-read correction, footer writer and unwind policy otherwise meet P2. **One change:** carry the complete saved `Location` as an opaque, fixed-size numeric cookie payload instead of using the bounded adapter stack, and restore it on normal leave, including when the detail ledger is full.

## P3 — not met

(c)3 says “A1d converts exactly this table,” but several rows still say “per door type” and “as today”; the native row relies on “their existing single call sites.” There are multiple visibility sites (`put_the_window_on_the_glass`, `hide_quake_window`, `let_go_of_this_window`) and two cursor calls inside `apply_pointer_cursor`. The `Present` row also names several token-consuming functions while promising one present-and-commit admission, alongside a separate `CompositorCommit` admission; their token boundaries remain unspecified. The PTY resize placement itself is correct: `commit_leaf_resize` reflows, calls `pty.resize` with `?`, then reconciles, so mapping refusal at that statement preserves the existing error ordering. Row 15, both writer-retirement halves and explicit deferrals are useful corrections. **One change:** finish the table at item level, giving each remaining effect its exact token signature, all callers, admission boundary, refusal handling and witness, with an explicit outer-batch/inner-door measurement relationship for presentation.

## P4 — not met

(c)4 permits each pinned helper to call “the next pinned member and non-vocabulary items.” A new non-vocabulary helper can call an admitted door through another helper without violating those direct-edge checks; the fence still does not establish the promised prohibition on extending indirect paths. Its supposedly exact baseline also omits the polling `sleep` already in both video `Engine::shutdown` bodies, and `PtySession::drop` finishes the input dump **before** calling `shutdown`, not through the ordered chain shown. Adding `Shutdown`, correcting static endpoint lifetime, detaching the PTY writer and naming repayments meet those parts of P4. The trace build-error cleanup remains conditional; this review does not establish a reachable constructor failure or an ordinary endpoint drop at quit. **One change:** replace the permissive helper rule with a closed, reviewed edge-and-effect inventory, including the actual ordering/counts, rejecting unlisted helper edges and effect-count changes; use the stated mutations and today's complete chains as its controls.

## P5 — met

(c)5 permits “`Exiting → Running`, **or idempotent from `Running`**” and makes the build-error arm call `exiting()` before returning. `Quit::answer(Cancel)` and incomplete `Quit::saved` both reach `settle_quit`'s Abandon arm without Write; the new rule therefore removes the ordinary-gesture violation. Write refusal still rolls back from Exiting, and successful retirement stays there. The explicit build-error transition supplies the retained trace guard's conditional cleanup contract without widening ordinary doors to Starting. Its real-driver cases meet the requested acceptance obligation, subject to P1's ticket allocation.

## P6 — met

(c)6 explicitly places transport effects “outside the admission invariant,” with named debt and per-function registrations, and says standalone `recv_timeout` calls use “A2's worker `wait` doors like any worker's.” The inspected ring waits, retirement condition variable, join/reap polling and dump-publisher sleep/sync justify that narrow exception; it preserves the manifest's lack of a `bt-platform` edge. The three standalone wait paths occur above Window entry and can use the specified once-per-process entry and lent context. The second constructor is explicitly pinned, superseding the earlier one-constructor rule. This is a declared contraction of budget §C-1, not an inference from Unset; the remaining `file_reads` execution design is correctly left to A2.

## P7 — met

(c)7 says an existing-role scope “changes nothing and restores nothing,” and makes `CallbackScope` private-field, `!Send` and `!Sync`. It removes the nonexistent image-generator completion and identifies the actual synchronous `generate` call, the `folioVideoDidPlayToEnd:` observer, `Notifier::show`'s Activated handler, and both callbacks installed by `DeviceResources::mint`. Those symbols and the render-to-platform dependency exist. Existing-role preservation, owning-scope unwind restoration, nested tests and scoped-transfer refusal meet P7 without adding the rejected worker-role backstop. The remaining callback symbols are explicitly required in A1a's brief.

## P8 — met

(c)8 requires “Each `compile_fail` case must be sensitive to removing the property it claims” and explicitly adopts the weaker property-isolating contract. Independent Send/Sync probes, scoped transfers, separate owned-token escape/storage/privacy/identity/consumption cases, box and function-pointer controls, the real `HandoffLane::spawn` refusal path, isolated counters/meters and duplicate Window-entry preservation answer P8. Typed registry/signature checks replace the inappropriate syntax proofs. This is an explicit departure from budget §C-6's diagnostic-reason requirement, consistent with CONVENTIONS' mutation-sensitive proof requirement; it claims neither executed acceptance nor reason-specific diagnostics. P1 still governs when the executed worker cases can land.

## Dispatch

**No.** Before dispatching A1a, correct P1's test allocation, replace P2's lossy return-context mechanism, and finish P3's identities and boundaries so A1a can generate an unambiguous registry. P4 must be corrected before A1e is dispatched. The coordinator's (c)9 priority ruling explicitly authorizes the future RULES 53 exception and retains scheduling debt; it does not change the owner's requirement that D-2 closes only after completed A1, A2 and A3. No wider review is requested here.
