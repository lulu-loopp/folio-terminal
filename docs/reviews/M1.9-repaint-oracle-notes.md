我先读取任务提示、仓库约束和当前工作树，再按文档定义实现 A/B/C。整个过程只加诊断与测试能力，保留用户现有改动；完成后会跑指定门禁、把入口与红测证据写入 output，并停在未提交状态。
### Shell: `"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$files = @('"'docs/prompts/codex-M1-9-repaint-flash-oracle.md','AGENTS.md','.agents/AGENTS.md'); f`
execution error: Io(Custom { kind: Other, error: "windows sandbox: runner failed during SpawnChild: CreateProcessAsUserW failed: 1312 (A specified logon session does not exist. It may already have been terminated.) | cwd=D:\\Developer\\BetterTerminal | cmd=\"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe\" -NoProfile -Command \"try { [Console]::OutputEncoding=[System.Text.Encoding]::UTF8 } catch {}\n$files = @('docs/prompts/codex-M1-9-repaint-flash-orac
首次只读检查被 Windows 沙箱的登录会话错误挡住了（尚未执行任何仓库操作）。我正在用同一范围重试；若仍失败，会改用更小的单条读取来定位环境问题。
### Shell: `"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command "Get-Location; Get-Content -LiteralPath 'docs/prompts/codex-M1-9-repaint-flash-oracle.md' -Raw"`
execution error: Io(Custom { kind: Other, error: "windows sandbox: runner failed during SpawnChild: CreateProcessAsUserW failed: 1312 (A specified logon session does not exist. It may already have been terminated.) | cwd=D:\\Developer\\BetterTerminal | cmd=\"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe\" -NoProfile -Command \"try { [Console]::OutputEncoding=[System.Text.Encoding]::UTF8 } catch {}\nGet-Location; Get-Content -LiteralPath 'docs/prompts/c
### Shell: `"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -NoProfile -Command "Get-Content -LiteralPath 'docs/prompts/codex-M1-9-repaint-flash-oracle.md' -Raw"`
execution error: Io(Custom { kind: Other, error: "windows sandbox: runner failed during SpawnChild: CreateProcessAsUserW failed: 1312 (A specified logon session does not exist. It may already have been terminated.) | cwd=D:\\Developer\\BetterTerminal | cmd=\"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe\" -NoProfile -Command \"try { [Console]::OutputEncoding=[System.Text.Encoding]::UTF8 } catch {}\nGet-Content -LiteralPath 'docs/prompts/codex-M1-9-repa
范围已确认：A 会在 `bt-pty` 读线程做纯旁路原始字节 dump，并自动生成含到达时间/块长度的 `.chunks` 清单；B 会提供一个无窗口的回放 oracle，逐发布帧输出 rendered/source 状态并对“已渲染后又露源码”返回非零；C 会复用同一帧分类器做确定性交互重绘测试。接下来开始改文件，不触碰公式几何或检测实现。
A/B/C 的首版入口已经落地：原始 dump + `.chunks`、headless 逐帧回放器、以及复用同一 oracle 的交互重绘测试。现在先做格式化和小范围编译/测试，重点检查无窗口回放的借用关系、清单边界校验，以及 C 在 M1.9m 基线上的真实状态序列。
小范围结果已经确认两点：dump 的原字节与块清单测试通过；C 在 M1.9m 的已知 TUI 同内容点击/双击重绘序列上通过（Ready 后未露源码）。我还修正了一个测试夹具投影问题。下一步会在 `D:\tmp` 的隔离副本叠加现有 M1.9n stash 跑同一条 C；不会改动或弹出仓库里的 stash。
隔离副本的首选临时根被执行环境拒绝写入（尚未创建任何文件）。我改用仓库内一个明确命名、跑完即删除的临时目录来完成同样的只读验证；路径会先固定校验，不会触碰源码目录或 stash。
M1.9n 隔离副本已成功套用 patch，当前仍在首次离线编译（临时副本没有复用主工作区 target，所以比普通测试慢）。主工作区和 stash 均未变化；编译完成后我会立即记录同一 C 的退出码与帧序列结论并清理副本。
M1.9n stash 在当前这条“同位置重绘”夹具上也通过，因此它不是能稳定触发 stash 缺陷的那组真实字节；我不会把它误报成红证据。按任务书允许的备选方案，我会在隔离副本里只插入一帧 `show_source` 闪回，再跑完全相同的 C，验证 oracle 必须因 Ready→Source→Ready 序列而红；正式工作区不保留该注入。
红证据已拿到：注入单帧后同一 C 以 exit 101 失败，序列明确是 `Source → Rendered → Source → Rendered → Rendered`，并报告 `flashed={"x"}`；未注入的 M1.9m 与 M1.9n 同位置夹具都为 exit 0。现在清理该隔离副本（只删刚创建的 `.tmp-m1n-oracle-*` 目录，可由 git/archive 重建），随后继续全门禁。
实现自审完成：正常 PTY 循环现在在进入读循环前一次性分流，未设置 `BT_PTY_DUMP` 时循环体与原实现等价（无 dump 分支、时钟或文件调用）；dump 初始化也已移到子进程创建前，避免无效路径造成已启动子进程的异常清理风险。开始跑全 workspace 离线测试，之后依次跑 clippy、fmt、adapter boundary 与 vendor/glyphon 差异门。
全 workspace 门已绿：共 522 passed、0 failed、1 ignored（ignored 是既有 Windows 交互项），新增 C 包含在其中。现在并行意义上接着跑静态门：clippy `-D warnings`、fmt check、adapter boundary；随后从原始日志提取 310 corpus 与 G1/G2/G3 的具体数字。
所有正式门禁已绿，headless 端到端演练也得到预期三帧：`Source`（初始）→`Rendered`（稳定）→`Rendered`（重绘），exit 0。现在只剩整理准确 file:line、删除两份临时 gate 日志，并把完整交付写入仓库根 `output`；不会提交。
最终自审发现一个值得收紧的点：红判定原先用“源码平面包含已渲染 source”匹配，像已渲染 `x` 与另一条 `$$x+y$$` 可能产生误报。它不会漏掉当前红证据，但不够精确。我会改成 delimiter-aware 的整块匹配并加反例测试；这是 oracle 自身准确性修正，不触碰产品逻辑，然后重跑门禁。
已完成 A/B/C，并写入 [output](/D:/Developer/BetterTerminal/output)。

- A：`BT_PTY_DUMP` 原始 VT 旁路及 `.chunks` 时序清单，正常路径循环零额外分支。
- B：headless 逐帧 oracle，输出 `Source/Rendered/Mixed` 序列，检测闪回时非零退出。
- C：交互重绘回归；M1.9m 正常通过。注入单帧闪回后 exit 101，捕获 `Source → Rendered → Source → Rendered → Rendered`。
- 门禁：523 passed、0 failed、1 ignored；310/310 corpus；clippy `-D warnings`、fmt、adapter boundary 全绿；vendor/glyphon 零改。
- 未提交；已有未跟踪文档和 `scratch-audit2.md` 均未改动。

停下等审。