# 备用屏公式呈现 · 第二次独立审因(只读,给方案)

## 角色与硬约束

只读独立审计,**不许改代码、不许提交**。协调者在闪回上已栽两刀(M1.9j 缓存、
M1.9n 发布前 reconcile——后者交互重绘时把块 band 算成只盖内容行、`$$` 露成源码,
静态门禁全绿但真机一点就损坏,已 stash)。现在停下盲改,请你独立给**方案**。
先读审因报告 `docs/reviews/M1.9-formula-pipeline-audit.md` 与 `git log --oneline`
里 m1.9k/m 的提交说明。当前 HEAD = `20846b1`(M1.9m)。

## 用户拍板的方向(评估 + 落地,可挑战)

**去掉备用屏的自适应行高。** 用户观察:备用屏是 Claude Code 完全掌控的**固定
N×M 栅格**,上下都有它固定的 UI(输入框、状态、Jump-to-bottom)。可变行高
(free-height)只该用在**主屏**(我们自有的滚动区);在备用屏强行可变行高,才
产生了顶部空白、输入框上移、底部锚定那一堆复杂度与 bug(image 220 顶部空白为证)。

**候选新模型:备用屏固定 cell 行高;公式渲染时缩放/裁放到"塞进它占的源码 band
(N 行 × cell)"内、居中;不改栅格总高。** 请评估:
- 这样是否消除顶部空白 / 输入框上移 / 底锚,并与"终端坐标不变量①"一致?
- 代价(短公式在自己 band 内局部留白;超高公式需缩放)是否可接受、如何取舍;
- M1.9m 的哪些 alt 几何要撤(底锚、按 raster 撑高/收拢),哪些保留(主屏的净高+
  padding 模型、show_source 按内容保持、padding 可配);给 file:line;
- 若你认为"固定行高塞进 band"不如另一个模型,给出并论证。

## 要根因的闪回

1. **双击公式所在行 → 闪回源码**(M1.9m 就有,独立于行高)。查:双击是否触发了
   选区 / `show_source` 切换 / 强失效?给 file:line 因果链;
2. **任意重绘闪回**(滚动/点击):M1.9m 为何仍闪(重绘→内容指纹变→失效→稳定
   窗口内先发源码);M1.9n 的 reconcile 为何交互时把 band 算错(只盖内容行)。
   给**稳健修法**的方向:协调者倾向"**内容寻址失效**——重绘时若公式源码未变,
   **保留现有装饰(连其正确 band)、根本不删不重检测**",而非 reconcile-重装。
   评估这个方向对①原地重绘②平移重绘(滚动)分别成不成立、边界在哪、file:line。

## 交付(写进 output 文件,不写代码)

- 备用屏固定行高模型的落地方案(撤哪些/留哪些,file:line)+ 取舍;
- 双击闪回、重绘闪回的因果链(file:line)与稳健修法(内容寻址失效 vs reconcile
  的裁决);
- **有序实现路线**(每片一句范围 + 依赖),标出哪片先做能最快让"双击/点击不闪";
- 如与既有不变量(坐标①、投影②、M1.9k 两条红线、帧铁律 B、N rows above)有冲突,
  明说。

只读、不改、不提交。停下等协调者拿方案决策。

Claude-Session: https://claude.ai/code/session_01M8B2ZEM1UsvgLCidRXEpvR
