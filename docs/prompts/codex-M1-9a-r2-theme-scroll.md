# m1.9a-r2 返工单:公式白底 + 上滚被拽回(xhigh,滚动案定性优先)

## 症状(用户实测,29d72aa 构建,真窗口)

1. **公式块白底**:暗主题终端上,渲染出的块是"白纸黑字贴纸"
   (用户截图:`'$$E = mc^2$$'` 定稿后渲染,白色矩形块)。
2. **上滚被拽回底部**:向上滚动时视口会自己跳回下方,只有滚得很快才能
   逃出去。**昨天的构建(7147911)没有此行为**——差分指向本片新增的
   公式块/高度树/发布路径。

## 案 1:主题化渲染(机理已明,照单修)

- Typst 排版输出必须**透明页面底 + 前景墨色来自主题**(暗主题
  --ink 白系、亮主题黑系;主题/BT_BG 变化时重排——复审低危 C 提前
  兑现:纹理缓存键补 fg/theme 维度,或沿用整表 clear 但把 fg 纳入
  失效触发)。
- SVG→resvg 栅格链路里所有"默认白底/默认黑字"的环节逐一排掉;
  预乘 alpha 与 wgpu 混合正确(块下面是终端背景,必须透出)。
- 回归:同一公式在暗/亮两主题下栅格不同且底透明(断言角落像素 alpha=0、
  墨色像素与主题前景一致);310 语料门禁照跑。

## 案 2:上滚被拽回(定性优先,不许猜修)

先复现钉死因果链再动手。嫌疑按序:

1. **块高度进入高度树与滚动锚定的交互**:块替换源行后高度变化,
   Anchored 视口的锚点/偏移换算是否在每次发布时把视口推回 Bottom;
2. **worker 完成→发布路径**是否重置视口跟随状态(发布帧时错误地
   走了"键盘输入跳底"或 Bottom 默认);
3. 与 m1.8 的 Bottom|Anchored 铁律对照:鼠标滚动产生的 Anchored 态,
   在无用户输入的后台发布(math ready、spinner)下**必须原地不动**
   ——找出哪条路径违反了它。
- 复现器:lifecycle 矩阵造"含已渲染块的转录 + wheel 上滚 + 后台发布
  (math ready / 普通输出)",断言视口锚不漂移;真窗口用
  scripts/dev/ui-probe.ps1 wheel + BT_PERF_TRACE 对时间线。
- 修复必须语义级;m1.8 系六个回归与呈现等价门不得回退。

## 顺手(复审低危 B,一行)

- bt-detect detect_math_blocks(:263 附近):单行块侦得后复位 `opening`,
  杜绝「lone $$ → $$x$$ → lone $$」重叠块;补该形态专测(复审低危 A 的
  四类拒绝专测一并补上:嵌套/空块/多行未闭合/相邻空块)。

## 边界

- vendor/glyphon 零改;m1.8d/r2 批帧与无变化跳过语义不动;
  310 语料、G1/G2/G3、全 workspace 门禁必须全绿。
- 工作树 docs/prompts/* 不动。结果写在最终答复里(含案 2 因果链
  file:line、修前/修后行为、门禁数字、审阅入口)。停下等审,不提交。
