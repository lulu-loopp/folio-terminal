# 公式渲染全链路 · 独立审因(只读,不许改代码)

## 你的角色与硬约束

这是一次**独立审计**,不是返工单。协调者从 m1.9e 一路补到 m1.9j,每修一条
用户就发现新的一条——典型的逐症打地鼠。现在**停下所有修补**,请你以**全新
视角、只读**方式把公式渲染链路查透。

- **绝对不许修改任何代码、不许提交。** 只读分析。若沙箱只读档位限制写操作,
  正好。
- **不接受协调者的结论为定论。** 下面列的失败模式和疑似根因是协调者的观察,
  你**必须独立复核,可以推翻**。给出你自己的 file:line 级因果链。
- **允许推翻现有架构。** 如果检测器该重写、M1.9j 的缓存方向不对、alt 屏
  "每次重绘都重新检测+稳定窗口"这个模型本身有问题——**直说,并给替代架构**。
- 产出是**审因报告**(写进 output 文件),不是补丁。

## 当前工作树状态

- HEAD = `30a7aea`(m1.9i:备用屏公式检测 + 颜色 env)。
- 工作树有**未提交的 M1.9j 改动**(`bt-term/session.rs` 的 live-artifact 缓存
  + show-source 源码列跨度修正,`bt-render`/`bt-viewport`/`bt-app` 配套)。
  **按当前工作树(含这些未提交改动)分析。**

## 已知失败模式(协调者观察,你独立复核 / 可推翻)

用户用一份丰富的 LaTeX 语料在 Claude Code(备用屏)里实测,现象:

**FM1 · 多行"内联 `$$`"块完全不检测**
- 形态:`$$` 与内容**同一行**、块**跨多行**,例:
  第 1 行 `$$\oint_{\partial \Omega} \mathbf{F} \cdot d\mathbf{r} = \iint_{\Omega} (`
  第 2 行 `\nabla \times \mathbf{F}) \cdot d\mathbf{A}$$`
- 协调者静态探针(无滚动无缓存)复现:此类块**停在源码不渲染**。
- 疑似根因:`detect_block_math`(`crates/bt-detect/src/lib.rs:364`)只认**单行**
  `$$X$$`;`detect_math_blocks_in_context` 的多行配对只认 `$$` **独占一行**
  (`whole_line_display_delimiter`)。`$$` 与内容同行且跨行的形态**两条路都不
  覆盖**。而这是 Claude Code 实际输出格式之一。
- **请核实,并判定这是检测器需要扩展形态覆盖,还是需要更根本的重写。**
  一并盘点:还有哪些真实 Claude Code / 标准 LaTeX 形态没覆盖(`\[`/`\]` 内联跨
  行?`\begin{env}` 与前后文同行?)。

**FM2 · 备用屏滚动/回看时公式退回源码**
- 用户上滚回看,已渲染的公式退回源码(image 214 多个块为源码)。
- 协调者判断:M1.9j 缓存只省了**重渲染时间**,没绕过**稳定窗口**——alt 屏每次
  重绘都 `invalidate` → 要等 `LIVE_MATH_STABLE_INTERVAL` 才重新检测 → 这段窗口
  显示源码。缓存命中在 `take_live_worker_task`(`session.rs`)发生,而它在稳定
  窗口之后才被调度。
- **请核实这条时序,并回答架构问题**:要做到"滚动零源码闪现",是否必须在
  **重绘那一帧同步检测+命中缓存即刻装回**(绕过稳定窗口)?这样做对非公式
  内容(spinner/部分输出)的误检风险有多大?现有的
  内容指纹快路径 / `preserve_live_after_top_scroll` / M1.9i-B 的
  `alternate_detection_context` 是否已提供了可复用的地基?给出可行架构。

**FM3 · 多行块渲染错乱(与窗口尺寸相关)**
- image 213:一个多行 `pmatrix` 块出现"渲染的 `(x y)` 带一条横线,压在源码
  文字上"的**重叠/错位**;用户报"与窗口大小有关"。该块源码用了**单反斜杠**
  `\ `(而非 `\\`)分隔行——即输入本身可能 malformed。
- **请判定**:这是 (a) malformed 输入下 Typst 的退化渲染 + 我们对"部分渲染/
  失败"的呈现不干净;还是 (b) 多行块的高度/band/裁剪/`left_subpixels` 布局
  bug(尤其 M1.9j 改了 source 显示的列跨度与 `left_subpixels`,查是否波及);
  还是 (c) 与窗口宽度相关的换行/横溢出处理缺陷。给 file:line。

**FM4 · 你自己发现的其它**
- 基线跑偏、show-source、复制语义、失败态呈现、live↔frozen 交接、内容指纹
  误判……凡你在链路里看到的失败或脆弱点,都列出来。

## 复现素材

- 既有回归语料:`test-assets/latex-render-check.md`。
- 用户的丰富语料含:多行内联-`$$`(FM1)、`\begin{pmatrix}`/`\begin{cases}`/
  `\begin{align}`、上下标与大算符(`\sum`/`\int`/`\oint`/`\iint`/`\lim`)、
  `\text{}` 内 CJK、行内 `$...$`(应保持禁用)、`\left(...\right)` 括号缩放。
- 可用 `BT_PROBE_INPUT`(VT 字节文件,alt 屏 `\x1b[?1049h`)静态复现渲染;
  滚动类(FM2)需交互,可从代码时序论证。

## 交付(写进 output 文件)

1. **失败模式全表**:FM1-FM4 + 你新发现的,每条一句现象;
2. **每条的独立因果链**(file:line),并对协调者疑似根因给**裁决**(证实/推翻);
3. **架构级结论**:
   - 检测器形态覆盖——扩展够不够,还是重写?给出应支持的形态全集;
   - "滚动零闪现"——同步检测+缓存即刻装回是否必要、是否安全、地基是否已在;
   - M1.9j 缓存方向对不对(保留 / 改造 / 丢弃),给理由;
   - alt 屏"重绘即重检测"模型是否要变;
4. **修复路线建议**:把上面切成有序、互不纠缠的片(哪条先做能最快让"没渲染"
   好转),每片一句范围 + 依赖关系。**只给路线,不写代码。**

再次强调:**只读、不改、不提交。** 停下等协调者拿你的报告决策。

Claude-Session: https://claude.ai/code/session_01M8B2ZEM1UsvgLCidRXEpvR
