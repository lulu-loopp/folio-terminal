# Markdown 样张 Sample

## 二级标题 with **bold** and `inline code`

### 三级标题

#### 四级标题(现渲染器不支持,应显示为标题而非原文)

正文段落,中英混排 mixed prose,带 **粗体** 与 `code`,还有一段足够长的句子让窄 pane 必须折行才放得下它,so the pane has to wrap this sentence somewhere in the middle.

- 无序列表一
- 无序列表二 with `code`
* 星号列表

1. 有序列表一(现渲染器不支持)
2. 有序列表二

| 列 A | 列 B | 列 C |
|---|---|---|
| 表格 | 现渲染器 | 不支持 |
| cell | cell | cell |

```rust
fn main() {
    println!("fenced code with lang badge");
}
```

```
no-lang fence
```

> 引用块(现渲染器不支持)

[链接文本](https://example.com)(现渲染器不支持)

---

水平线之后的尾段。
